# Chinmay Astro — Workflow Registry
**Version:** 2.19 | **Last Updated:** 27 May 2026 (sprint `pre-go-live-final-follow-up-2026-05-26` Batch 4 COMPLETE — TD-PGF-02 + TD-PGF-03 landed)

### 2026-05-27 — Sprint pre-go-live-final-follow-up Batch 4 (COMPLETE — 2 of 2 done)

- **TD-PGF-03 WF-11 internal `message:` → `messageText:` rename — landed (2026-05-27T05:06:30Z).** Renamed intermediate producer→consumer field across 3 producer Code nodes (`Format List` ×2 return-object keys, `Format Stats` ×1, `Prepare HELP Text` ×1) + 3 paired consumer `executeWorkflow` mappings (`Send List To Admin`, `Send Stats To Admin`, `Send Help To Admin` — all now `{{ $json.messageText }}`). Brings WF-11 internal vocabulary in line with canonical `{channelId, messageText}` Slack-envelope contract (design.md §2.4). 19 nodes unchanged structurally. jq-on-disk Python transform → single curl PUT. MCP strict `valid: true` (0 errors, 30 advisories all pre-existing). Lint clean (0 hard rejects, 9 advisory Contract-First on pre-existing Code/Switch/Postgres upstreams). Backup at `archive/backups/GoTYo0GS2y8qjjkw-2026-05-27-15-05.json`. `.pseudo` untouched (parametric — pseudo describes only WF-10→WF-11 INPUT contract, not internal intermediate names). Sibling regression: corpus-wide grep for `$json.message }}` consumer pattern returned zero hits — WF-11 was the lone case.
- **TD-PGF-02 WF-00 `nfm_reply` parse branch — landed (2026-05-27T05:01:35Z).** Added `else if (message.interactive.nfm_reply)` branch inside `case 'interactive':` of `Parse WhatsApp Message` jsCode. Option B locked per TD-PGF-01A — captures raw `nfm_reply.response_json` cleartext into `messageContent` (Flow is in send-only mode; no encryption-svc needed). Single `patchNodeField` partial-update. WF-00.pseudo Step 2 already specifies this case (line 25) — parametric implementation catch-up, no pseudo edit. MCP strict `valid: true` (0 errors, 15 pre-existing advisories). Downstream WF-22 was already designed for the cleartext JSON string per Batch 2.5 TD-PGF-13 `||→??` fix. Backup at `archive/backups/JQu1MkK5vgtUCeNO-2026-05-27-15-01.json`.
- **Batch 4 complete (2026-05-27T05:08:08Z).** 2 of 2 P1 surgical edits landed. Post-batch dependency map rebuilt. Sibling regression clean — no other workflow reads `$json.message }}` as the messageText source; nfm_reply branch is unique to WF-00. Next: Batch 5 (TD-PGF-07 + TD-PGF-08 — P3 cosmetic + envelope-everywhere completion in WF-10 + WF-45).

### 2026-05-27 — Sprint pre-go-live-final-follow-up Batch 3 (COMPLETE — 8 of 8 workflows done)

- **TD-PGF-05 WF-32 phoneNumber read — landed (2026-05-27T04:45:08Z).** `Prepare Reassurance Message` Code node now reads `phoneNumber` from canonical top-level envelope (`$('When Executed by Another Workflow').item.json.phoneNumber`) instead of the nested `user.phone_number` DB-row path. Cosmetic alignment with §2.1 canonical envelope. 10 nodes unchanged.
- **TD-PGF-05 WF-50 Prepare Payload fallback tighten — landed (2026-05-27T04:44:29Z).** Removed redundant fallback chain `(input.messageContent ?? input.message ?? input.messageBody) ?? null` → `input.messageContent ?? null`. Canonical envelope is now enforced upstream; legacy `message` / `messageBody` aliases no longer needed. No other changes.
- **TD-PGF-05 WF-40 envelope add-on — landed (2026-05-27T04:43:57Z).** Extended `Call WF-25 (Intent Classifier)` `workflowInputs.value` from 4 → 6 fields, adding `userName` (`($('When Executed by Another Workflow').item.json.user || {}).name`) and `slackChannelId` (`...user || {}).slack_channel_id`) per data-contract principle. No other changes — WF-40 was explicitly excluded from TD-PGF-09 halt-and-notify scope (Decision #2: WF-25's stopAndError propagates and halts WF-40 naturally; STOP-clarifier branch is structurally protected via sentinel mismatch).
- **TD-PGF-05 + TD-PGF-09 WF-43 combined PUT — landed (2026-05-27T04:42:48Z).** Populated `Call WF-25 Intent Classifier` `workflowInputs.value` from empty (passthrough-via-trigger) to explicit 6-field mapping. Added halt-and-notify chain (9 new nodes, same shape as WF-23/WF-30) on `Gemini General Response` error branch. 21 → 30 nodes. MCP runtime-profile validate clean. WF-43.pseudo revised — extended Algorithm with Steps 13-24 covering halt-and-notify path.
- **TD-PGF-05 + TD-PGF-09 WF-31 combined PUT — landed (2026-05-27T04:40:44Z).** Populated `Call WF-25 Intent Classifier` `workflowInputs.value` from empty (passthrough-via-trigger) to explicit 6-field mapping. Added halt-and-notify chain (9 new nodes) on `Gemini General Response` error branch (Branch A only — Branch B Slack admin relay unaffected, fires in parallel regardless of intent). 15 → 24 nodes. WF-31.pseudo revised with renumbered Steps 1-24 linear (Branch A halt-and-notify Steps 9-17; Branch B renumbered 21-24).
- **TD-PGF-05 WF-44 envelope add-on — landed (2026-05-27T04:36:42Z).** Extended `Call WF-25 Intent Classifier` `workflowInputs.value` from 4 → 6 fields. No pseudo touch (parametric add-only). 9 nodes unchanged.
- **TD-PGF-05 + TD-PGF-09 WF-30 combined PUT — landed (2026-05-27T04:34:40Z).** Same halt-and-notify pattern as WF-23 (9 new nodes + onError flip on `Gemini General Response`). Additionally: extended `Call WF-25` mapping with 2 envelope fields (`userName` = `($json.user || {}).name`, `slackChannelId` = `($json.user || {}).slack_channel_id`); simplified `Is Pass-Through Intent?` IF from 4 conditions (notEquals each of {garbage, malicious_abusive, inappropriate, stop_intent} combined with AND) to a single condition `intentResult != "stop_intent"` — per TD-PGF-05 dead-branch cleanup, the other 3 intent values never return from WF-25 after the halt-inside-WF-25 redesign. Semantics preserved (TRUE=pass-through, FALSE=stop_intent). 12 → 21 nodes. MCP strict-validate `valid: true` (0 errors, 30 warnings — same pre-existing pattern set as WF-23). Backup at `archive/backups/gGJBY5fJha0Let8I-2026-05-27T04-32-08Z-manual.json`. WF-30.pseudo revised — renumbered Steps 1-20 linear, added halt-and-notify Steps 8-16, IF simplification documented in Notes.
- **TD-PGF-05 + TD-PGF-09 WF-23 combined PUT — landed (2026-05-27T04:30:31Z).** Extended `Call WF-25 Intent Classifier` `workflowInputs.value` from 4 → 6 fields (added `userName` = `($json.user || {}).name || ($json.pendingUser || {}).contact_name`, `slackChannelId` = `($json.user || {}).slack_channel_id`) per data-contract principle — WF-25 reads these for its own halt-and-notify alert template. Set `onError=continueErrorOutput` on `Gemini General Response` HTTP node; wired error branch to 9-node halt-and-notify chain mirroring WF-25's pattern (Build User Apology Payload Set v3.4 → Send Apology via WF-50 → Build Admin Alert Text Code v2 [General Response copy variant "AI assistant couldn't generate a reply"] → IF Has Consult Channel? v2.2 notEmpty unary → TRUE: Build Consult Alert Set v3.4 → Send to Consult via WF-51 → convergence at Build Admin Cmds Alert Payload → Send to Admin Cmds via WF-51 → Halt on Gemini Failure stopAndError v1; FALSE: skip consult-channel, direct to Build Admin Cmds Alert). 12 → 21 nodes. MCP strict-validate `valid: true` (0 errors, 30 warnings — all pre-existing project patterns + false-positive IF main[1] "error output" advisories + cachedResultName-missing per Step 5f.3 canonical shape). Backup at `archive/backups/VpCER0Vqq3NYJGpI-2026-05-27T04-26-36Z-manual.json`. WF-23.pseudo revised — renumbered Steps 1-20 linear, added halt-and-notify Steps 8-16, updated Notes with TD-PGF-09 + TD-PGF-05 entries. Optional-chaining bug caught + fixed mid-PUT: `?.` not supported in n8n `={{ }}` expressions → rewrote as `(... || {}).field` and re-PUT.
- **TD-PGF-09 WF-25 Gemini halt-and-notify rebuild — landed.** Replaced silent `Handle Gemini Error` fallback (default intent + return) with explicit halt-and-notify chain inside WF-25 itself. New error-branch flow: error → Build User Apology Payload (Set v3.4) → Send Apology via WF-50 → Build Admin Alert Text (Code) → IF `Has Consult Channel?` (slackChannelId non-empty) → TRUE: Build Consult Alert (Set v3.4) → Send to consult channel via WF-51 → Build Admin Cmds Alert (Set v3.4) → Send to admin-commands via WF-51 → Stop and Error; FALSE: skip consult-channel send, go directly to admin-commands. Pre-form users (no consult channel yet) get admin-only alert. Stop and Error propagates termination to all 6 callers (WF-23/30/31/40/43/44) via n8n executeWorkflow error-propagation semantics — NO caller-side IF bail-guards needed (design simplification approved this session over original "5 caller IF" approach). 14 → 22 nodes (+8 net: -1 Handle Gemini Error, +9 new). Plugin lint clean (0 hard rejects); n8n MCP validate_workflow strict-profile `valid: true` (0 errors, 39 warnings — all pre-existing patterns, intentional typeVersion floor per Step 5e.1a, or false-positive IF/Switch "error output" advisories). Backup at `archive/backups/eTV1lUcYrXBg2q2T-2026-05-27-03-57-manual.json`. WF-25.pseudo revised (renumbered Steps 1-15, removed tombstone, updated Ambiguities/Notes). 
- **Batch 3 complete (2026-05-27T04:45:08Z).** All 8 workflow PUTs landed: WF-25 (last session) + WF-23 + WF-30 + WF-44 + WF-31 + WF-43 + WF-40 + WF-50 + WF-32. TD-PGF-05 + TD-PGF-09 closed. Post-batch sibling regression: all 5 Gemini HTTP sites confirmed to have `onError=continueErrorOutput`. Adjacent finding (WF-25 Classify Intent retry=false) logged to `followups.md`. Next: Batch 4 (TD-PGF-02 + TD-PGF-03 — P1 surgical edits in WF-00 + WF-11).

### 2026-05-26 — Sprint pre-golive-gap-decisions (Gap-10 fan-out + pre-go-live P1 gaps; 11 done, 1 obsolete)

- **Gap-10 fan-out — systemic `mappingMode: passthrough` → `defineBelow + value: {}` recipe correction.** WF-01 unblocked first (4 Class A nodes + 9 Class B Code-node returns), smoke-tested green; then mechanical fan-out via jq+PUT across 23 P1/P2/P3 workflows (Batches 2+3). Mid-Batch-3 smoke caught `value: null` runtime TypeError → recipe corrected to `value: {}` (empty object) project-wide. Resolves the `WorkflowHasIssuesError` class that surfaced post-data-contract-Phase-1 PUTs on n8n 2.1.4. Plugin 1.32.0 released mid-sprint to document the corrected recipe + add `exec_passthrough_literal` and `code_node_terminal_return_shape` lint hard-rejects.
- **GAP-1 — WF-01 Silent Reject text → email redirect.** "We'll get back to you as soon as possible." + email callout to `chinmay_astro@gmail.com`.
- **GAP-3B — Email-channel callout in WF-22/32/42 messages.** Locked wording: "In the meantime, you can ask any general questions here, or email chinmay_astro@gmail.com if you need anything we can't help with right now."
- **GAP-7-STAGE1 — WF-31 paid-elapsed aging tag in Slack relay.** `Build Slack Payload` Code node now prefixes `⏱ Paid {n} min/h/d ago` from `payments.created_at` (latest row).
- **GAP-DCP-WF25 — WF-25 garbage+block warning canonical contract.** Closes the sole outstanding caller-contract gap project-wide.
- **GAP-DCP-WF45 — `.pseudo` §2.3 cite addition.** Pseudo-only polish; live code already compliant.
- **GAP-2 — "Done, thanks" 3rd post-consult button.** WF-42 `Prepare Feedback Message` buttons array gains `{id: btn_done, title: "Done, thanks"}`; WF-43 gains additive `Is Done Button?` IF + thank-you WhatsApp via WF-50 + "user tapped Done" Slack notification via WF-51. 16 → 21 nodes on WF-43. No state change.
- **GAP-3C — Distribute Gemini answer pattern to WF-23/30/31 + WF-43 paraphrase rewrite.** Each of WF-23/30/31 gained `Is General Enquiry?` IF (v2) → 4-node Gemini chain (Prepare Prompt jsCode → Gemini HTTP [gemini-2.5-flash-lite, temp=0.3, retryOnFail=true, maxTries=3, googlePalmApi cred] → Extract Reply → Send via WF-50 [defineBelow + value:{}]). WF-43's existing Gemini prompt rewritten to weave state-specific cue + email callout in paraphrase form; model URL bumped 2.0 → 2.5; temperature 0.7 → 0.3. **Gemini 2.0-flash-lite deprecated by Google; project standard locked at 2.5-flash-lite** (new memory `project_gemini_model.md`; CLAUDE.md Key Credential IDs table updated; WF-25 already on 2.5).
- **GAP-10-IMAGE-PIN — marked obsolete (deferred to post-MVP).** Docker `n8n-prod` `:latest` → digest pin moved to `followups.md` post-MVP per user direction; infrastructure-mutation scope kept out of go-live-critical-path sprint.
- **Plugin v1.32.1 + v1.33.0 — two new `build-workflow` sub-steps.** 1.32.1 adds Step 5f.6 (sibling-parity audit when replicating an existing node pattern — catches node-level hardening attributes like `retryOnFail`/`maxTries` that live ON the node and slip past parameter-only rollers; failure-case study from this sprint's GAP-3C Gemini-copy). 1.33.0 adds Step 6b (runtime-issue probe via `mcp__n8n__validate_node` strict-profile per-node — closes the gap between workflow-level MCP validation and n8n's runtime `getNodeParametersIssues`; failure-case study from this sprint's WF-01 Postgres operation-default).

### 2026-05-25 — Sprint data-contract-sprint-bug-fix (WF-26 re-engagement handler + data-contract hygiene)

- **BUG-NEW-02 closed — WF-26 Re-Engaged Opted-Out User Handler.** Opted-out users re-engaging via WhatsApp were previously routed to WF-21 form-issuer (UX regression: re-asked for onboarding data already on file). New flow: WF-01 opted-out branch loads full user row + emits §2.1 envelope (TD-DCP-105) → `Route Opted-Out to WF-26` (TD-DCP-107, retargeted from WF-21) → WF-26 (TD-DCP-106, new sub-workflow at n8n `tKjwTYF6EER8ED3y`). WF-26 lifts `users.status` from `opted_out` to `consultation_closed`, refreshes the §2.1 envelope's `user.status`, sends a personalized welcome-back WhatsApp via WF-50, and re-routes the inbound message through WF-02 so the first re-engagement message is answered in one turn. Design Q1-Q5 locked before build (`handoffs/handoff-td-dcp-105-done-wf26-build-pending.md`). Pseudo-first per `[[feedback_pseudocode_first_refactor]]`. WF-21's `wasOptedOut` prefix branch becomes dead code (defensive — left in place per TD-DCP-110 recommendation). TC-0607 flipped from ✅ Covered → ⏳ Pending re-verification (TD-DCP-109) — test user 30 (+61466927921) reserved in `opted_out` for next smoke session.
- **Data-contract hygiene (Phase 1 review followups).** TD-DCP-101 added `slackChannelId` mapping to WF-01 `Prepare User Data`. TD-DCP-111 expanded WF-10 `Load User Status` SELECT with `slack_channel_id` + `current_consultation_id`. TD-DCP-102 promoted `slackMessageTs` to always-required for `transport='slack'` in WF-60 (caller-compliance audited: WF-10 Slack-inbound + WF-51 Slack-outbound both pass `slackMessageTs` already). TD-DCP-104 added `userStatus` carry-forward to WF-20 `Normalize Keyword` (closes pre-existing TD-DRIFT-006: WF-47 STOP path orphan-consultation bug). TD-DCP-112 fixed WF-33 `Extract Command Data` writing `channelId` to `payments.verified_by` instead of `adminUserId` (closes TD-DRIFT-017). TD-DCP-113 reordered WF-47 atomicity — IF now sits at head of graph so close-consultation precedes user opt-out UPDATE (closes TD-DRIFT-007).
- **WF-52 contract hygiene.** TD-DCP-103 cleaned `Prepare Channel Name` in WF-52: removed legacy `phone_number` fallback, renamed `userName:` emit key to canonical `name:` (zero runtime behaviour change — internal dataflow only). Companion item **TD-DCP-110 (proposed WF-21 entry guard) marked obsolete** — over-scoped per design.md §3.3 audit; the design enumerates entry guards on routing boundaries (WF-02/§2.7, WF-11/§2.8) plus the 4 utility contracts, not on every direct-call leaf. WF-21's original justification (WF-01's opted-out branch caller) evaporated when TD-DCP-107 retargeted that branch to WF-26; post-Batch-3 WF-21 is a WF-02 downstream whose envelope is enforced at the WF-02 boundary.
- **Plugin follow-ups captured.** TD-DCP-PLG-001/002/003 logged for `flush-plugin-improvements` (upstream-mapping audit in subagent envelope briefs; forward-traceability scan in sibling regression; subagent review must diff against immediate pre-sprint snapshot, not historical state).

### 2026-05-23 — Sprint inline-20260522-102910 Batch 3 (doc/cleanup)

- **SP-04 — WF-23/30/31 stop_intent clarifier refactor.** Removed auto-unsubscribe on Gemini-classified `stop_intent` in WF-23 (Pre-Form Intent Filter), WF-30 (Payment Pending Intent Filter), and WF-31 (Payment Submitted Handler). For each: dropped `Is Stop Intent?` IF + `Call WF-47 Unsubscribe` executeWorkflow; inserted `Build WF-50 (Stop Clarifier) Payload` (Set v3.4) + `Call WF-50 (Stop Clarifier)` (executeWorkflow v1.2 canonical); rewired `Is Pass-Through Intent?` main[1] (FALSE) → Build → Call. All 3 use WF-40's verbatim clarifier text ("This is an automated message from Chinmay Astro — we noticed your message mentioned STOP..."). Users must now send literal STOP keyword via WF-20 to opt out canonically — protects against Gemini false-positives and aligns with Meta WhatsApp Business Policy. WF-47 callers reduced from [WF-20, WF-23, WF-30, WF-31, WF-43, WF-44] to [WF-20, WF-43, WF-44]. Implementation via Step 5e jq-on-disk PUT (one per workflow); 3 backups under `archive/backups/`-2026-05-23-19-13; lint clean post-PUT on all 3; dangling-name re-scan clean. Pseudos (WF-23.pseudo / WF-30.pseudo / WF-31.pseudo) revised earlier in the session (per `[[feedback_pseudocode_first_refactor]]`). Node count unchanged (7/7/10).
- **SP-09 — WF-12 Admin → WhatsApp Relay full purge.** Deleted from n8n (DELETE /api/v1/workflows/RjwHs9Dx5cK8Q5wD → 200) after 5-day deactivation cooling period (BUG-05 deactivated it 2026-05-18). Pre-purge JSON backup at `archive/backups/RjwHs9Dx5cK8Q5wD-2026-05-23-18-32-pre-purge.json`. Removed: `workflows/RjwHs9Dx5cK8Q5wD.json`, `docs/pseudocode/WF-12.pseudo`, `docs/pseudocode/WF-12.md`, table row in `docs/pseudocode/INDEX.md`, table rows in `docs/STATUS.md` and `docs/workflow-registry.md` (both occurrences). Dependency map regenerated (73 → 72 edges). Historical narrative references (this changelog, BUG-05 entry, TD-023 verification text) preserved as audit trail. **Resurrect path:** restore from `archive/backups/RjwHs9Dx5cK8Q5wD-2026-05-23-18-32-pre-purge.json` via POST /workflows (importing JSON gets a new ID — original `RjwHs9Dx5cK8Q5wD` will not be reused).
- **SP-07 / SP-08 — pseudo↔live drift remediation.** WF-51.pseudo rewritten to document the `Post to Slack → Build WF-60 Payload (Code) → Call WF-60 Message Logger` chain that TD-002 / TD-003 F3 added. WF-60.pseudo rewritten to match live multi-transport design (9-column INSERT incl. `slack_message_ts`, multi-key SELECT, TD-030 placement note clarifying WF-60 owns no bot-echo guard). WF-60 caller list in this registry expanded from "WF-00 (inbound) and WF-50 (outbound success + drop)" → "WF-00 (WhatsApp inbound), WF-50 (WhatsApp outbound success + drop), WF-10 (Slack inbound — admin/user commands), WF-51 (Slack outbound)".
- **SP-06 — marked obsolete.** Drift check on 2026-05-23T08:28:03Z found WF-46.pseudo already comprehensively rewritten as a SP-03 cascade side-effect (archive removed, `Calls Sub-Workflows: WF-51` set, admin_actions deprecation noted, caller refs updated to WF-11 + WF-25, DR-10 contradiction self-flag removed). No further action needed.

### 2026-05-18 — Smoke-post-p0-review-tc04xx sprint (5 bugs + 3 plugin guardrails)
**Source:** user_journey_map.html v2.1 + live n8n audit (Mumbai VPS, Apr 2026) + design clarifications sessions 3–4

### 2026-05-18 — Smoke-post-p0-review-tc04xx sprint (5 bugs + 3 plugin guardrails)

Closed the smoke-test-derived P0/P1 backlog (sprint slug `smoke-post-p0-review-tc04xx-2026-05-18`, all four batches done; sprint state at `docs/artefacts/sprints/smoke-post-p0-review-tc04xx-2026-05-18/state.md`):

- **BUG-02 (WF-43):** `Gemini General Response` HTTP node — jsonBody converted from raw-string interpolation to whole-body object-expression `={{ {contents:[...]} }}` so n8n JSON-encodes natively. Fixes "JSON parameter needs to be valid JSON" on prompts containing quotes/newlines/braces. Sweep showed only WF-43 had the anti-pattern.
- **BUG-01 (WF-10, WF-21, WF-22, WF-34, WF-60):** Postgres `options.queryReplacement` converted from comma-separated expression string to JS-array form `={{ [a, b, c] }}` on 5 user-controlled-text nodes. Pre-existing lint debt swept in the same PUTs (WF-22 3 executeWorkflow nodes canonical-1.2 fields; WF-34 `User Found?` IF singleValue:true).
- **BUG-03 (WF-43):** `Prepare Gemini Response Prompt` jsCode — `${d.messageText}` → `${d.messageContent}` (callers pass messageContent; prior reference produced literal "User: undefined" in the Gemini prompt).
- **BUG-04 (WF-25, WF-43):** Root cause was a misconfigured query-auth credential, not "null intent" as the smoke-test report hypothesised. (a) Both Gemini HTTP nodes switched from generic httpQueryAuth to predefined `googlePalmApi` cred. (b) WF-25 Prepare Intent Request now reads userStatus from `input.userStatus || input.user?.status || 'unknown'`. (c) Parse Intent + Handle Gemini Error route `consultation_closed + uncertain → feedback_intent` (defensive fallback for genuine Gemini outages).
- **BUG-05 (WF-12):** Admin → WhatsApp Relay deactivated — orphaned (0 callers in dependency-map.md), superseded by WF-41. Doc reconcile across `workflow-registry.md`, `CONTEXT.md:144`, and `STATUS.md:109,133,158` to remove the prior four-way "Not Built" / "Active" disagreement.
- **Plugin v1.17.0 — three new technical-workflow-review guardrails** (C13 HTTP raw-string jsonBody, C14 Postgres comma-string queryReplacement, C15 orphaned-active-workflow detector). Each encodes the corresponding bug class as a pre-go-live static check. Plugin commit 456513a; active cache rolled to 1.17.0.

Commits: `2a33905` (Batches 1+2: BUG-01/02/03/04), `3451197` (Batch 3: BUG-05).

### 2026-05-18 — Canonical executeWorkflow shape sweep (residual 10 workflows)
- **26 executeWorkflow nodes across 10 workflows** rolled to canonical n8n 2.1.4 tv-1.2 shape via idempotent jq roller. Same pattern as 2026-05-17 sweep (`source:"database" + operation:"call_workflow" + mode:"once" + workflowId:{__rl,value,mode:"list",cachedResultUrl} + workflowInputs:{mappingMode:"passthrough",...}`) — this sprint covered the 10 workflows the 2026-05-17 sprint did not touch. Triggered by smoke test surfacing WF-31 `Call WF-25 Intent Classifier` runtime failure ("No information about the workflow to execute found"). Workflows touched: **WF-12, WF-20, WF-23, WF-25, WF-30, WF-31, WF-40, WF-44, WF-45, WF-47**. WF-47's nodes were at tv=1.0 (older than the rest) and bumped to 1.2 by the same roller.
- **3 superseded workflow JSONs moved to `archive/superseded-workflows/`** — `fdlIpl67amL2Ho6U` (BACKUP_20260412_WF-25 Post-Consultation Options), `yIZwO3CZk6bOBAXl` (BACKUP_20260412_WF-30 pre-consent), `z6as85o3b1zK22eF` (WF-30 New User Onboarding WRONG-DEACTIVATED). The `workflows/` directory now mirrors live n8n state (28 files).
- **`docs/pseudocode/WF-XX.md` companion files regenerated** from fresh JSON exports post-sweep so the canonical 1.2 shape is reflected. INDEX.md timestamp updated to 2026-05-18; manual workflow descriptions preserved.

### 2026-05-17 — Post-P0 smoke test fixes
- **47 executeWorkflow nodes across 14 workflows** restored to canonical n8n 2.1.4 tv-1.2 shape: `source:"database" + operation:"call_workflow" + mode:"once" + workflowId:{__rl,value,mode:"list",cachedResultUrl} + workflowInputs:{mappingMode:"passthrough",...}`. Required by n8n 2.1.4 — prior P0 sprint had stripped `__rl` workflowId objects to plain strings per a stale lint hook, causing "No information about the workflow to execute found" errors at runtime. Workflows touched: WF-00, WF-01, WF-02, WF-10, WF-11, WF-21, WF-22, WF-33, WF-34, WF-42, WF-50, plus 3 sub-workflows (3va0M06kijgyLejf, 6PzJRZsF7k2d9hV7, UV62An60fzflU0uD).
- **WF-00 wiring fix** — Gather Message Info now branches in parallel to both Call WF-60 (fire-and-forget log) and Call WF-01 (routing), matching pseudocode Step 8a/9 intent. Previous P0 sprint had wired Call WF-60 → Call WF-01 inline, causing WF-01 to receive `{logged: true}` instead of the parsed message data.

---

## Status Key

| Status | Meaning |
|---|---|
| 🟢 Active | Deployed and active in n8n — confirmed live Apr 2026 |
| 🟡 Built (Inactive) | Exists in n8n, imported but not activated |
| 🔴 WRONG — Deactivate | Active in n8n but implements incorrect logic — must be deactivated before go-live |
| 🔵 Placeholder | Exists in n8n with single trigger node only — needs full implementation |
| 🔨 Rebuild Required | Exists in n8n — logic needs verification/updating before activation |
| ⚫ Superseded | Wrong design — deactivated, kept only for reference |

## Priority Key

| Priority | Meaning |
|---|---|
| 🔴 P1 — Critical | System cannot function without this |
| 🟠 P2 — Core | Required for complete user journey |
| 🟡 P3 — Supporting | Improves experience; degraded but functional without |
| ⚪ P4 — Maintenance | Post go-live / background hygiene |

---

## Naming Convention

| Range | Domain |
|---|---|
| WF-0x | Infrastructure — entry point, routing, security |
| WF-1x | Admin — Slack-side command handling |
| WF-2x | Onboarding — new user, form, birth details |
| WF-3x | Payment — confirmation, approval, rejection |
| WF-4x | Consultation — relay, close, post-consult, rebook |
| WF-5x | Messaging utilities — WhatsApp sender, Slack sender, channel manager |
| WF-6x | Data — message logging, audit |
| WF-7x | Background jobs — scheduled health checks, reminders, cleanup |

---

## State Machine (User Status)

```
[no record] →(form submitted)→ payment_pending →(tap "Payment Completed")→ payment_submitted
    →(admin APPROVE)→ consultation_active →(admin CLOSE)→ consultation_closed
    →(REBOOK)→ payment_pending [loop]

any state →(admin BLOCK)→ blocked
payment_submitted →(admin REJECT)→ payment_pending [retry]
```

**Two-dimensional state:** `status` (above) + `stage` (sub-state within status, e.g. `awaiting_feedback`).

---

## WF-0x — Infrastructure

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-00 | Webhook Receiver | 🟢 Active | 🔴 P1 | n8n: "WF-00 Webhook Receiver" (JQu1MkK5vgtUCeNO) | Entry point for all inbound WhatsApp messages. Meta webhook verification, deduplication, bot echo filter (TD-030), whitespace-only message guard (TD-034 May 2026). Filters non-text (images/audio/reactions → ignore). Routes valid text messages to WF-01. 2026-05-19 TD-002: added `Build WF-60 Payload (Inbound)` Code mapper between `Gather Message Info For Processing` and `Call WF-60 Message Logger` — emits canonical WF-60 input contract (direction='inbound', userId=null for WF-60 to resolve via phone lookup). Parallel branch to WF-01 preserved. 2026-05-21 TD-F: `Parse WhatsApp Message` now extracts `interactiveLabel` (button_reply.title / list_reply.title) in parallel with the routable `messageContent` (button_id) — WF-01 routing semantics preserved. `Build WF-60 Payload (Inbound)` writes `interactiveLabel || messageContent` into `messages.content` so audit rows are human-readable; the original id is stored in `metadata.interactiveButtonId` for traceability. Pseudo-first per [[feedback_pseudocode_first_refactor]]. |
| WF-01 | Message Router | 🟢 Active | 🔴 P1 | n8n: "WF-01 Message Router" (hYGNM97sXvdo1WmI) | Security filters: country check, blocked-user check. Loads user + pendingUser records from DB. Routes: no record→WF-21, pendingUser→WF-22, existing user→WF-02. Updated Apr 2026 to pass pendingUser to WF-02. Fixed May 2026 (TD-NEW-002): Layer 3 now emits OPTED_OUT for opted_out users (originally routed to WF-21 for re-engagement; 2026-05-25 TD-DCP-107 retargeted this branch to WF-26 — see Sprint data-contract-sprint-bug-fix changelog) vs REJECTED for blocked users only; Lookup SQL updated to SELECT status. Updated 2026-05-18 (ICV-001): 'Silent Reject (Message Type)' Code node now returns {phoneNumber, message: "⚠️ Sorry, we only handle text messages right now..."} instead of {silentReject, reason}; non-text messages now actually trigger the deflection WhatsApp send via WF-50 (caller name was misleading — design decision recorded in sprint followups-input-contract-sweep). 2026-05-22 SP-11: added explicit user-load anomaly gate between `Prepare User Data` and `Call WF-02 Rule Router`. 4 new nodes: `User-Load Gate` (Set v3.4 with `includeOtherFields: true` — required so phoneNumber/user/pendingUser/messageType/messageContentUpper pass through to WF-02; default v3.4 behavior drops upstream fields and emits only assignments, which caused exec 1630 regression before fix) derives `routing` ∈ {to_wf02, anomaly_interactive, anomaly_keyword} from (user, pendingUser, messageType, messageContentUpper); `Anomaly Route?` (IF v2.2) branches on routing=='to_wf02'; FALSE branch → `Build Admin Anomaly Alert` (Set v3.4, default includeOtherFields=false because it intentionally produces WF-51's {channelId, messageText} contract) → `Call WF-51 (Admin Anomaly Alert)` (executeWorkflow v1.2) posts to chinmay-admin-commands (C0A5B0ZE81E). Intercepts interactive button taps from unrecorded phones (bot/spam/deleted-row case) and exact STOP/REBOOK from unrecorded phones (eliminates WF-47 pre-onboarding edge at source). Legitimate cases (existing user, pendingUser exists, plain-text from new phone) flow to WF-02 unchanged. 18 → 22 nodes. Pseudo-first per [[feedback_pseudocode_first_refactor]]: docs/pseudocode/WF-01.pseudo Steps 12a-12c revised before JSON mutation. Smoke tested: Test A (recorded-user happy path) exec 1635 ✅; Tests B/C/D (anomaly paths) deferred — need unrecorded phone (logged in sprint followups). |
| WF-02 | User State Router | 🟢 Active | 🔴 P1 | n8n: "WF-02 User State Router" (PubCsNTOspF3xqXZ) | Routes by messageType + user status. button_reply routing: consultation_closed → POST_CONSULT_TEXT (WF-43, TD-024 May 2026); all others → PAYMENT_CONFIRM (WF-32). Routes: NEW_USER→WF-21, PRE_FORM_TEXT→WF-23, DETAILS_FORM→WF-22, POST_CONSULT_TEXT→WF-43, PAYMENT_CONFIRM→WF-32, PAYMENT_PENDING_TEXT→WF-30, PAYMENT_SUBMITTED_TEXT→WF-31, RELAY→WF-40. Fixed May 2026 (TD-NEW-004): text messages now pass through WF-20 (Keyword Handler) before state routing — STOP/HELP/REBOOK intercepted; interactive messages bypass WF-20 directly to Route Switch. |

---

## WF-1x — Admin (Slack-Side)

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-10 | Slack Admin Handler | 🟢 Active | 🔴 P1 | n8n: "WF-10 Slack Admin Handler" (wMh0oBRtJbvhLgOf) | Entry point for Slack events. Filters bot messages. Routes commands to WF-11. Admin text in user channels relays to WF-41 only if user is consultation_active (TD-023 May 2026 — status guard added). Updated 2026-05-18 (BUG-01): "Load User Status" `options.queryReplacement` converted from comma-separated expression string to JS-array form `={{ [a, b, c] }}` — admin messages containing commas were being split by n8n's comma-parser and misaligning the `$1/$2/$3` placeholders. 2026-05-20 TD-003 F2: added `Build WF-60 Payload (Slack Inbound)` Code mapper + `Call WF-60 Message Logger` executeWorkflow (tv 1.2, onError=continueRegularOutput) as a 3rd parallel side branch from `Extract Required Fields`. Logs every Slack inbound that has a user_channel match; admin-wide commands (LIST/STATS/HELP) get silently dropped by WF-60's skip-if-not-resolvable path. 2026-05-22 SP-01: redesigned the user-channel relay path. `Load User Status` SQL simplified to `SELECT id, status, name, phone_number ... WHERE slack_channel_id = $1` (dropped unquoted camelCase aliases `$2 as channelName, $3 as messageText` which postgres silently lowercased, creating field-name drift downstream). Added 3 nodes: `Build WF-41 Payload` (Set v3.4) on TRUE branch — emits `{phoneNumber, adminMessage}` to WF-41; `Build Admin Feedback` (Set v3.4) + `Call WF-51 (Inactive User Feedback)` (executeWorkflow v1.2) on FALSE branch — posts "⚠️ User not in active consultation — message not relayed to WhatsApp." into the same consult-<phone> channel so the admin sees their relay attempt was dropped (was previously a silent drop). 22 → 25 nodes. Pseudocode revised first (docs/pseudocode/WF-10.pseudo). 2026-05-22 SP-11: added orphan-channel gate between `Load User Status` and `User Consultation Active?`. 3 new nodes: `User Row Exists?` (IF v2.2) checks `$('Load User Status').item.json.id` not null; TRUE → existing `User Consultation Active?` IF (semantics now tight — fires only when row returned); FALSE → `Build Orphan Channel Alert` (Set v3.4) → `Call WF-51 (Orphan Channel Alert)` (executeWorkflow v1.2) posts to chinmay-admin-commands (C0A5B0ZE81E). Previously aod=true on `Load User Status` caused no-match SELECTs to fall through to `Build Admin Feedback` with the misleading "User not in active consultation" message — `.status` was actually undefined because no users row mapped to the slack_channel_id (orphaned channel). The gate splits "user exists but not active" from "no user record at all". 25 → 28 nodes. Pseudocode revised first (docs/pseudocode/WF-10.pseudo Steps 15a + 18a). Smoke tested: Test E (admin types in fresh `consult-orphan-test` channel) exec 1652 ✅ — User Row Exists? FALSE branch fired, Build Orphan Channel Alert posted exact expected text to chinmay-admin-commands, existing FALSE-branch "Build Admin Feedback" correctly did NOT fire. **2026-05-23 SP-03 (WF-10 implementation phase only — downstream WF-11/33/34/42 cleanups + smoke test deferred to next session):** centralized validation gate landed. 9 superseded nodes dropped (Detect Command - Admin/User Channel, Command - Admin/User Channel ?, User Consultation Active?, Build Admin Feedback, Build Wrong Channel Warning, Call WF-51 (Inactive User Feedback), Call WF-51 (Wrong Channel Warning)); 19 new nodes added (Classify Admin/User Channel Message Code nodes computing `{kind, commandKeyword, typedPhone, channelDerivedPhone, phoneStatus, expectedState}`; Route by Kind (Admin/User) Switches; Phone Match? 3-way Switch; State Match? IF; Dispatch by Kind Switch; 6 Build-Alert Set + Call-WF-51 chains: Wrong-Channel Admin, Help Prompt, Wrong-Channel User, Phone Absent, Phone Mismatch, Wrong State). 28 → 38 nodes. All new exec nodes at canonical 1.2; all new IF v2.2; Set v3.4; Switch v3.3 — matched live typeVersions, no bumps. Build approach: fresh-rebuild Python script copied 19 keeper nodes verbatim (preserving id/position/credentials/webhookId 27a3efa5-…-bd42) and spliced in 19 new nodes; single PUT, lint clean, zero dangling refs. Backup: `archive/backups/wMh0oBRtJbvhLgOf-2026-05-23-12-14.json`. **Live JSON updated; functional smoke test deferred.** versionId: `f9a50569-cfbb-40e3-968d-51bbe3376fa5`. **2026-05-23 SP-03 BUG-02 fix (surgical, superseded same session by BUG-02 systemic):** smoke test exposed contract drop — `Dispatch by Kind` output 0 was passthrough from postgres `Load User Status` (`{id,status,name,phone_number}`), so WF-11 received user-row instead of classified payload. Added `Build WF-11 Payload` Set v3.4 with `{messageText,adminUserId,channelId,channelName}` between Dispatch and Call WF-11. 38 → 39 nodes. Backup: `archive/backups/wMh0oBRtJbvhLgOf-2026-05-23-13-44.json`. **2026-05-23 SP-03 BUG-02 systemic fix:** smoke exposed a second contract drift — Slack auto-linkifies typed phone numbers as `<tel:+xxx|yyy>` markup, which WF-11's `Parse Command` regex `/^\+?\d{10,15}$/` couldn't tokenize, so `phoneNumber=null` → WF-42's `Load User by Phone` queryReplacement evaluated to null → postgres "Query Parameters must be a string of comma-separated values or an array of values" error. Root cause: WF-11 duplicates parsing work WF-10 already does, creating multiple drift points (3 surfaced in one session: user-row passthrough, missing messageText, regex blind to tel: markup). Per [[feedback_systemic_before_individual]] applied systemic fix: revised pseudos (WF-10.pseudo + WF-11.pseudo) so WF-10 owns parsing+validation and WF-11 trusts the classified input. Implementation in WF-10: (a) `Classify User Channel Message` Code rewritten — strips Slack mrkdwn including `<tel:..|..>` and `<http(s)..|..>` wraps before classification, renames internal `commandKeyword`→`commandType` (terminology match with WF-11 Switch), adds `reason` extraction (tokens after typedPhone); (b) `Classify Admin Channel Message` Code rewritten — same strip, adds `commandType` for admin-wide commands (LIST | STATS | HELP); (c) `Build WF-11 Payload` (user branch) widened 4 → 7 assignments (adds commandType, phoneNumber=typedPhone, reason); (d) new `Build WF-11 Payload (Admin)` Set v3.4 added with same 7-field shape pulling from Classify Admin Channel Message; (e) `Route by Kind (Admin)` out 0 rewired to flow through new Admin Set → Call WF-11 (was direct passthrough). 39 → 40 nodes. WF-11 also touched (see WF-11 row): Parse Command deleted (no re-parse), Switch wired directly to trigger, UNBLOCK flow's `targetPhone` references renamed to `phoneNumber` for canonical-field consistency. Backup: `archive/backups/wMh0oBRtJbvhLgOf-2026-05-23-13-56.json`. versionId: `743003cc-c09c-474b-b11d-c4914ab4ced1`. |
| WF-11 | Command Parser | 🟢 Active | 🔴 P1 | n8n: "WF-11 Command Parser" (GoTYo0GS2y8qjjkw) | Parses APPROVE PAYMENT→WF-33, REJECT→WF-34, CLOSE→WF-42, BLOCK→WF-46, UNBLOCK→WF-11 unblock flow (Lookup Blocked User → Unblock User / No Blocked User Found). All nodes active (TD-005 May 2026). UNBLOCK guard: SELECT status=blocked — opted_out users not affected. 2026-05-20 TD-003 F4: 8 direct `n8n-nodes-base.slack` send nodes refactored in-place to `n8n-nodes-base.executeWorkflow` calls into WF-51 (Confirm Consultation Closure, Confirm User Blocked, Send List To Admin, Send Stats To Admin, Unknown Command Response, Confirm User Unblocked, No Blocked User Found, Send Help To Admin). Same node names/positions/connections preserved (no topology change). `mappingMode: defineBelow` carries `channelId` + `messageText`. The 4 nodes that previously used `={{ $json.channelName }}` (a channel name) now use `={{ $('Parse Command').item.json.channelId }}` (an ID) to match WF-51's slack-node `mode: id`. The 2 hardcoded admin-channel nodes keep the literal `C0A5B0ZE81E`. WF-51 (F3) now logs every send to messages table. 2026-05-20 TD-005: `Get Stats` SQL `revenue_today` sub-select changed from `WHERE status='approved'` to `WHERE status='verified'` to match the canonical payment status value (renamed in WF-33 and DB-migrated same date). **2026-05-23 SP-03 BUG-02 systemic fix — WF-11 gutted of duplicate parser:** smoke exposed multi-layer contract drift at WF-10→WF-11 boundary (WF-11's Parse Command regex was blind to Slack `<tel:..|..>` auto-linkified phones, so phoneNumber=null → downstream WF-42 postgres error). Pseudo revised first (WF-11.pseudo trust-mode strengthened; Step 2 Parse Command deleted; inputs/Step 16/19 updated to `phoneNumber`). Implementation: (a) removed `Parse Command` Code node — WF-10 now owns parsing per centralized validation gate; (b) removed `Blocked User Found?` IF + `No Blocked User Found` executeWorkflow (Task #3 planned cleanup bundled — pre-validated by WF-10 State Match? gate, unreachable post-SP-03); (c) `When Executed by Another Workflow` → `Switch` wired directly (was → Parse Command); (d) `Lookup Blocked User` → `Unblock User` wired directly (was → Blocked User Found? → IF TRUE → Unblock User); (e) renamed all `$('Parse Command').item.json.targetPhone` references → `$json.phoneNumber` in `Lookup Blocked User` SQL, `Unblock User` SQL, `Confirm User Unblocked` text; renamed other `$('Parse Command').item.json.X` → `$json.X` (passthrough from WF-10) in `Send List/Stats/Help To Admin`, `Confirm Consultation Closure`, `Confirm User Blocked`, `Unknown Command Response`; renamed `$('Parse Command').item.json.originalMessage` → `$json.messageText` in `Unknown Command Response`. 23 → 20 nodes. Switch unchanged (still reads `$json.commandType` which WF-10 now provides). Lint clean, dangling-name re-scan clean, downstream WF-33/34/42/46 callees unaffected (passthrough still passes the right shape). Backup: `archive/backups/GoTYo0GS2y8qjjkw-2026-05-23-13-56.json`. versionId: `0412e162-f79d-48ea-ae83-f6a79982acad`. **2026-05-23 SP-03 v2 patch (smoke-driven):** smoke D1 retry #2 exposed series-after-sub-workflow contract drop — 7 nodes (Confirm Consultation Closure, Confirm User Blocked, Confirm User Unblocked, Unblock User, Send List/Stats/Help To Admin) were reading `$json.X` for trigger-contract fields (channelId, phoneNumber, reason) but their predecessor was an executeWorkflow/postgres/Code node that REPLACES line data. Patched all 7 to use `$('When Executed by Another Workflow').item.json.X` for WF-10-contract fields; left `$json.message` alone for Code-node-produced text and `$json.commandType` in Switch (Switch is first-after-trigger, passthrough OK). Backup: `GoTYo0GS2y8qjjkw-2026-05-23-14-09.json`. versionId: `75fe8b7d-...`. **2026-05-23 SP-03 BUG-05 fix (smoke-driven):** D1 retry #3 surfaced duplicate "Consultation closed" Slack posts — WF-42 has its own confirmation (richer: includes name + WA-feedback note); WF-11's `Confirm Consultation Closure` posted a minimal phone-only duplicate. Single-owner principle applied: WF-42 owns the post-close Slack message. Removed `Confirm Consultation Closure` executeWorkflow node from WF-11 (formerly chained after Call WF-42); Call WF-42's success connection now terminates the CLOSE branch. WF-11.pseudo Step 6 amended + Step 7 marked deleted. 20 → 19 nodes. Backup: `GoTYo0GS2y8qjjkw-2026-05-23-14-20.json`. versionId: `adeb14d5-...`. **2026-05-23 BUG-05 BLOCK sibling fix (pre-Phase-F audit):** WF-11's `Confirm User Blocked` posted a phone+reason duplicate AFTER calling WF-46, which already owns the admin BLOCK confirmation via `Call WF-51 Notify Admin`. Same single-owner shape as the CLOSE dedup. Removed `Confirm User Blocked` executeWorkflow node from WF-11 (formerly chained after Call WF-46); Call WF-46's success connection now terminates the BLOCK branch. WF-11.pseudo Step 8 amended + Step 9 marked deleted. WF-46's `Prepare WF-51 Payload (Notify Admin Blocked)` jsCode updated in same commit to include the caller's `reason` (falls back to 'Not provided') so the reason text is preserved. 19 → 18 nodes. Backup: `GoTYo0GS2y8qjjkw-2026-05-23-15-42.json`. APPROVE/REJECT confirmed clean in same audit (WF-11 has no Confirm-* node for those — WF-33/34 own). **2026-05-23 BUG-07 fix (Phase F2 smoke-driven):** UNBLOCK happy path posted its confirmation to the admin channel (`C0A5B0ZE81E`) instead of the user's consult channel where the admin typed the command — violated WF-11.pseudo Step 19 ("Post to input channelId") and DR-13 (user-targeted commands answer in user channel). Root cause: `Confirm User Unblocked` node had `channelId` literal `"C0A5B0ZE81E"` hardcoded; the 2026-05-23 v2 patch had updated its phoneNumber refs but missed channelId. Fix: `channelId` literal → `={{ $('When Executed by Another Workflow').item.json.channelId }}` cross-node ref. 18 nodes unchanged; no structural change. Backup: `archive/backups/GoTYo0GS2y8qjjkw-2026-05-23-16-44.json`. Verified via F2 attempt 2 (execs 1851-1856): Slack post landed in `C0B567A175W` (consult-+61466927921) with correct text; users.status `blocked` → `consultation_closed`. |

---

## WF-2x — Onboarding

> **Design principle:** No DB record is created until the user submits the WhatsApp Flow form. The very first message triggers a single combined response: policy URL in message body + WhatsApp Flow form. Filling and submitting = implicit consent.

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-21 | New User Welcome + Form | 🟢 Active | 🔴 P1 | n8n: "WF-21 New User Welcome + Form" (zM8WbxSdt9nXRoLZ) | Triggered by WF-02 when no DB record + no pendingUser exists. No DB write. Sends ONE free-form message: policy URL + service description + ₹500 fee + WhatsApp Flow form. Flow ID: 1408011897720771, flowCta: "Fill Details". Tested Apr 2026 — working. Activated Apr 2026. Updated 2026-05-18 (BUG-01 sweep): "Insert Pending User" queryReplacement converted to JS-array form — `contactName` from WhatsApp profile can contain commas. |
| WF-22 | Form Response Handler | 🟢 Active | 🔴 P1 | n8n: "WF-22 Form Response Handler" (dr8QM0m92Ml8MvIh) | Handles WhatsApp Flow encrypted callback. Decrypts + validates payload. **First DB write here:** creates user record with status=payment_pending, stores name/DOB/TOB/birth_place. **Immediately calls WF-52 to create `consult-{phone}` Slack channel and saves slack_channel_id to DB.** Sends GPay payment instructions + "Payment Completed" interactive button. Updated May 2026: channel creation moved here from WF-32. Updated 2026-05-17 (FU-3): removed redundant `User Created?` IF node (both branches converged to same downstream); `Create User Record` now connects directly to `Ensure Slack Channel Exists (WF-52)`. Updated 2026-05-18 (BUG-01 sweep): "Create User Record" queryReplacement converted to JS-array form — `full_name` and `place_of_birth` are user-input fields that often contain commas (e.g. "Smith, John", "Mumbai, India"). Also applied canonical-1.2 roller to 3 executeWorkflow nodes (added missing `source`/`operation`/`mode`). Updated 2026-05-26 (TD-PGF-01B Step 3): "Create User Record" INSERT extended with `email_address` column ($6 parameter) — paired with `chinmay_astro.users.email_address` schema migration + Flow v2 (`workflows/flows/collect-personal-details-v2.json`) form field addition. Cutover to v2 Flow pending in next session. |
| WF-23 | Pre-Form Intent Filter | 🟢 Active | 🟡 P3 | n8n: "WF-23 Pre-Form Intent Filter" (VpCER0Vqq3NYJGpI) | Handles free-form messages from users with pendingUser record (received form but haven't submitted yet). Runs WF-25 Gemini intent filter → contextual reply + re-sends policy + form. Built and active (session 6). 2026-05-23 SP-04: dropped `Is Stop Intent?` + `Call WF-47 Unsubscribe`; added `Build WF-50 (Stop Clarifier) Payload` + `Call WF-50 (Stop Clarifier)` on `Is Pass-Through Intent?` FALSE branch — clarifier mirrors WF-40 verbatim. WF-47 no longer called from here; WF-50 is now called twice (existing path + new clarifier path). **2026-05-26 GAP-3C (Batch 6):** added `Is General Enquiry?` IF (v2) on `Is Pass-Through Intent?` TRUE branch — TRUE → new Gemini chain (`Prepare Gemini Response Prompt` jsCode v2 with pre-form stage cue + email callout → `Gemini General Response` HTTP v4.2 [gemini-2.5-flash-lite, temp=0.3, retryOnFail=true, maxTries=3, googlePalmApi cred] → `Extract Gemini Reply` jsCode v2 → `Send Gemini Reply via WF-50` exec v1.2 [defineBelow + value:{}]); FALSE → existing `Prepare Flow Form` chain (untouched). 7 → 12 nodes. Backup: `archive/backups/VpCER0Vqq3NYJGpI-2026-05-26-05-23.json`. Pseudo renumbered 7 → 10 steps (linear, no tombstones). |

---

## WF-3x — Payment

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-30 | Payment Pending Intent Filter | 🟢 Active | 🟡 P3 | n8n: "WF-30 Payment Pending Intent Filter" (gGJBY5fJha0Let8I) | Handles free-form messages from payment_pending users. WF-25 intent filter blocks garbage/malicious/inappropriate inside WF-25 (no return); pass-through intents (wants_consultation, general_enquiry, rebook_intent, feedback_intent) → contextual reply + re-prompt payment. Updated May 2026 (TD-028): stop_intent routing added. **2026-05-23 SP-04:** stop_intent auto-unsubscribe removed — `Is Stop Intent?` IF + `Call WF-47 Unsubscribe` dropped; replaced with `Build WF-50 (Stop Clarifier) Payload` + `Call WF-50 (Stop Clarifier)` on `Is Pass-Through Intent?` FALSE branch (WF-40 clarifier-pattern parity). User must send literal STOP via WF-20 to opt out. **2026-05-26 GAP-3C (Batch 6):** added `Is General Enquiry?` IF (v2) on `Is Pass-Through Intent?` TRUE branch — TRUE → new Gemini chain (payment-pending stage cue + email callout, gemini-2.5-flash-lite, temp=0.3, retryOnFail=true/maxTries=3); FALSE → existing `Prepare Payment Reminder` chain (untouched). 7 → 12 nodes. Backup: `archive/backups/gGJBY5fJha0Let8I-2026-05-26-05-23.json`. Pseudo renumbered 8 → 10 steps. |
| WF-31 | Payment Submitted Handler | 🟢 Active | 🟡 P3 | n8n: "WF-31 Payment Submitted Handler" (HB8nXudAtk9iXz7C) — was "WF-27 Payment Review Handler" | Handles free-form messages from payment_submitted users. WF-25 intent classifier → pass-through (non-blocked intents): "under review" ack via WF-50. Also relays all messages to user's Slack consultation channel via WF-51 (TD-016 May 2026). **2026-05-23 SP-04:** stop_intent auto-unsubscribe removed — `Is Stop Intent?` IF + `Call WF-47 Unsubscribe` dropped; replaced with `Build WF-50 (Stop Clarifier) Payload` + `Call WF-50 (Stop Clarifier)` on `Is Pass-Through Intent?` FALSE branch (WF-40 clarifier-pattern parity). User must send literal STOP via WF-20 to opt out. Parallel WF-51 Slack-relay branch untouched. **2026-05-26 GAP-3C (Batch 6):** on Branch A added `Is General Enquiry?` IF (v2) on `Is Pass-Through Intent?` TRUE branch — TRUE → new Gemini chain (payment-submitted "under review" stage cue + email callout for urgent, gemini-2.5-flash-lite, temp=0.3, retryOnFail=true/maxTries=3); FALSE → existing `Prepare Under Review Message` chain (untouched). Branch B (Slack admin relay) untouched. 10 → 15 nodes. Backup: `archive/backups/HB8nXudAtk9iXz7C-2026-05-26-05-23.json`. Pseudo renumbered 12 → 14 steps (Branch B steps shift +2). |
| WF-32 | Payment Confirmation Receiver | 🟢 Active | 🔴 P1 | n8n: "WF-32 Payment Confirmation Receiver" (emUOLWVZiNVxcOe3) — was "WF-24 Payment Confirmation Handler" | User taps "Payment Completed" button. Updates status=payment_submitted. Loads existing slack_channel_id from DB (channel was already created in WF-22). Posts payment notification to user's consult channel. Admin reads notification and types `APPROVE PAYMENT <phone>` in that same channel. Updated May 2026: WF-52 call removed — channel is pre-created at form submission. |
| WF-33 | Payment Approval Processor | 🟢 Active | 🔴 P1 | n8n: "WF-33 Payment Approval Processor" (NcHZedq9ycnAQ9SW) — was "WF-40 Payment Approval Processor" | Admin issues APPROVE PAYMENT <phone> in Slack. State guard added (TD-021 May 2026): only proceeds if user is in payment_submitted state; otherwise posts error to consultation channel. Updates status=consultation_active. Notifies user on WhatsApp that consultation has begun. Updated 2026-05-18 (ICF-005): added "Prepare WF-51 Payload (Notify Admin)" Set node between WF-50 notify-user and WF-51 notify-admin-in-channel caller — admin Slack ack was lost in TC-0303 because passthrough was forwarding the WF-50 response instead of {channelId, messageText}. Updated 2026-05-18 (ICF-006): added "Prepare WF-51 Payload (Wrong State)" Set node on User in Correct State? false-branch before Notify Admin Wrong State caller — same passthrough fix pattern. 2026-05-20 TD-005: `Update Payment Status` SQL renamed status value `'approved'` → `'verified'` to align with the existing `verified_at` column name (canonical naming consistency). DB-side migration done atomically (`UPDATE payments SET status='verified' WHERE status='approved'`; 2 rows). **2026-05-23 SP-03 Task #3 trust-mode cleanup:** removed `User in Correct State?` IF + `Prepare WF-51 Payload (Wrong State)` + `Call WF-51 Notify Admin Wrong State` — pre-validated by WF-10's centralized validation gate (SP-03 systemic fix, commit `91c0975`), so these guards are guaranteed unreachable. `Load User by Phone` now wires directly to `Update Payment Status`. 14 → 11 nodes. Pseudo Steps 4 + 13 already declared removed (no pseudo change needed). Backup: `archive/backups/NcHZedq9ycnAQ9SW-2026-05-23-17-12.json`. TD-021 resolved (state guard now lives centrally in WF-10 State Match? gate). |
| WF-34 | Payment Rejection Processor | 🟢 Active | 🔴 P1 | n8n: "WF-34 Payment Rejection Processor" (se82n3MUQ9xE5aEr) — was "WF-41 Payment Rejection Processor" | Admin issues REJECT in Slack. Updates status=payment_pending. Sends rejection reason to user with retry instructions. Updated 2026-05-18 (ICF-007): added "Prepare WF-51 Payload (Notify Admin Rejected)" Set node between WF-50 sender and WF-51 admin caller. Updated 2026-05-18 (ICF-008): added "Prepare WF-51 Payload (User Not Found)" Set node on User Found? false-branch (channelId from trigger input — no user record on this branch). Updated 2026-05-18 (ICF-009): added "Prepare WF-51 Payload (Wrong State)" Set node on User in Correct State? false-branch. Same passthrough fix pattern across all 3. Updated 2026-05-18 (BUG-01 sweep): "Update Payment Record" queryReplacement converted to JS-array form — `rejectionReason` is admin-typed free text that can contain commas. Also fixed "User Found?" IF node — added `singleValue: true` to `notEmpty` unary operator (pre-existing schema validation issue surfaced during PUT). **2026-05-23 BUG-06 — Phase E2 smoke surfaced silent admin-reason drop:** admin-typed REJECT reason (e.g., `"Test rejection reason - retry payment"`) was lost in 3 layers. Root causes: (a) WF-34 expected field `rejectionReason` but WF-10/WF-11 emit `reason` (producer/consumer drift; same convention used by WF-46); (b) `Update Payment Record` queryReplacement read `$json.reason`, but upstream `Load User by Phone` (postgres) replaces line data — series-after-sub-workflow contract drop (plugin candidate (n)); (c) `Prepare WF-51 Payload (Notify Admin Rejected)` Code emitted a hardcoded text without referencing any reason field — wording drift introduced 2026-05-18 ICF-007. Fix: WF-34.pseudo renamed `rejectionReason` → `reason` (Inputs, Step 1, Step 5, Step 9) + added Notes for the field-name alignment + WA-generic rationale + Razorpay post-MVP trigger; live `Update Payment Record` queryReplacement now reads `$('When Executed by Another Workflow').item.json.reason` (cross-node ref); live `Prepare WF-51 Payload (Notify Admin Rejected)` jsCode rebuilt to read trigger reason + emit pseudo Step 9 format with cross-node refs. WA body intentionally unchanged (generic per pseudo; surfaced reason gated on post-MVP Razorpay integration). 14 nodes unchanged; no structural change. Backup: `archive/backups/se82n3MUQ9xE5aEr-2026-05-23-16-19.json`. Verified end-to-end via Phase E2 attempt 3 (execs ≥1826): DB `payments.id=17.rejection_reason='Test rejection reason - retry payment'`; admin Slack ack rendered `❌ Payment rejected for Abcs (+61466927921). Reason: Test rejection reason - retry payment. User notified to retry payment.`; single WF-51 fired (BUG-05 sibling check clean for REJECT). **2026-05-23 SP-03 Task #3 trust-mode cleanup:** removed `User Found?` IF + `User in Correct State?` IF + `Prepare WF-51 Payload (User Not Found)` + `Call WF-51 Notify Admin User Not Found` + `Prepare WF-51 Payload (Wrong State)` + `Call WF-51 Notify Admin Wrong State` — pre-validated by WF-10's centralized validation gate (Phone Match? + State Match? in SP-03 systemic fix). `Load User by Phone` now wires directly to `Update Payment Record`. 14 → 8 nodes. Pseudo Steps 3 + 4 already declared removed (no pseudo change needed). Backup: `archive/backups/se82n3MUQ9xE5aEr-2026-05-23-17-15.json`. |

---

## WF-4x — Consultation

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-40 | User → Admin Relay | 🟢 Active | 🔴 P1 | n8n: "WF-40 User -> Admin Relay" (du32QBZbSQOjfESe) | Handles inbound WhatsApp messages during consultation_active. Loads user from DB (gets slackChannelId + name + status), formats message, calls WF-51. Implemented + activated session 3. 2026-05-21 TD-E: added WF-25 intent classifier at head (Design Rule #6 compliance — last free-form-text handler to comply). Fan-out from WF-25 output: (a) always relay verbatim to admin via Format Slack Message → WF-51; (b) conditional `Stop Intent?` IF — on `intentResult=stop_intent` send WF-50 automated clarifier to user explaining how to opt out cleanly (avoids false-positive auto-opt-out). Garbage / malicious_abusive / inappropriate intents handled by WF-25 internally (no return). 4 → 8 nodes. Pseudo-first per [[feedback_pseudocode_first_refactor]]: docs/pseudocode/WF-40.pseudo revised + user-approved BEFORE JSON mutation. |
| WF-41 | Admin → User Relay | 🟢 Active | 🔴 P1 | n8n: "WF-41 Admin -> User Relay" (6PzJRZsF7k2d9hV7) | Admin types in user's Slack channel → WF-50 → WhatsApp. Fixed session 3: added postgres + Slack credentials, corrected WF-50 ID (BUVun38WEKb12zg9). WF-10 now routes here. 2026-05-20 TD-003 F5 (revised scope): removed orphaned WA→Slack branch (4 nodes: Detect Direction, Route by Direction, Prepare Channel Lookup, Post to Slack Channel). That branch was redundant with WF-40 (User → Admin Relay) and never fired from any caller. Trigger now wires directly to Extract Phone from Channel. WF-41 is now single-direction (admin → user only) and pseudocode was updated first (docs/pseudocode/WF-41.pseudo) to match. Down from 9 → 5 nodes. 2026-05-20 mid-session: operator patched stale node-name reference ($('Detect Direction') → $('Extract Phone from Channel')) in n8n UI; exported here as part of TD-G. 2026-05-21 Batch 2 sibling regression: surfaced strict lint debt on `WF-50 (Send WhatsApp)` executeWorkflow node — missing canonical `source`/`operation`/`mode` fields at typeVersion 1.2 (silent-drop risk). Fixed in-sprint via partial update; canonical shape restored. 2026-05-22 SP-01: removed `Extract Phone from Channel` (Code) + `Load User by Phone` (Postgres) — caller-prepared payload from WF-10's new `Build WF-41 Payload` Set node now passes `{phoneNumber, adminMessage}` through directly. `Prepare WhatsApp Message` Code rewritten to read `$json.phoneNumber` / `$json.adminMessage` from trigger passthrough; no re-derivation, no re-query. Down from 5 → 3 nodes. State-precondition verification (consultation_active) is now owned solely by WF-10. Pseudocode rewritten (docs/pseudocode/WF-41.pseudo). |
| WF-42 | Consultation Closer | 🟢 Active | 🔴 P1 | n8n: "WF-42 Consultation Closer" (fx70vqyJtRdF2DgR) | Admin issues CLOSE in consult channel. State guard added (TD-022 May 2026): only proceeds if user is consultation_active. Sends interactive button message with btn_feedback / btn_rebook (TD-015 May 2026 — replaces unapproved template). Updates status=consultation_closed. **Does NOT archive Slack channel** — channel intentionally kept open for reuse by WF-45 on rebook (TC-0305 design decision 2026-05-16). All 3 admin Slack confirmations routed via WF-51 (FU-9 May 2026 — closes Theme 7 admin-Slack alignment; no remaining direct slack nodes). Updated 2026-05-18 (ICF-010): added "Prepare WF-51 Payload (Notify Admin Closed)" Set node between WF-50 feedback sender and main "Notify Admin in Slack" caller. Updated 2026-05-18 (ICF-011): added "Prepare WF-51 Payload (Wrong State)" Set node on User in Correct State? false-branch before Notify Admin Wrong State caller. Same passthrough fix pattern. Updated 2026-05-18 (ICF-012): added "Prepare WF-51 Payload (User Not Found)" Code node on User Found? false-branch before Notify Admin User Not Found caller; uses trigger.channelId since no user record exists on this branch. **2026-05-23 SP-03 Task #3 trust-mode cleanup:** removed `User Found?` IF + `User in Correct State?` IF + `Prepare WF-51 Payload (User Not Found)` + `Notify Admin User Not Found` + `Prepare WF-51 Payload (Wrong State)` + `Notify Admin Wrong State` — pre-validated by WF-10's centralized validation gate (Phone Match? + State Match? in SP-03 systemic fix). `Load User by Phone` now wires directly to `Close Consultation Record`. Success-path keepers (`Notify Admin in Slack`, `Prepare WF-51 Payload (Notify Admin Closed)`) unaffected. 14 → 8 nodes. Pseudo Steps 3, 4, 11, 12 already declared removed (no pseudo change needed). Backup: `archive/backups/fx70vqyJtRdF2DgR-2026-05-23-17-17.json`. TD-022 resolved (state guard now lives centrally in WF-10 State Match? gate). **2026-05-26 GAP-2 (Batch 5):** `Prepare Feedback Message` jsCode buttons array gains 3rd entry `{id: btn_done, title: "Done, thanks"}` alongside existing `btn_feedback` / `btn_rebook`. No structural change to WF-42 itself; downstream handling lives in WF-43. 7 nodes unchanged. Backup: `archive/backups/fx70vqyJtRdF2DgR-2026-05-26-14-38.json`. Pseudo Step 4 buttons line updated. |
| WF-43 | Post-Consultation Handler | 🟢 Active | 🟠 P2 | n8n: "WF-43 Post-Consultation Handler" (3va0M06kijgyLejf) — was "WF-28 Post-Consultation Text Handler" | Handles consultation_closed users. Button_reply routing added (TD-024 May 2026): btn_rebook → WF-45 directly; btn_feedback → sends feedback prompt via WF-50. Text messages: WF-25 intent classifier → rebook_intent→WF-45, feedback_intent→WF-44, general→Gemini response. Hardened May 2026 (TD-NEW-016): Gemini HTTP node has retryOnFail=true, maxTries=3, timeout=10000. Updated 2026-05-18 (BUG-02): "Gemini General Response" jsonBody converted from raw-string interpolation (`={"contents":[{"parts":[{"text":"{{ $json.geminiPrompt }}"}]}],...}`) to object-interpolation (`={{ {contents:[{parts:[{text:$json.geminiPrompt}]}],generationConfig:{...} } }}`) so n8n JSON-encodes natively — fixes "JSON parameter needs to be valid JSON" failures on prompts containing quotes/newlines/braces. Updated 2026-05-18 (BUG-03): "Prepare Gemini Response Prompt" jsCode renamed `${d.messageText}` → `${d.messageContent}` — WF-43 callers pass `messageContent`, so the prior reference produced literal "User: undefined" in the Gemini prompt. Updated 2026-05-18 (BUG-04 A): "Gemini General Response" HTTP node switched from generic httpQueryAuth (credential ZkLShpFmp8Mi1gZl — broken) to predefined nodeCredentialType=googlePalmApi + credential zT7defyXYEvxWwZm. Same fix as WF-25 Classify Intent. **2026-05-26 GAP-2 (Batch 5):** added btn_done routing branch — additive `Is Done Button?` IF (v2) between `Is Button Reply?` TRUE and existing `Is Rebook Button?`; TRUE → parallel fork of `Build Thank-You Payload` (Set v3.4, contract-emit per §2.3) → `Send Thank-You via WF-50` + `Build Btn-Done Slack Payload` (Set v3.4, contract-emit per §2.4) → `Send Btn-Done Slack via WF-51`; FALSE → existing `Is Rebook Button?` chain (untouched). 16 → 21 nodes. No state change (`consultation_closed` stays). Slack channel sourced from `user.slack_channel_id` per WF-42 convention. New exec nodes match live convention `mappingMode: "defineBelow" + value: {}` (Gap-10 fan-out canonical shape). Backup: `archive/backups/3va0M06kijgyLejf-2026-05-26-14-38.json`. Pseudo Step 3 reshaped as cascaded-IF + new Steps 5/6 + Outputs / Sub-Workflows updated to include WF-51. **2026-05-26 GAP-3C (Batch 6):** `Prepare Gemini Response Prompt` jsCode rewritten to include user-stage cue ("user has completed their consultation with Dr. Chinmay") + email-callout cue (`chinmay_astro@gmail.com`) for paraphrase-style integration (plain conversational English, not literal append). `Gemini General Response` model URL bumped `gemini-2.0-flash-lite` → `gemini-2.5-flash-lite` (2.0 deprecated; see `project_gemini_model` memory). jsonBody temperature 0.7 → 0.3 for response stability. retryOnFail/maxTries unchanged. 21 nodes (no structural change). Backup: `archive/backups/3va0M06kijgyLejf-2026-05-26-05-23.json`. Pseudo Step 12-13 patched in place. |
| WF-44 | Feedback Recorder | 🟢 Active | 🟡 P3 | n8n: "WF-44 Feedback Recorder" (Du2CJ3OTohRFZYoA) — was "WF-26 Feedback Recorder" | Captures user feedback text. Runs WF-25 intent classifier first: rebook_intent → WF-45 Rebook; stop_intent → WF-47 Unsubscribe (WF-44-STOP 2026-05-16); all other intents → saves feedback to DB + sends ack via WF-50. Updated May 2026 (TD-032). 2026-05-19 TD-001: Save Feedback to DB postgres-node parameter binding fixed — `additionalFields.queryParams` (silently ignored at v2.5) moved to `options.queryReplacement`; expression uses cross-node reference `={{ [$('When Executed by Another Workflow').first().json.messageContent, .first().json.user.id] }}` (was `[$json.messageText, $json.userId]` — both fields undefined at that point in the chain). |
| WF-45 | Rebook Handler | 🟢 Active | 🟠 P2 | n8n: "WF-45 Rebook Handler" (MUG7rPgSHc7UtAE9) | Handles REBOOK keyword or rebook_intent. Loads user record, sets status=payment_pending, sends interactive "Payment Completed" button with standard ₹500 UPI instructions (+91-9653240263, Chinmay Mujumdar). Updated May 2026 (TD-011): standard UPI text + interactive button. **Reuses existing consult-{phone} Slack channel** — reads slack_channel_id from DB, does NOT call WF-52 (TC-0305 design decision 2026-05-16). Updated 2026-05-18 (ICF-001): added "Prepare WF-50 Payload (Rebook Payment)" Code node between Set status=payment_pending and Send Payment Instructions — passthrough was forwarding the Postgres UPDATE row instead of the WF-50 input contract. Code node now emits interactive button payload (Welcome back ${name} + UPI instructions). |
| WF-46 | User Blocker | 🟢 Active | 🟠 P2 | n8n: "WF-46 User Blocker" (UV62An60fzflU0uD) — was "WF-43 User Blocker" | Admin issues BLOCK. Updates status=blocked. Sends admin confirmation via WF-51. Also called by WF-25 on malicious_abusive/inappropriate intent. Updated 2026-05-17 (FU-1): DR-10 fix — removed channel-archive nodes (channels are intentionally preserved for REBOOK reuse after UNBLOCK); admin confirmation refactored from direct Slack post to WF-51 call; channelId sourced from caller input with `slack_channel_id` fallback for WF-25 path. Updated 2026-05-18 (ICF-013): added "Prepare WF-51 Payload (Notify Admin Blocked)" Set node between Update User to Blocked Status and Call WF-51 Notify Admin — passthrough was forwarding the postgres row instead of {channelId, messageText}. Set node now realises the caller-channelId-or-slack_channel_id fallback intent. **2026-05-23 BUG-05 BLOCK sibling fix:** `Prepare WF-51 Payload (Notify Admin Blocked)` jsCode extended to include caller's `reason` field (falls back to 'Not provided') — paired with removal of WF-11's duplicate `Confirm User Blocked` node so WF-46 becomes single owner of the post-block Slack confirmation. Message format: `🚫 User blocked: <name> (<phone>). Reason: <reason or 'Not provided'>. Status set to 'blocked'.`. WF-46.pseudo also revised in same commit: dropped stale channel-archive Steps 5-6 (FU-1 removed those nodes 2026-05-17; pseudo never caught up), added single-owner note, added drift note that `blocked_reason` DB column is hardcoded to 'Blocked by admin' (separate TD-candidate). Backup: `UV62An60fzflU0uD-2026-05-23-15-42.json`. |
| WF-47 | Unsubscribe Handler | 🟢 Active | 🔴 P1 | n8n: "WF-47 Unsubscribe Handler" (2U7mxHMyqA41ROKX) | Handles user-initiated STOP (regulatory opt-out). Called from WF-20. **STOP is unconditional** (2026-05-21 redesign — TD-B): UPDATE users SET status='opted_out', current_consultation_id=NULL. If incoming userStatus was 'consultation_active': also UPDATE consultations (status='closed', ended_at=NOW(), closed_by='user_opted_out') to avoid dangling open rows. If users.slack_channel_id present (i.e. user progressed past form submission): post opt-out notice to consult channel via WF-51 so admin sees the conversation has ended. Send opt-out confirmation to user via WF-50. **DR-10:** consult channel preserved (not archived) for potential REBOOK reuse. **No admin_actions write** (TD-NEW-026 — table deprecated; opt-out fact recoverable from users.status + users.updated_at + inbound STOP messages row). Updated May 2026 (TD-019). Updated 2026-05-18 (ICF-002/ICF-003): added Prepare WF-50 Payload Code nodes (later replaced by 2026-05-21 redesign — hold-branch nodes removed entirely). Updated 2026-05-21 (TD-B expanded): hold-during-consultation branch removed, admin_actions log removed, consultation auto-close + Slack notice added; queryReplacement on Update User Status to opted_out fixed (was missing — caused $1 out-of-range ISSUE-08). |

---

## WF-2x additions

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-25 | Intent Classifier | 🟢 Active | 🟠 P2 | n8n: "WF-25 Intent Classifier" (eTV1lUcYrXBg2q2T) | Shared Gemini 2.5 Flash Lite intent classifier (project standard per `project_gemini_model` memory; 2.0 deprecated; CLAUDE.md "Key Credential IDs" table updated 2026-05-26 GAP-3C). Input: phoneNumber, userId, messageContent, userStatus (or nested user.status), userStage. Output: intentResult (wants_consultation / general_enquiry / rebook_intent / feedback_intent / stop_intent / garbage / malicious_abusive / inappropriate). Gemini error → falls back to general_enquiry (TD-029 May 2026); when userStatus=consultation_closed → falls back to feedback_intent (BUG-04 2026-05-18). Fixed May 2026 (TD-NEW-003): stop_intent added to Gemini prompt, VALID whitelist, and Route by Intent Switch. Called by WF-23, WF-30, WF-31, WF-43, WF-44. Updated 2026-05-18 (ICF-004): added "Prepare WF-51 Payload (Garbage Admin)" Code node between Send Garbage Warning and Notify Admin of Garbage — passthrough was forwarding the WF-50 status object instead of {channelId, messageText}. channelId hardcoded to chinmay-admin-commands (C0A5B0ZE81E); messageText includes phone/userId/status + first 280 chars of the offending message. Updated 2026-05-18 (BUG-04 A): Classify Intent HTTP node switched from generic httpQueryAuth credential (ZkLShpFmp8Mi1gZl — had param-name field misconfigured as literal "Gemini n8n Key" instead of "key", causing every Gemini call to return 400) to predefined nodeCredentialType=googlePalmApi + credential zT7defyXYEvxWwZm ("Google Gemini(PaLM) Api account"). Same canonical setup as user's other project (WF-42 Deal Evaluator). Updated 2026-05-18 (BUG-04 B): Prepare Intent Request jsCode now reads userStatus from `input.userStatus || input.user?.status || 'unknown'` and emits it as a top-level field — previously all callers passed nested user.status while destructure expected flat. Updated 2026-05-18 (BUG-04 C): Parse Intent + Handle Gemini Error now route consultation_closed+uncertain → feedback_intent instead of general_enquiry (defensive fallback for Gemini outages and ambiguous classifications). |
| WF-26 | Re-Engaged Opted-Out User Handler | 🟢 Active | 🔴 P1 | n8n: "WF-26 Re-Engaged Opted-Out User Handler" (tKjwTYF6EER8ED3y) | Created 2026-05-25 by TD-DCP-106 (sprint `data-contract-sprint-bug-fix`). Closes BUG-NEW-02 — opted_out users re-engaging via WhatsApp were previously routed to WF-21 form-issuer (UX regression: re-asked for onboarding data already on file). New flow: WF-01 opted-out branch → WF-26 → (a) UPDATE `users.status` from `opted_out` to `consultation_closed`, (b) refresh §2.1 envelope's `user.status` so WF-02 routes on fresh value, (c) send personalized welcome-back WhatsApp via WF-50, (d) re-route inbound message through WF-02 so first re-engagement message is answered in one turn. 7 nodes: `When Executed by Another Workflow` (trigger v1.1), `Validate Inputs` (Code v2, envelope entry guard — throws on missing/wrong-shape inputs incl. `user.status !== 'opted_out'`, `pendingUser !== null`, `wasOptedOut !== true`), `Update User Status` (Postgres v2.5, alwaysOutputData:true), `Refresh Envelope Status` (Set v3.4, includeOtherFields:false, contract-emit — explicit per-field §2.1 envelope assignments with `user.status` overridden to `consultation_closed`), `Build Welcome Payload` (Set v3.4, includeOtherFields:false, contract-emit for WF-50 §2.3), `Call WF-50 Welcome Back` (executeWorkflow v1.2, canonical), `Call WF-02 Re-Route` (executeWorkflow v1.2, canonical). Topology: linear chain through Refresh, then fan-out to [Build Welcome → WF-50] + [WF-02]. Called by: WF-01 (post-TD-DCP-107 — `Route Opted-Out to WF-26` node, live 2026-05-25). Calls: WF-50, WF-02. Design decisions locked in `docs/artefacts/sprints/data-contract-sprint-bug-fix/handoffs/handoff-td-dcp-105-done-wf26-build-pending.md` (Q1 re-entry status, Q2 first-message handling, Q3 welcome wording, Q4 unified payment_submitted edge, Q5 input contract). Pseudo-first per [[feedback_pseudocode_first_refactor]]: `docs/pseudocode/WF-26.pseudo` authored + user-approved 2026-05-25 before live build. Lint: 3 advisory (Step 5g forbidden-token matches on developer-facing `throw new Error('WF-26 contract: …')` strings — internal, not delivered to human channel; matches WF-60 TD-DCP-102 precedent). Workflow auto-activated via TD-DCP-107 (2026-05-25); first opted_out re-engagement WhatsApp will trigger it. In design.md §3.3 entry-guard scope per the 2026-05-25 Amendment to `data-contract-discipline Phase 1` (WF-26 added as the third router-downstream guard alongside WF-02 and WF-11). |

---

## WF-5x — Messaging Utilities

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-50 | Send WhatsApp | 🟢 Active | 🔴 P1 | n8n: "WF-50 Send WhatsApp" (BUVun38WEKb12zg9) | Sends outbound WhatsApp message via Meta Cloud API. Accepts phoneNumber + message body. Null/empty body guard added (TD-033 May 2026) — exits gracefully if text message body is empty, prevents Meta API 400. Calls WF-60 to log. Used by all workflows that send to user. Hardened May 2026 (TD-NEW-016): all 3 Meta API send nodes have retryOnFail=true, maxTries=3, timeout=10000. 2026-05-19 TD-002: added `Build WF-60 Payload (Outbound)` Code mapper between `Process Result` and `Call WF-60 Message Logger`, and `Build WF-60 Drop Payload` mapper between `Should Drop?` and `Log Drop to WF-60`. Both emit canonical WF-60 input contract with direction='outbound'. Fixes silent direction=inbound misrecording on every outbound message. 2026-05-21 TD-F: both WF-60 payload mappers now do per-messageType content extraction (text→messageContent; interactive→interactivePayload.body.text || JSON-serialise; template→'template:'+templateName) so `messages.content` is non-NULL and human-readable for ALL message types — was NULL for interactive/template rows. Metadata enriched with `buttons` (interactive) or `templateParams` (template). Orphaned `Build Log Input` node removed (zero upstreams, dead code from pre-TD-002). 18 → 17 nodes. Pseudo-first per [[feedback_pseudocode_first_refactor]]. |
| WF-51 | Send Slack Message | 🟢 Active | 🔴 P1 | n8n: "WF-51 Send Slack Message" (wlZRK0YxnhP0b2RL) | Posts a message to a specified Slack channel. Accepts channelId + messageText. Credentials: Slack - Chinmay Astro. Activated 12 Apr 2026. Required by WF-40 (and historically WF-12, deactivated 2026-05-18). 2026-05-20 TD-003 F3: chained `Build WF-60 Payload (Slack Outbound)` Code mapper → `Call WF-60 Message Logger` executeWorkflow (tv 1.2, onError=continueRegularOutput) after `Post to Slack`. Logs every Slack outbound that routes through WF-51 (covers WF-02/22/25/31/32/33/34/40/42/46). Mapper uses Slack API response `ts` for slack_message_ts; falls back to slack_channel_id → users.id lookup in WF-60. |
| WF-52 | Slack Channel Manager | 🟢 Active | 🟠 P2 | n8n: "WF-52 Slack Channel Manager" (IO5BZLUxuVmjzk5I) | **Idempotent:** checks for existing `consult-{phone}` channel before creating — returns existing channel if found. Returns `{ channelId, channelName, isNew }`. Safe to call multiple times. **Inputs:** `phoneNumber` (string), `userName` (string); optional: `userId` (integer). Called only by WF-22 (at form submission — TD-007/TD-008 May 2026). WF-32 and WF-42 do NOT call WF-52. |

---

## WF-6x — Data & Logging

| WF | Name | Registry Status | Priority | n8n Actual | Notes |
|---|---|---|---|---|---|
| WF-60 | Message Logger | 🟢 Active | 🔴 P1 | Active as "WF-60 Message Logger" (6H75p935FpBVBQtV) | Logs all inbound and outbound messages. Called by WF-00 (WhatsApp inbound), WF-50 (WhatsApp outbound success + drop), WF-10 (Slack inbound — admin/user commands), and WF-51 (Slack outbound). 2026-05-19 TD-002: architectural rebuild (Option A — explicit canonical-shape mappers in callers). Extract Message Data rewritten to normalize field aliases, expose top-level `phoneNumber`, and compute `_filterReason` (TD-034 whitespace guard) + `_needsLookup` flags so downstream Filter Skip? / Needs Phone Lookup? IFs route correctly (they previously checked fields Extract never emitted → 0 rows persisted across 8 message events). Log to Messages Table got `onError: continueRegularOutput` so logging failure never fails the parent chain. All 3 callers now wrap each Call WF-60 invocation with a "Build WF-60 Payload" Code mapper emitting the canonical contract `{direction, userId, consultationId, messageType, content, whatsappMessageId, phoneNumber, metadata}`. 2026-05-20 TD-003 F1: extended for Slack transport — Extract Message Data accepts `transport='slack'` and emits unified `_lookupColumn`/`_lookupValue` fields (resolves via `phone_number` OR `slack_channel_id` against `users`). `Lookup User By Phone` SQL rewritten to handle both columns based on `_lookupColumn` value. INSERT extended with `slack_message_ts` column (8 positional params). WA path unchanged (transport defaults to 'wa'). Admin-wide commands (LIST/STATS/HELP) get silently dropped by the existing skip-if-not-resolvable path. |

---

## WF-7x — Background Jobs (Post Go-Live)

| WF | Name | Registry Status | Priority | Notes |
|---|---|---|---|---|
| WF-70 | Health Check Monitor | 🔵 Build Fresh | ⚪ P4 | Hourly. Pings WhatsApp API + DB. Slack alert if unhealthy. Post go-live. |
| WF-71 | Payment Reminder | 🔵 Build Fresh | ⚪ P4 | Daily. payment_pending users >24hrs → WhatsApp reminder template. Needs Meta template approval. Post go-live. |
| WF-72 | Inactive User Scanner | 🔵 Build Fresh | ⚪ P4 | Daily. consultation_closed with no activity >90 days → mark inactive. Post go-live. |
| WF-73 | Stale Form Cleanup | 🔵 Build Fresh | ⚪ P4 | Daily. Delete records for users who never submitted form after 7 days. Post go-live. Note: since DB write only happens on form submission, this may clean up any partial records. |
| WF-74 | Data Retention Cleanup | 🔵 Build Fresh | ⚪ P4 | Monthly. Anonymise/delete records beyond retention period. Post go-live. |

---

## 🔨 WORK IN PROGRESS — Next Session Action List

**Technical-review sprint completed 2026-05-16** (commits `a741d0b`, `bf21bc2`):
- ✅ F-01 — `users.stage` column added (fixes WF-44, WF-45 runtime errors)
- ✅ F-02 — `admin_actions` column names fixed (WF-11, WF-47): `action`→`action_type`, `reason`→`notes`
- ✅ F-03 — executeWorkflow nodes downgraded v2→v1 (WF-20, WF-45, WF-12)
- ✅ F-04 — WF-47 workflowId resource-locator → string (UI re-select)
- ✅ F-05 — Postgres `=` prefix on expression queries (WF-45, WF-11, WF-40, WF-46, WF-47)
- ✅ F-06 — WF-43 Gemini `jsonBody` bracket balance
- ✅ F-07 — Slack node operations verified via execution history (WF-11, WF-33, WF-34, WF-41, WF-42, WF-46, WF-51)
- ✅ F-09 — `onError: continueRegularOutput` on webhook nodes (WF-00, WF-10)

**Post-sprint AOD audit 2026-05-16:**
- ✅ F-13 — `alwaysOutputData: true` added to two Postgres SELECT→IF guards: WF-11 `Lookup Blocked User`, WF-10 `Load User Status`. Audit of all 28 workflows showed no other gaps.

**SP-02 (sprint inline-20260522-102910) completed 2026-05-23:**
- ✅ `alwaysOutputData: true` set on 9 Postgres write nodes — protects against silent halt-on-empty for user-keyed UPDATE/INSERT operations. Nodes: WF-21 Insert Pending User, WF-22 Save Slack Channel ID, WF-32 Create Payment Record + Update User Status, WF-34 Reset User Status to payment_pending, WF-44 Save Feedback to DB, WF-45 Set status=payment_pending, WF-47 Update User Status to opted_out + Close Open Consultation. Per-node audit confirmed no IF-guard additions required — upstream WF-02 routing already gates on user-existence; WF-47's existing IF graph handles aod=true empty-`{}` gracefully via FALSE-branch bypass. Mid-flight postgres failures (connection blip after upstream gates pass) are functionally out-of-scope; logged as TD-NEW-029 for the planned error-handling sprint.

**SP-03 (sprint inline-20260522-102910) completed 2026-05-23:**
- ✅ Admin-action precondition audit + centralized validation gate landed in WF-10. WF-10 (28 → 40 nodes) now owns user-exists + phone-match + state-precondition checks for every user-targeted admin command and the WF-41 relay path, plus admin-wide command classification. WF-11 (23 → 18 nodes): duplicate `Parse Command` removed, all trigger-contract refs canonicalized, single-owner cleanup of `Confirm Consultation Closure` + `Confirm User Blocked` (WF-42/46 own those messages). Task #3 downstream trust-mode cleanups: WF-33 (14 → 11), WF-34 (14 → 8), WF-42 (14 → 8) — redundant `User Found?` / `User in Correct State?` guards + their FALSE-branch Prepare/Call WF-51 pairs removed because WF-10 now pre-validates. Smoke test (10 phases A–G, `docs/artefacts/tests/smoke-wf10-centralized-gate-2026-05-23/session.md`) passed; 3 smoke-driven bugs fixed mid-test (BUG-05 BLOCK sibling, BUG-06 admin reason drop, BUG-07 UNBLOCK channel hardcode). Resolves TD-021, TD-022, TD-023.

**data-contract-discipline Phase 1 (sprint 2026-05-24-data-contract-discipline-phase-1) completed 2026-05-24:**
- ✅ Canonical payload contracts landed across the entry-and-relay surface (§2.1 WF-01 envelope, §2.2 WF-10 Command + Relay envelopes, §2.3 WF-50 sender variants, §2.4 WF-51, §2.5 WF-52, §2.6 WF-60). 15 subagent edit-plans (Wave 1: sub-1/2/3/4/5/6a/7 — entry guards + envelope builders on WF-00/01/10/50/51/52/60; Wave 2: sub-6b/8/9/10/11/12/13/14 — consumer-side rewrites + Load-User SELECT removals on WF-02/11/20/21/22/23/30/31/32/33/34/40/41/42/43/44/46) applied to live n8n via curl/jq + verified per-WF via re-fetch and grep. **Strict findings resolved this sprint:** entry-guard validation gates installed on every transport boundary (WF-00/50/51/52/60); WF-01 Silent Reject path patched; WF-30/31/43/44 legacy `message:` key removed; WF-31/32/33/40/42/46 redundant Load-User SELECTs removed (envelope coverage); WF-41 latent runtime bug fix (was reading `input.adminMessage` but Wave-1 Relay Envelope emits canonical `messageText` — admin relays would have failed at WF-50 entry guard); WF-34 envelope adoption (Load User by Phone simplified to payment_id-only fetch). Pseudo (handwritten design source) rewritten linearly for all touched WFs per `[[feedback_pseudo_linear_numbering]]`; .md AS-IS projections regenerated for all 27 workflows. Post-batch sibling regression across all 27 workflows produced zero strict findings; three adjacent findings logged to sprint `followups.md` (WF-10 internal `adminMessage` intermediate field — cosmetic; WF-11 3 internal Slack-payload builders with legacy `message:` — Phase 2 candidate; WF-45 local `Load User Record` SELECT — Phase 2 envelope-discipline candidate). Resolves TD-DCP-010 / -030 / -040 / -050 / -051 / -052 / -060 / -061 / -062.

**Deferred to next sessions:**
- F-08 — admin command smoke test (APPROVE/REJECT/CLOSE/BLOCK) — user-driven interactive
- F-10/F-11/F-12 — post-MVP (UI cosmetic, n8n upgrade, WF-60 message-logging fix)
- GAP-10-IMAGE-PIN — post-MVP (Docker `n8n-prod` `:latest` → digest pin on VPS; full process steps in `docs/artefacts/sprints/pre-golive-gap-decisions-2026-05-26/followups.md` under "Post-Batch-6"). Sprint 2026-05-26 deferred this so the go-live-critical sprint stayed code-only; pick up in a dedicated infrastructure sprint after go-live + one stabilization week.

---

**n8n housekeeping completed 12 Apr 2026:**
- ✅ WF-30 (old New User Onboarding) deactivated — backed up as yIZwO3CZk6bOBAXl
- ✅ WF-25 (Post-Consultation Options) deleted — superseded, backed up as fdlIpl67amL2Ho6U
- ✅ All workflows renamed to match registry WF-XX naming
- ✅ Placeholders created for WF-12, WF-20, WF-23, WF-30 (new), WF-45
- ✅ WF-01 + WF-02 updated with pendingUser routing fix (Apr 2026)
- ✅ WF-21 + WF-22 tested and working end-to-end

**All workflows now exist in n8n. Pick up any workflow below and either implement (placeholders) or fix (rebuild required).**

---

### P1 — Critical path (implement/fix these first)

| Priority | WF | Name | Status | What to do |
|---|---|---|---|---|
| ~~1~~ | ~~WF-32~~ | ~~Payment Confirmation Receiver~~ | ✅ Done | Fixed session 2. |
| ~~2~~ | ~~WF-51~~ | ~~Send Slack Message~~ | ✅ Done | Built + activated session 2. |
| ~~3~~ | ~~WF-40~~ | ~~User → Admin Relay~~ | ✅ Done | Implemented + activated session 3. |
| ~~4~~ | ~~WF-41~~ | ~~Admin → User Relay~~ | ✅ Done | Fixed + activated session 3. |
| ~~5~~ | ~~WF-12~~ | ~~Admin → WhatsApp Relay~~ | ✅ Done | Built + activated session 4. |
| ~~6~~ | ~~WF-47~~ | ~~Unsubscribe Handler~~ | ✅ Done | Built + activated session 4 (ID: 2U7mxHMyqA41ROKX). |
| ~~7~~ | ~~WF-20~~ | ~~Keyword Handler (STOP fix)~~ | ✅ Done | STOP branch now calls WF-47. Fixed session 5. |
| ~~8~~ | ~~WF-11~~ | ~~Command Parser (UNBLOCK)~~ | ✅ Done | UNBLOCK branch added session 5. Parse Command updated for UNBLOCK parsing + targetPhone field. |
| ~~9~~ | ~~WF-01~~ | ~~Message Router (opted_out routing)~~ | ✅ Done | opted_out routing added session 5. Lookup extended to include opted_out; Layer 3 distinguishes REJECTED/OPTED_OUT/PASSED; new Opted Out? IF node routes to WF-21. |
| ~~10~~ | ~~WF-25~~ | ~~Intent Classifier~~ | ✅ Done | Built + activated session 6. ID: eTV1lUcYrXBg2q2T. Gemini 2.0 Flash Lite, API key in URL param. |

### P2 — Core (needed for complete journey)

| Priority | WF | Name | Status | What to do |
|---|---|---|---|---|
| ~~11~~ | ~~WF-22~~ | ~~Form Response Handler~~ | ✅ Done | Payment instructions updated to correct UPI wording. Session 5. |
| ~~12~~ | ~~WF-45~~ | ~~Rebook Handler~~ | ✅ Done | Payment message updated to correct UPI wording. Session 5. |
| ~~13~~ | ~~WF-43~~ | ~~Post-Consultation Handler~~ | ✅ Done | Rebuilt session 6. WF-25 → rebook→WF-45, feedback→WF-44, general→Gemini. Active. |

### P3 — Supporting (degraded but functional without)

| Priority | WF | Name | Status | What to do |
|---|---|---|---|---|
| ~~14~~ | ~~WF-30~~ | ~~Payment Pending Intent Filter~~ | ✅ Done | Implemented session 6. WF-25 → contextual reply + UPI reminder. Active. |
| ~~15~~ | ~~WF-31~~ | ~~Payment Submitted Handler~~ | ✅ Done | Fixed session 6. WF-25 → "under review" ack. Active. |
| ~~16~~ | ~~WF-23~~ | ~~Pre-Form Intent Filter~~ | ✅ Done | Implemented session 6. WF-25 → text reply + re-send flow form. Active. |
| ~~17~~ | ~~WF-44~~ | ~~Feedback Recorder~~ | ✅ Done | Rebuilt session 6. Saves feedback to DB, clears stage, sends ack. Active. |

### Final Activation Sequence (after all above done)

Activate in this order:
1. ~~WF-51 → WF-40 → WF-41 → WF-12~~ (done)
2. ~~WF-32~~ (done)
3. ~~DB migration~~ — opted_out added to users.status (done session 5 per Prasad)
4. ~~WF-47~~ (Unsubscribe Handler) — done, active (ID: 2U7mxHMyqA41ROKX)
5. ~~WF-20~~ — STOP fix done, active
6. ~~WF-11~~ — UNBLOCK done, active
7. ~~WF-01~~ — opted_out routing done, active
8. ~~WF-22 + WF-45~~ — payment message fix done
9. ~~WF-25~~ (Intent Classifier) — built + activated session 6 (ID: eTV1lUcYrXBg2q2T)
10. ~~WF-43, WF-44~~ — rebuilt + activated session 6
11. ~~WF-30, WF-31, WF-23~~ — implemented + activated session 6
12. **End-to-end smoke test** — 🔄 IN PROGRESS (session 7). Steps 1–4 ✅. Blocked at step 5 by WF-02 bug (now fixed). Resume from step 5.

---

## n8n Inventory — Full Mapping (Updated 12 Apr 2026)

All 25 workflows now present in Mumbai VPS n8n (20 existing + 5 placeholders created Apr 2026). Names match registry.

| n8n Name | n8n ID | Active? | Registry WF | Status |
|---|---|---|---|---|
| WF-00 Webhook Receiver | JQu1MkK5vgtUCeNO | 🟢 Yes | WF-00 | ✅ No changes needed |
| WF-01 Message Router | hYGNM97sXvdo1WmI | 🟢 Yes | WF-01 | ✅ Updated session 5: opted_out routing added (Lookup extended, Layer 3 OPTED_OUT path, new Opted Out? IF → originally `Route Opted-Out to WF-21`; 2026-05-25 TD-DCP-107 renamed/retargeted to `Route Opted-Out to WF-26`). |
| WF-02 User State Router | PubCsNTOspF3xqXZ | 🟢 Yes | WF-02 | ✅ Rebuilt session 7 (24 Apr 2026). All stale/disabled nodes replaced. 8 routes: NEW_USER, PRE_FORM_TEXT, DETAILS_FORM, PAYMENT_CONFIRM, PAYMENT_PENDING_TEXT, PAYMENT_SUBMITTED_TEXT, RELAY, POST_CONSULT_TEXT. Detect Route uses messageType + rawMessage.interactive.type to distinguish button vs flow submissions. |
| WF-10 Slack Admin Handler | wMh0oBRtJbvhLgOf | 🟢 Yes | WF-10 | ✅ No changes needed |
| WF-11 Command Parser | GoTYo0GS2y8qjjkw | 🟢 Yes | WF-11 | ✅ UNBLOCK branch added session 5. Parse Command handles UNBLOCK <phone> → commandType=UNBLOCK_USER + targetPhone. |
| WF-20 Keyword Handler | LgIDj1v4ZbCPlX25 | 🟢 Yes | WF-20 | ✅ Fixed session 5 — STOP branch now calls WF-47. Updated May 2026 (TD-027): HELP response is now status-aware — ternary on userStatus for payment_pending/submitted/consultation_active/consultation_closed; generic fallback for new/unknown. |
| WF-25 Intent Classifier | eTV1lUcYrXBg2q2T | 🟢 Yes | WF-25 | ✅ Built + activated session 6. Gemini 2.0 Flash Lite, temp=0. Intents: wants_consultation, general_enquiry, rebook_intent, feedback_intent, garbage, malicious_abusive, inappropriate. garbage→warn+Slack notify. malicious/inappropriate→warn+auto-block via WF-46. API key in URL param. |
| WF-21 New User Welcome + Form | zM8WbxSdt9nXRoLZ | 🟢 Yes | WF-21 | ✅ Tested + working Apr 2026 |
| WF-22 Form Response Handler | dr8QM0m92Ml8MvIh | 🟢 Yes | WF-22 | ✅ Payment message updated to correct UPI wording session 5 (+91-9653240263). |
| WF-23 Pre-Form Intent Filter | VpCER0Vqq3NYJGpI | 🟢 Yes | WF-23 | ✅ Implemented + activated session 6. Calls WF-25 → sends contextual text reply + re-sends WhatsApp Flow form (flowId: 1408011897720771). garbage/malicious/inappropriate handled by WF-25. |
| WF-30 Payment Pending Intent Filter | gGJBY5fJha0Let8I | 🟢 Yes | WF-30 | ✅ Implemented + activated session 6. Calls WF-25 → sends contextual payment reminder for all pass-through intents. Re-sends UPI details +91-9653240263. |
| WF-30 New User Onboarding (WRONG - DEACTIVATED) | z6as85o3b1zK22eF | ⚫ No | ❌ | ⚫ Deactivated Apr 2026 — wrong design |
| WF-31 Payment Submitted Handler | HB8nXudAtk9iXz7C | 🟢 Yes | WF-31 | ✅ Fixed + active session 6. Calls WF-25 → sends "under review" acknowledgement for pass-through intents. No state change. |
| WF-32 Payment Confirmation Receiver | emUOLWVZiNVxcOe3 | 🟢 Yes | WF-32 | ✅ Fixed 12 Apr 2026 — calls WF-52 → WF-51 |
| WF-33 Payment Approval Processor | NcHZedq9ycnAQ9SW | 🟢 Yes | WF-33 | ✅ Verify correct |
| WF-34 Payment Rejection Processor | se82n3MUQ9xE5aEr | 🟢 Yes | WF-34 | ✅ Verify correct |
| WF-40 User -> Admin Relay | du32QBZbSQOjfESe | 🟢 Yes | WF-40 | ✅ Implemented + activated session 3 |
| WF-41 Admin -> User Relay | 6PzJRZsF7k2d9hV7 | 🟢 Yes | WF-41 | ✅ Fixed + activated session 3 (creds + WF-50 ID corrected) |
| WF-42 Consultation Closer | fx70vqyJtRdF2DgR | 🟢 Yes | WF-42 | ✅ Verify correct |
| WF-43 Post-Consultation Handler | 3va0M06kijgyLejf | 🟢 Yes | WF-43 | ✅ Rebuilt + activated session 6. Calls WF-25 → rebook_intent→WF-45, feedback_intent→WF-44, general/wants_consultation→Gemini response via WF-50. |
| WF-44 Feedback Recorder | Du2CJ3OTohRFZYoA | 🟢 Yes | WF-44 | ✅ Rebuilt + activated session 6. Saves messageText to users.feedback + clears stage=NULL. Sends ack via WF-50. |
| WF-45 Rebook Handler | MUG7rPgSHc7UtAE9 | 🟢 Yes | WF-45 | ✅ Payment message updated to correct UPI wording session 5. |
| WF-47 Unsubscribe Handler | 2U7mxHMyqA41ROKX | 🟢 Yes | WF-47 | ✅ Built + activated. 2026-05-21 (TD-B redesign): STOP unconditional — any state → status=opted_out + current_consultation_id=NULL. consultation_active also auto-closes consultations row. slack_channel_id present → opt-out notice via WF-51. No admin_actions write. |
| WF-46 User Blocker | UV62An60fzflU0uD | 🟢 Yes | WF-46 | ✅ Verify correct |
| WF-50 Send WhatsApp | BUVun38WEKb12zg9 | 🟢 Yes | WF-50 | ✅ No changes needed |
| WF-51 Send Slack Message | wlZRK0YxnhP0b2RL | 🟢 Yes | WF-51 | ✅ Built + activated 12 Apr 2026 |
| WF-52 Slack Channel Manager | IO5BZLUxuVmjzk5I | 🟢 Yes | WF-52 | ✅ No changes needed |
| WF-60 Message Logger | 6H75p935FpBVBQtV | 🟢 Yes | WF-60 | ✅ No changes needed |

---

## Build Summary (as of 15 Apr 2026 — session 6)

| Category | Total | 🟢 Done | 🔨 Fix/Verify | 🔵 Build Fresh | ⚪ Post Go-Live |
|---|---|---|---|---|---|
| WF-0x Infrastructure | 3 | 3 | 0 | 0 | 0 |
| WF-1x Admin | 3 | 3 | 0 | 0 | 0 |
| WF-2x Onboarding | 4 | 4 | 0 | 0 | 0 |
| WF-3x Payment | 5 | 5 | 0 | 0 | 0 |
| WF-4x Consultation | 8 | 8 | 0 | 0 | 0 |
| WF-5x Messaging | 3 | 3 | 0 | 0 | 0 |
| WF-6x Data | 1 | 1 | 0 | 0 | 0 |
| WF-7x Background | 5 | 0 | 0 | 0 | 5 |
| **Total** | **32** | **27** | **0** | **0** | **5** |

**🎯 All pre-go-live workflows are active. Next: end-to-end smoke test (session 7).**
