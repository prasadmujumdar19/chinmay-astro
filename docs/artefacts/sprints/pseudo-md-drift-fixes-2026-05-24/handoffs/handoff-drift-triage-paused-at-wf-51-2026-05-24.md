## Stopping Point

Per-WF drift triage for sprint `pseudo-md-drift-fixes-2026-05-24` paused after completing WF-45, WF-46, WF-50 (UTC 2026-05-24T07:36:02Z). Triage Phase 2 has now covered 24 of 27 WFs (21 from prior handoff + 3 this session). `tasks.md` now carries 28 sprint items: TD-DRIFT-001..026 from prior sessions, plus three new this session — TD-DRIFT-027 (WF-45 .pseudo Step 4 greeting copy align live→pseudo per Option A precedent, drop dead `name`/`userId` optionals, structured Inputs with passthrough-extras note from WF-20/WF-43/WF-44 callers, placeholder `void` Outputs); TD-DRIFT-028 (WF-46 .pseudo structured Inputs + D4 Notes annotation per TD-DRIFT-025 precedent + D8 accepted-as-is per TD-DRIFT-011 / WF-25 D2 precedent); TD-DRIFT-029 (WF-50 .pseudo discriminated-union structured Inputs — discriminator + per-variant required fields + logging-context passthrough partition, canonical-vs-legacy alias documentation pending TD-DRIFT-013, D6 minor accepted-as-is). **Retroactive caller-list audit completed this session** (per user instruction following WF-45 systemic-observation) — all 8 prior per-WF triage rows (WF-32/33/34/40/41/42/43/44) had correct caller lists; no retroactive fixes needed. WF-45's gap (handoff narrative missed WF-20 caller) was confirmed as narrative-prose oversight, not tracker error — WF-20 is already in tracker as its own 🔴 DRIFT row awaiting triage.

## Next Action

Resume per-WF triage starting with **WF-51** (Send Slack Message — shared sub-workflow called by many leaves; canonical input contract `{channelId, messageText}`). Then continue sequence WF-52 → WF-60. Reminders for the three remaining WFs:

- **WF-51** tracker findings (2): D3 (no error-branch node for Slack API failure — `.pseudo` acknowledges as TD-NEW-028; live node set has no placeholder error path), D2 minor (Trigger uses passthrough; `.pseudo` implies structured destructuring at entry). Likely a single pseudo bundle item; D3 is a real reliability gap that the contract-first sprint or a tech-error sprint owns — defer/cross-reference rather than fix here. Apply [[feedback_pseudo_tech_separation]] (D3 is mechanism, defer to tech-error-handling sprint).
- **WF-52** tracker findings (3): **D1 admin user IDs mismatch — REAL needs-decision moment** (live invites `U0B4BBML6CS` + `U0A4175DJ5D`; pseudo says `U0AGTECS1KR`; neither live ID matches the spec — user must confirm canonical admin IDs). D3 (`$env.SLACK_TEAM_ID` in pseudo vs `channelData.context_team_id` runtime derive in live), D9 (vague Inputs). Use **citation-discipline pattern** per [[feedback_needs_decision_context]] when surfacing D1 — quote both sets of admin IDs verbatim, note that all three may be stale, and ask which IDs are canonical for the post-payment-approval channel invite. Per [[feedback_admin_message_tone]], the surfacing prompt should use business framing (admin IDs, not internal Slack-user-ID jargon).
- **WF-60** tracker findings (3 + 1 minor): D9 (discriminated-union variants `transport='wa'|'slack'` not declared as distinct variants), D8 (`inboundMessageId`/`sentAt` consumed by `Extract Message Data` but absent from `.pseudo` Inputs; top-level vs nested status ambiguous), D7 (`reason='pre_onboarding_user'` live vs `'no userId — …'` pseudo — string contract diverges), D6 minor (`onError: continueRegularOutput` setting). **Apply the discriminated-union Inputs template established in TD-DRIFT-029** (WF-50): WF-60's `transport` discriminator → per-variant required (`wa` requires `phoneNumber`/`whatsappMessageId`; `slack` requires `slackChannelId`/`slackMessageTs`); WF-50's pattern translates directly.
- For each WF: read `docs/pseudocode/WF-XX.{pseudo,md}`, grep tracker `### WF-XX findings` in `docs/artefacts/drift-checks/2026-05-24/tracker.md`, then run audit-before-spec per [[feedback_audit_before_spec]] + [[feedback_systemic_before_individual]]. Apply [[feedback_pseudo_tech_separation]] to auto-defer n8n-mechanism findings, [[feedback_pseudo_linear_numbering]] for step-numbering (branch sub-steps like `Step 3a` are OK — only tombstones are disallowed), and citation discipline (cite specific tasks.md line numbers from prior decisions when surfacing options).

## Blockers

- **No user input blockers** for resuming WF-51. **WF-52 will produce a real needs-decision moment for D1** (canonical admin user IDs for post-payment-approval channel invite — three candidate IDs in play: live `U0B4BBML6CS`, live `U0A4175DJ5D`, pseudo `U0AGTECS1KR`; none verified). Surface via `AskUserQuestion` with citation context per [[feedback_needs_decision_context]].

- Established decision patterns from this session that should carry into remaining triage:
  - **Discriminated-union Inputs template (TD-DRIFT-029 precedent):** For WFs with a variant-selecting discriminator field (`messageType`, `transport`, etc.) the Inputs block organizes into three partitions: (a) discriminator + common required fields, (b) per-variant required fields (one block per variant), (c) passthrough fields (forwarded but not consumed locally — e.g., logging-context). Apply directly to WF-60.
  - **Convention-only contract acceptance (TD-DRIFT-028 precedent extending TD-DRIFT-011 / WF-25 D2):** D8-class findings about "trigger uses `inputSource=passthrough` with no schema" are accepted-as-is project-wide via a Notes-line annotation citing the WF-25 D2 + WF-46 D8 precedents. Cross-cutting schema-hardening would be its own dedicated tech sprint, not a per-WF decision.
  - **Read-source / cosmetic-passthrough Notes convention (TD-DRIFT-025/028 precedent):** D4-class findings ("mappingMode=passthrough with `convertFieldsToString=true`; pseudo implies explicit hand-off") where the upstream Code node emits the canonical sub-workflow contract are accepted-as-is via a Notes-line annotation. Zero live churn; document the convention.
  - **Citation discipline for D1 needs-decision moments (TD-DRIFT-027 precedent):** When surfacing options to the user for materially-divergent copy/IDs/text, cite specific prior precedent items by line number from `tasks.md` AND quote both versions verbatim so the user can compare without re-reading the source. Pattern carried from prior handoff and reinforced this session via D1 greeting-copy decision.
  - **Pseudo-vs-live drift vs robustness-gap distinction (TD-DRIFT-027 D5 precedent):** A finding that the live "bypasses SELECT" or "lacks zero-row guard" is often NOT actually drift if the pseudo and live agree on the read source; the latent risk is a robustness concern that goes to the tech-error-handling sprint via `deferred-to-tech-sprint.md`, NOT the per-WF pseudo bundle. Carry forward when triaging WF-51 D3 (Slack API failure error branch — likely tech-error defer) and WF-60 D6 (`onError: continueRegularOutput` setting — likely tech-error defer).

- **Pre-existing tasks.md anomaly (not fixed, carried from prior handoff):** Duplicate `### TD-DRIFT-001 · WF-00 nfm_reply parse path — fix live` heading at lines 66 and 111 (one in 🔴 P0 section, one in 🟠 P1 section). Likely paste-error during a prior P0↔P1 reclassification. User informed in prior handoff; chose to defer dedupe. Address before sprint execution if not earlier.

- **Plugin improvement candidates (carried from prior handoff + this session):**
  1. (Carried, still pending) `pseudo-md-drift-check` skill should document the override pattern (27 parallel Sonnet subagents in background, parent-aggregates JSON) as a valid path with discipline.
  2. (Carried) D8/D9 rubric in `pseudo-md-drift-check` should call out router-workflow distinction between consumed inputs and passthrough-to-sub-workflow inputs.
  3. (Carried) Elevate `feedback_pseudo_tech_separation` from project memory to plugin principle.
  4. (Carried) Document "audit-before-cross-cutting-fix" as a build-sprint principle.
  5. (Carried) D8.5 rubric refinement — input-alias finding requires caller-side cross-check before triage decision.
  6. (Carried) Linear pseudo numbering convention promotion to plugin-level guidance.
  7. (Carried, 2026-05-24) Drift-check D1 rubric missed the WF-42 Slack-confirmation message-text divergence (multi-line embedded literal in a Code node).
  8. (Carried, 2026-05-24) Cross-cutting items with `mappingMode=passthrough` callers MUST audit the upstream Code/Set node (not just the Execute-Workflow node's `workflowInputs.value`).
  9. (Carried, 2026-05-24) "Read-source convention" pseudo-Notes pattern — when live mixes trigger-envelope reads and prior-node `$json` reads for identical values, the cleanest fix is a Notes-line annotation declaring the preferred convention rather than a live cleanup edit.
  10. (Carried, 2026-05-24) "Citation discipline for needs-decision moments" — when surfacing options to the user, cite specific `tasks.md` line numbers from prior decisions so the user can verify the precedent.
  11. **(New, 2026-05-24 this session)** **Discriminated-union Inputs template** — for WFs with a discriminator field (`messageType`, `transport`, intent-result, etc.), the Inputs block should partition into (a) discriminator + common required, (b) per-variant required (one block per variant), (c) passthrough / logging-context fields. Established in TD-DRIFT-029 (WF-50), will be reused in WF-60. Worth promoting to a plugin-level Inputs-block authoring guideline.
  12. **(New, 2026-05-24 this session)** **Caller-list audit-on-handoff-reconciliation pattern** — when a handoff's narrative caller-list ("called from WF-43 and WF-44") may be incomplete, run the dependency audit before committing to a per-WF item. The retroactive audit this session (all 8 prior triage rows checked, zero gaps found) validates that single-caller WFs rarely produce narrative-prose errors, but multi-caller WFs (like WF-45's three callers) need explicit dependency-map verification. Worth promoting as a build-sprint hook: "if next per-WF item has >1 caller per dependency-map, verify handoff narrative before triage."

  Apply all 12 via `flush-plugin-improvements` skill in a future session — context grew during this triage, deferring rather than mid-session apply.

## Changed Reference Values

None. No credentials, n8n IDs, URLs, or environment values changed this session.

New artefacts created or modified (existing-locations, not changed values):
- `docs/artefacts/sprints/pseudo-md-drift-fixes-2026-05-24/tasks.md` — 3 new sprint items added this session (TD-DRIFT-027 WF-45, TD-DRIFT-028 WF-46, TD-DRIFT-029 WF-50).
- `docs/artefacts/sprints/pseudo-md-drift-fixes-2026-05-24/deferred-to-tech-sprint.md` — 2 new D5/D6 entries added (WF-45 D5 trigger-envelope UPDATE robustness, WF-46 D6 shared zero-row error-path gap).
- `docs/artefacts/drift-checks/2026-05-24/tracker.md` — WF-45, WF-46, WF-50 rows and their finding sections updated with triage status (🔴 DRIFT → 🟡 TRIAGED) and sprint-item cross-references.
