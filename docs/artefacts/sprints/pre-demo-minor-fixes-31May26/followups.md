# Follow-ups — pre-demo-minor-fixes-31May26

## [2026-06-06] — PDF-04/05 validation finding (adjacent)

- **WF-25 Intent Classifier** (`eTV1lUcYrXBg2q2T`): service/offering/pricing questions phrased with booking language are mis-classified `wants_consultation` instead of `general_enquiry`, so they bypass the grounded-KB Gemini reply (the PDF-04/05 fix) and get the bare payment-reminder fallback.
  - **Repro:** test user 61466927921 (`payment_pending`). "Do you offer video consultations?" → `general_enquiry` → grounded reply ✅ (exec 3668). "How much is audio consultation and how do I get it" → `wants_consultation` → `Prepare Payment Reminder` ❌ (exec 3680). Same class of question, inconsistent result.
  - **Root cause:** WF-25 category def `wants_consultation: "user wants to book a new consultation OR is asking about booking"` — the "asking about booking" clause overlaps `general_enquiry: "general question about ... the service"`. "how do I get it" trips the booking clause.
  - **Classification:** adjacent to PDF-04/05 (the fix itself is verified working; this is upstream routing). Does NOT block PDF-04/05 done.
  - **Blast radius:** WF-25 is shared by 4 callers (WF-30, WF-31, WF-40, WF-43) per dependency-map — needs impact analysis before change.
  - **Proposed fix:** narrow `wants_consultation` to explicit proceed/book intent ("I want to book", "let's start", "I'm ready to pay"); move service/offering/pricing/how-to questions to `general_enquiry`. Impact-check WF-40 (relay) doesn't branch on the distinction.
  - **Decision:** RESOLVED 2026-06-06 — fixed now (Option 1, bundled). Tracked as sprint item **PDF-10** (done). User-directed scope: `wants_consultation` = text-consultation booking intent only; service/non-text/astrology-adjacent → `general_enquiry`.

## Plugin improvement candidates (flush at sprint/batch boundary, not now)

- ✅ **FLUSHED — plugin v1.40.0 (commit c33a8ef), 2026-06-08.** Shipped `scripts/assert-pseudo-fresh.sh` (hermetic `live_reconciled_at` frontmatter stamp gate) + plan-sprint Step 3g pseudo-impact tag + build-workflow 5f.0 post-build stamp step + build-sprint batch/close gate. **New gate for plugin (build-workflow + build-sprint + plan-sprint, 2026-06-07) — PSEUDO FRESHNESS GATE.** Root cause of a real miss this sprint: PDF-04/05/10/11/12 made functional-logic changes (grounded KB, topic-gated defer, intent-boundary, button re-attach, payment block) but the `.pseudo` files were never updated — pseudo went stale vs live. Pseudo-first is a documented principle ([[feedback_pseudocode_first_refactor]]) but **no skill enforces it as a step**; the `.md` AS-IS has `assert-md-fresh.sh` as a guard, pseudo has no equivalent. PDF-10/11/12 also emerged ad-hoc in a live session, outside item ceremony, so there was no per-item checkpoint at all. **Fix (3 layers):**
  - **plan-sprint:** at classification, tag each item `pseudo-impact: yes/no` (yes = functional-logic change: routing, branches, new/removed business rules, what message/action is produced under what condition; no = non-functional: node typeVersion bump, mapping/passthrough plumbing, error-handling-only, pure cosmetic copy that doesn't change *when/why* a message is sent). Persist into state.md item block.
  - **build-workflow:** add a step right after *classify* — if functional → pseudo-first (read+revise `.pseudo` before touching live); if non-functional → record `pseudo: N/A — <reason>` explicitly. No silent skip.
  - **build-sprint:** batch-close assertion mirroring the `.md` freshness check — no item reaches `done` until its `.pseudo` was updated OR explicitly marked `pseudo: N/A`. Consider shipping an `assert-pseudo-fresh.sh` analogous to `assert-md-fresh.sh` (but pseudo is hand-authored, so the check is "was it touched in this item's change set / does it carry the new business rule," not an mtime hash).
  - **Key design note:** the functional-vs-cosmetic test must be explicit so the gate doesn't over-fire — PDF-09 (the "Dr. Chinmay" rename) correctly needs NO pseudo; PDF-04/05/10/11/12 all do. Test phrasing: *"does it change WHAT the workflow does, or only HOW / which words?"*
  - Routing: methodology-level → plugin skills via update-skill workflow ([[feedback_update_skill_routing]]); not a cache edit. Flush via `flush-plugin-improvements` at sprint/batch boundary.


- ✅ **FLUSHED — plugin v1.38.1 (commit 04f83ee), 2026-06-08.** brainstorm-sprint Step 2 now requires grounding third-party-platform-dependent items in the platform's published docs (cited), with a matching Red-Flags row. **New pattern for plugin (brainstorm-sprint Step 2 grounding, 2026-06-08):** When a candidate gap hinges on a **third-party platform's rules** (Meta/WhatsApp 24h window, template/parameter constraints, pricing), "verify against live state" (current Step 2) is necessary but **not sufficient** — the live n8n workflows can be internally consistent yet still violate an external platform rule the team only half-remembers. brainstorm-sprint Step 2 should add: *when the issue depends on an external platform's behaviour, ground the design in that platform's published docs (cited), not on assumption or memory.* This sprint's 24h-window items (PDF-15..19) were grounded in Meta's own pricing + template-guideline pages and the citations were captured in the design spec — the user explicitly required "ground in actual Meta documentation, not your own understanding." Routing: methodology-level → brainstorm-sprint skill via update-skill workflow ([[feedback_update_skill_routing]]).

- ✅ **FLUSHED — plugin v1.39.0 (commit 0b4fe12), 2026-06-08.** plan-sprint Step 3d single-root symptom-cluster detection (carrier item + hard-dep'd siblings) + Step 3f single-root exception to the never-mix-priority rule. **New pattern for plugin (plan-sprint, 2026-06-04):** Exception to the "never mix priority tiers in one batch" rule. When N items are independent *symptoms of ONE inseparable single-workflow fix* (e.g. PDF-06 P1 / PDF-07 P0 / PDF-08 P2 — all one WF-10 genuine-message guard across its relay, alert, and WF-60-logging branches), collapsing them into a single mixed-priority batch is correct because splitting by priority would mean authoring the same change three times. plan-sprint should: detect same-root-same-workflow symptom clusters, collapse into one batch, designate a carrier item, hard-dep the siblings on the carrier, and record the mixed-priority justification explicitly. Currently the skill's Step 3f says priority dominates batch assignment — add the single-root-fix exception alongside the contract-coupling guidance in Step 3d.

- ⏳ **CANDIDATE (not yet flushed) — `generate-workflow-md.py` per-WF filter (build-workflow / generate-workflow-md, 2026-06-08).** The script's signature is `generate-workflow-md.py <workflows_dir> <output_dir>` — it ignores any further argv and globs **every** `workflows/*.json`, rewriting all ~31 `.md` files (each with a fresh `generated_at`). After a single-workflow build (e.g. PDF-15 touched only WF-41) this produces ~30 phantom diffs whose only change is the timestamp. It's harmless to GitHub on chinmay-astro (no local `.git`; commits are scratch-clone-copy of named files only), but on a project that commits the working dir directly it would churn 30 files per build, and it adds local Drive-resync noise everywhere. **Fix:** add an optional filter — `generate-workflow-md.py workflows docs/pseudocode --only WF-41,WF-50` (or a positional WF-ID list). In `main()` accept a `wf_filter` set; in the glob loop, after `parse_wf_id`, `continue` if a filter is set and `wf_id not in wf_filter`. Update the build-workflow Step 8 / build-sprint regen call sites to pass the touched WF-IDs. Routing: methodology-level → plugin via update-skill/flush-plugin-improvements ([[feedback_update_skill_routing]]). Surfaced by the user during PDF-15 build.
