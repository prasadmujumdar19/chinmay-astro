# Handoff — SP-10 Invocation 1 (Contract-First trio) landed as plugin v1.26.0; Invocation 2 (j+k+m) next

_Written 2026-05-23T11:30:00Z_

## Stopping Point

SP-10 Invocation 1 complete and landed. Plugin `n8n-whatsapp-methodology` bumped 1.25.0 → 1.26.0 (commit `6d0ab53` on `prasadmujumdar19/n8n-whatsapp-methodology` `main`); active cache renamed `1.25.0 → 1.26.0` with symlink, `installed_plugins.json` updated, marketplace cache aligned. Chinmay-astro `state.md` committed and pushed as `4422d39` on `prasadmujumdar19/chinmay-astro` `main` over `2475b64` (handoff carry-over commit) — captures the 3-principle expansion (j/k/m added per Option B), the 4-invocation sub-plan, and the Invocation 1 progress record. SP-10 status now `in-progress`. Stopped here proactively at ~70% context per build-sprint Step 4a — not at a blocker.

## Next Action

Re-invoke `/n8n-whatsapp-methodology:build-sprint` (no args — `_active` marker auto-resumes). Skill lands at SP-10 mid-execution. Then invoke `/n8n-whatsapp-methodology:flush-plugin-improvements` for **Invocation 2 — Authoring discipline (j+k+m)** targeting plugin v1.27.0. The full spec is already drafted in `docs/artefacts/sprints/inline-20260522-102910/state.md` under SP-10's `execution_sub_plan` → "Invocation 2 — Authoring discipline" subsection. Concretely:

1. `build-workflow/SKILL.md`:
   - **Step 5 scope rubric:** split the current "Anything else" bucket (3+ node mods / new node + rewiring / lint debt) into two distinct buckets — `jq-on-disk` (default for structural changes; preserves node IDs/positions/credentials/webhookIds verbatim) and `author-fresh` (Python script rebuilds from scratch; higher bar, explicit-approval gate).
   - **New Step 5e.0 "Author-fresh gate":** explicit-approval requirement before invoking author-fresh; justification rubric (pseudocode-driven complete rebuild OR jq-on-disk complexity itself is a defect risk). Cite SP-03 WF-10 28→38 rebuild as the validated case (2026-05-23T02:30:47Z).
   - **New Step 5e.1a "typeVersion floor":** before authoring fresh nodes, grep live JSON for every existing instance of the same `.type`; pick the highest already-present typeVersion as the default; never auto-bump. Post-build verification = per-type typeVersion-array diff (live pre vs new post); only valid delta is "removed-version dropped" or "new entry equals existing version". Cite SP-11 exec-1630 Set v3.3→v3.4 silent-bump scenario as the canonical failure mode (cross-references Step 5f.5 already added in v1.26.0).
   - **New Step 5g "Message authoring conventions"** (placed post-5f, before Step 6 lint): forbidden-token list + sanity test ("would a small-business owner understand without a glossary?"). Forbidden tokens: `WF-XX`, `WF-XX.pseudo`, `DB row`, `users row`, `pending_users`, `slack_channel_id`, `executeWorkflow`, `sub-workflow`, `IF node`, `Set node`, `payload`, internal field names (`messageType=...`, `userStatus=...`, `pendingUser=...`, `interactiveType=...`), architecture references ("edge case", "pre-onboarding-STOP gate", "eliminates X at the source"). Cite SP-11 + WF-02 UNHANDLED alert rewrites as validation.
2. `scripts/lint-workflows.py`:
   - **New advisory check `forbidden_tokens_in_message_strings`:** grep Set/Code nodes for text-emit fields (`messageText`, `assignments[].value` for Set v3.4 nodes whose name matches `Build .* (Alert|Payload|Message)`, `jsCode` for Code nodes building messageText). Flag any forbidden-token match. Advisory — false-positive prone (real customer messages might legitimately mention internal-sounding words), never hard-reject.
   - **New advisory check `typeversion_bump_against_live`:** harder to implement than (j) because it needs the *live pre-PUT* snapshot to compare against. Recommended approach: implement as a two-script pattern — Step 5e pre-flight saves `/tmp/claude-scratch/_pre-PUT-<wf-uuid>.json`; post-PUT check reads both pre- and post-PUT JSONs, builds per-`.type` typeVersion sets, flags any post-set element that wasn't in the pre-set. The lint script's `lint_workflow(path)` currently takes only the workflow JSON file path — extend signature to optionally accept a pre-PUT comparator path, or refactor to a separate function called from build-workflow Step 6a rather than from the main lint loop. **This is a design choice worth surfacing to the user before coding** — pick between (a) extending lint-workflows.py signature, (b) adding a separate post-PUT-only script.

Pre-flight before starting Invocation 2:
- Confirm n8n SSH tunnel open (this session: tunnel was confirmed up at start, n8n healthz=200). New session should re-verify.
- Confirm plugin v1.26.0 is the active cache version: `ls -la ~/.claude/plugins/cache/prasadmujumdar19/n8n-whatsapp-methodology/ | grep 1.26.0` should show the dir, and the `1.25.0` symlink should resolve to `1.26.0`.

## Blockers

None operationally. One **methodology observation worth capturing as a plugin improvement** before next session, surfaced this session but not yet acted on:

- **Plugin improvement: lint-hook-landing-severity pattern.** When a new lint hook encodes a principle whose existing-state violations are numerous (90 sites in this case), the hook should default to `warn` severity at landing, with a documented "flip to hard-reject after remediation initiative" path. The hard-reject design intent lives in the skill text (build-workflow Step 5f.2 says "rejected at lint"); the lint-script implementation lands as advisory with a CHANGELOG callout. Validated this session — user chose this option after the smoke-test surfaced 90 findings. Apply via `flush-plugin-improvements` skill before next session, OR fold into Invocation 2's CHANGELOG as a methodology callout. Target file: `flush-plugin-improvements/SKILL.md` (new "Lint-hook severity at landing" subsection) and/or `build-workflow/SKILL.md` (Step 6 lint description — note the warn-vs-fail pattern as standard rather than ad-hoc).

## Changed Reference Values

- **Plugin version (active):** `1.25.0` → `1.26.0`. Active cache dir renamed; `1.25.0` is now a symlink to `1.26.0`. `installed_plugins.json` and marketplace-cache plugin.json both updated. `installed_plugins.json` `gitCommitSha` = `6d0ab53`.
- **Plugin commit:** `c42efd0` (1.25.0 baseline) → `6d0ab53 1.26.0: Contract-First sub-workflow calls (SP-10 c/g/n)` on `prasadmujumdar19/n8n-whatsapp-methodology` `main`.
- **Chinmay-astro commit:** `2475b64` (handoff carry-over) → `4422d39 sprint: SP-10 expanded with j/k/m; Invocation 1 (Contract-First) landed as plugin v1.26.0` on `prasadmujumdar19/chinmay-astro` `main`. One file changed: `docs/artefacts/sprints/inline-20260522-102910/state.md` (+111 / -2).
- **Sprint state file:** SP-10 status `pending` → `in-progress` with `started_at: 2026-05-23T10:55:00Z`. SP-10 description expanded from 10 → 13 principles (added j, k, m). New `execution_sub_plan` block (~100 lines covering principle→skill mapping, 4-invocation grouping, lint hook spec). New `invocation_progress` block records Invocation 1 outcome (landed_at, landed_files, cache_sync details, lint_severity_decision: "warn-only", verification: 109 advisory findings = 90 contract-first + 19 Set-v3.4).
- **Plugin source-of-truth files now containing v1.26.0 content:**
  - `skills/build-workflow/SKILL.md` — Step 5f.2 rewritten (Contract-First, 4 conditions, whitelist mechanism); Step 5f.3 passthrough-only (removed defineBelow allowance); Step 5f.5 NEW (Set v3.4 includeOtherFields hazard + SP-11 exec-1630 case study); Step 5e regenerate-by-copy reference updated; Step 6 lint description lists 2 new checks.
  - `skills/pseudo-md-drift-check/SKILL.md` — Step 3.2 taxonomy: new D9 row "Inputs contract declaration shape"; Step 3.3 severity rule: any D9 finding = 🔴 DRIFT.
  - `scripts/lint-workflows.py` — module docstring updated; `_build_upstream_map()` + `_has_bypass_note()` helpers added; `lint_workflow()` extended with contract-first advisory check + Set v3.4 includeOtherFields advisory check; `main()` rewritten to support `fail` vs `warn` severity classes (exits 0 if warns-only).
- **Lint backlog baseline (as of v1.26.0 landing):** 109 advisory findings across 27 workflow JSONs in the chinmay-astro project. Breakdown: 90 contract-first violations (executeWorkflow upstream is Code/IF/orphan rather than Set) + 19 Set v3.4 missing-explicit-includeOtherFields (mostly legitimate contract-emit nodes that just need the explicit `includeOtherFields: false` to silence the check). This is the inventory for the future Contract-First Sub-Workflow Calls multi-sprint initiative — start from `python3 ~/.claude/plugins/cache/prasadmujumdar19/n8n-whatsapp-methodology/1.26.0/scripts/lint-workflows.py workflows/ 2>&1 | tee /tmp/claude-scratch/contract-first-baseline.txt`. Note: the SP-05 audit catalogued 18 sites (defineBelow only); the new lint check surfaces 90 because it also catches the Code-upstream pattern, which is the more common defect shape. Use the lint output as the authoritative inventory, not `sp05-defineBelow-sites-2026-05-23.json`.
- **Tunnel state:** n8n + Postgres + pgAdmin reachable via SSH tunnel (n8n healthz=200 confirmed mid-session). Tunnel must remain open for next session.

## Plugin Improvement Candidates (for next session)

In addition to the lint-hook-landing-severity pattern noted in Blockers:

- **Memory→skill sweep (post-Invocation-4):** user raised this side note this session — once Invocations 2-4 are done and j/k/m are encoded in the plugin, do a project-wide sweep of `~/.claude/projects/.../memory/feedback_*.md` entries to classify each as project-specific vs methodology-level (per `[[feedback_principle_placement]]`); for methodology-level entries not yet in any plugin skill, propose an SP-12 or fold into a future plugin update. Goal: keep `MEMORY.md` from accumulating micro-suggestions that belong as plugin discipline. Not urgent — schedule for after Invocation 4 closes. Tracked here so the next session doesn't lose it; log to `followups.md` if Invocation 4 closes before this gets done.
- **Source-memory annotation discipline:** when a memory entry's content gets encoded into a plugin skill (e.g., `feedback_typeversion_floor.md` → build-workflow Step 5e.1a once Invocation 2 lands), the memory file should NOT be deleted — it should get a header line *"Encoded into plugin vX.Y.Z as <skill> Step <N> (YYYY-MM-DD). Kept for historical context — the rule lives in the plugin, the narrative lives here."* This preserves the failure-narrative (exec 1630, SP-11 stories) that proves the rule is non-obvious, without duplicating the rule itself. Apply for Invocation 2's j (`feedback_admin_message_tone.md`) and m (`feedback_typeversion_floor.md`) source memories when those land. For Invocation 1 (c/g/n), no dedicated memory files existed — principles were captured in completion notes/SP-10 description directly, so no memory cleanup needed.
