## Stopping Point
Mid **BMX-P5-MATRIX live smoke** (sprint exit gate). Driving the full onboarding journey (operator's chosen setup path = full journey → consult → close → flip opted_out, so the 7 S8 opted_out cells can be tested with a real slack_channel_id). The journey has NOT yet reached the S8 cells — the session was spent clearing front-of-funnel blockers the static matrix trace missed. Test phone **61466927921**; all detail logged in `docs/artefacts/tests/smoke-bmx-s8-optedout-reengage-2026-05-31/session.md` (system of record per operator directive — sprint artefacts keep one-liners + pointer only). Five bugs found; BUG-01/02/02a/03/05 fixed, BUG-04 deferred. Last fix (BUG-05, WF-22 consent-boolean) is live but NOT yet re-tested.

## Next Action
Re-test the **form submission** to verify BUG-05 fix: from 61466927921 send "Hi" (pre-form user → WF-23 re-sends form), open & fill the form, **Submit**. Confirm WF-22 (`dr8QM0m92Ml8MvIh`) now succeeds → creates `chinmay_astro.users` row status=`payment_pending` + creates `consult-61466927921` Slack channel (WF-52) + sends ₹500 GPay payment instructions to the handset. Watch via the open SSH tunnel (n8n localhost:5678) + Postgres MCP; exec cursor in test dir `.cursors/exec-cursor`. Then continue the journey (tap Payment Completed → APPROVE → CLOSE → flip status=opted_out) to finally run the original **7 S8 opted_out re-engagement cells (S8×A,B,C,D,E,F,I)** — that is the actual BMX-P5-MATRIX acceptance still outstanding.

## Blockers
- **BUG-04 (deferred, non-blocking, cosmetic):** pattern-validated form fields (Full Name, Time of Birth, …) show their error-message on initial render before input. The `(...)?`-optional-pattern fix did NOT suppress it (operator re-tested) → WhatsApp likely validates `required` fields eagerly regardless of pattern. Operator chose to move on. Revisit options: Form-level `error-messages`, or drop `pattern` (keep helper-text, validate server-side), or accept as minor. Not journey-blocking.
- **Stored flow JSON is stale vs published flow.** `workflows/flows/collect-personal-details.json` is still the OLD 2-screen version. The PUBLISHED Meta flow (`1137788551887662`, "Collect Birth Details", Without-endpoint) is now a single-screen layout + `EmbeddedLink` privacy policy + OptIn. Update the stored JSON to match the final published flow (incl. any BUG-04 resolution) before committing the flow.
- **Commit deferred.** Many live workflow edits are exported but uncommitted (operator said "test the flow first"). Decide commit/push at resume. Working dir has no local .git — use the clone-copy-push flow in CLAUDE.md. Don't push the flow JSON until it matches the published flow.
- **`.md` regen pending at sprint end** — WF-01/21/23/45/50/22 `.md` are stale or hand-swapped. Tracked in test dir `followups-md-regen.md`; run one `generate-workflow-md` pass at sprint end.
- Plugin improvement candidate: live smoke caught two bug classes the static `.md` matrix trace structurally cannot (string-vs-bool type mismatch in a Code node; Meta flow-send contract: navigate needs flow_action_payload.screen + routing-model entry-screen rule). Consider a WhatsApp-Flows send/callback pre-go-live checklist in the plugin. Apply via `flush-plugin-improvements` before next sprint if judged cross-project.

## Changed Reference Values
- **WhatsApp Flow ID: `2260297164474475` → `1137788551887662`.** The 2260… ID was a **test-account** flow (same Meta business profile, different account) the production token cannot send → Meta `(#131009)`. New ID is the published Without-endpoint "Collect Birth Details" flow under the correct WABA (phone 919653240263). Persisted in live WF-21 (`zM8WbxSdt9nXRoLZ`), WF-23 (`VpCER0Vqq3NYJGpI`), WF-45 (`MUG7rPgSHc7UtAE9`) + CLAUDE.md Key Credential IDs + workflow-registry + pseudocode WF-21/45 `.pseudo` & WF-21/23/45 `.md`. (Supersedes BMX-P8-DOCS which had set docs to the 2260… test-account ID.)
- **WF-01 (`hYGNM97sXvdo1WmI`)** live changed — Country Filter reads bare-digit phone (BUG-01). Backup `archive/backups/hYGNM97sXvdo1WmI-2026-05-31-09-30.json`.
- **WF-50 (`BUVun38WEKb12zg9`)** live changed — `flow_action_payload:{screen: flowScreen||'DETAILS_SCREEN'}` added (BUG-02a). Backup `…-2026-05-31-10-49.json`. WF-50.pseudo updated.
- **WF-22 (`dr8QM0m92Ml8MvIh`)** live changed — `consent` handled as boolean (BUG-05). Backup `…-2026-05-31-11-32.json`.
- All exports refreshed in `workflows/`. Backups for WF-21/23/45 flow-ID swap: `…-2026-05-31-10-26.json`.
