## Stopping Point
Batch 2 (Phase 0b / BMX-P0-U3) is COMPLETE and post-batch regression passed: WF-62 U3 New-Contact Intent Classifier built (`tJknCwk2PzLpEwTX`, 7 nodes, active), lint clean, MCP strict-validate `valid:true`/0 errors; dependency map rebuilt (78 edges). PLUS an unplanned in-batch fix the user caught: U1 (WF-53, `ONzUJ1Lj9hIbUYT0`) Gemini-failure contract was corrected from `return {notified:true}` to **HALT** (both branches → `Halt on Gemini Failure` stopAndError) per safety-net spec line 151 — a Batch-1 build defect. Live scan of ~250 workflows confirmed **zero live callers of U1**, so no breakage. All item status lives in `state.md` (BMX-P0-DB/U1/U2/U3 ✅ done; Batches 3–10 ⬜ pending).

## Next Action
Start Batch 3 = **BMX-P1-PSEUDO** (pseudo-first authoring): rewrite `.pseudo` for all changed workflows + author new `.pseudo` for U1/U2/U3, reconciled against both specs, BEFORE any Phase 2–4 n8n edit. Fold three things into the new pseudo as you author: (a) **WF-53.pseudo** — U1 halts on Gemini failure + the `Call U1` caller convention (default `onError=stopWorkflow` so the halt propagates); (b) **WF-53.pseudo** — make the admin-alert "the user has been told…" sentence conditional on `userFacing` (the adjacent finding in `followups.md`); (c) **WF-62.pseudo** — U3 returns raw `{bucket, confidence}` and the CALLER applies `confidence<0.5 → unrelated` fail-open.

## Blockers
- **Commit status (read carefully — this handoff is intentionally commit-agnostic):** This file was written BEFORE the Batch-2 commit so it rides in the **same** push; it therefore carries **no commit SHA**. Do **NOT** assume Batch 2 is uncommitted. Proactively check the latest commit on `main` (`gh api repos/prasadmujumdar19/chinmay-astro/commits/main`, or `git log`) and reconcile against `state.md` before re-doing any work. Batch 1 is already committed (`8723082`). If this file is present on `main`, the Batch-2 changeset shipped with it.
- **Batches 5–6 caller obligation (U3):** WF-21/WF-23 must pass `{phoneNumber, text, stage, consultChannelId?}` to U3, read the raw `{bucket, confidence}` and apply `confidence<0.5 → unrelated`, and keep `Call U3` at default `onError=stopWorkflow` so a U3 Gemini-failure halt propagates and terminates the turn (no defaulted intent).
- **Deferred plugin improvement:** handoff-staleness → commit-agnostic phrasing (this very pattern) is logged in `followups.md` under "Plugin improvements (deferred)". Flush via `flush-plugin-improvements` at the sprint boundary, not mid-batch.
- **U3 live test deferred:** end-to-end Gemini-classification execution deferred to Batch 5/6 caller wiring + Batch 10 smoke test (no parent caller yet; Gemini cred lives in the n8n vault, so no standalone curl test). Structure is strict-validated.

## Changed Reference Values
- **WF-62** U3 New-Contact Intent Classifier — n8n id `tJknCwk2PzLpEwTX` (active). Inputs `{phoneNumber, text, stage:new|pre_form, consultChannelId?}`; returns `{bucket, confidence}` (success only; halts on Gemini failure via U1).
- **WF-53** U1 — contract changed: now **halts** on Gemini failure (no return). Backup of pre-fix version: `archive/backups/ONzUJ1Lj9hIbUYT0-2026-05-29-22-43.json`.
