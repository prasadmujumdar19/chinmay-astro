## Stopping Point
Batch 3 (Phase 1 / BMX-P1-PSEUDO) is COMPLETE. All 16 `.pseudo` files authored — 3 net-new utilities (WF-53/61/62) + 13 rewrites (WF-01/02/20/21/23/25/26/30/31/40/43/44/45) — reconciled against the BMX-06 + safety-net specs, tech-agnostic, linear-numbered, with the 3 handoff folds applied. **No live/n8n changes this batch** (pseudo-only). State.md lint clean. Post-batch regression skipped (exclusively documentation batch). All item status lives in `state.md` (Batches 1–3 ✅ done; Batches 4–10 ⬜ pending).

The 3 utility pseudo (WF-53/61/62) were **verified against LIVE** (user-directed) and reconciled pseudo→live: WF-53 admin-alert sentence is unconditional in live (fixed pseudo + deferred-improvement note), WF-53 placeholder fallbacks `—`, WF-61 block-alert copy matched to live wording; WF-62 matched clean. Live untouched.

## Next Action
Start Batch 4 (Phase 2a) = **BMX-P2-WF01 + BMX-P2-WF02** — the first batch that rebuilds LIVE. This is now standard **pseudocode-first** (the Batch-3 pseudo is the agreed design; build live to match it). WF-01 = identity/security-gate rebuild (D1 block-unify, non-text removed, brand-new → WF-21 direct, anomaly routing dropped); WF-02 = router edits (new EXISTING_NON_TEXT→U2 branch, nfm_reply stage guard, WF-20 gated to existing-user text, NEW_USER branch removed). Surface WF-01/WF-02 user-facing copy for verbatim sign-off per the BMX-06 §11 banner BEFORE any n8n write (mostly reuse-existing: WF-02 "email us" deflection). **If any build-time discussion changes the design, update that workflow's `.pseudo` in the SAME batch, then regenerate its `.md` from live + commit** (per [[feedback_pseudo_live_sync_per_batch]]).

## Blockers
- **Commit status (commit-agnostic):** this handoff + the Batch-3 changeset (16 `.pseudo` + `state.md` + `followups.md`) are intended to ship in the **same push**. If you see this file on `main`, Batch 3 is committed+pushed. Reconcile against `state.md` before re-doing any work. (Memory `feedback_pseudo_live_sync_per_batch` is in `~/.claude/`, outside the repo — not in the push.)
- **Sync convention (locked this session):** pseudocode-first is the standard. The reversed "reconcile-pseudo-TO-live" direction applied ONLY to the Phase-0 utilities (WF-53/61/62) that plan-sprint sequenced build-live-first. **Hard safety rule: never overwrite existing live to match pseudo.** For Batches 4–9 the pseudo already exists → forward order, no reversed-order risk (swept all batches; only WF-46 in Batch 8 is a delete, archive its pseudo/md at retirement). Untouched workflows synced at sprint end (Batch 10).
- **pseudo↔md drift-check deferred to Batch 10 (BMX-P5-DRIFT) by design** — the 13 rewrites are intentionally pseudo-ahead-of-live; the 27-workflow drift was accepted + gate opened at sprint start; NO `.last-run` marker written this batch.
- **2 BMX-06 spec divergences to confirm at build:** (1) WF-01 country filter kept at live `{91, 61}` not §5 "non-+91"; (2) WF-23 pre-form non-text → deflection (decision #5 + §11.1) over §4-table "silent." Both noted inline in the respective `.pseudo`.
- **WF-53 userFacing-conditional admin sentence:** open LIVE fix (not pseudo-only) — apply to live + pseudo together in a future batch / when a non-user-facing U1 caller appears. Logged in `followups.md`.
- **Deferred plugin improvements** (flush via `flush-plugin-improvements` at sprint boundary): (a) handoff commit-agnostic phrasing; (b) plan-sprint greenfield-pseudo-first sequencing; (c) fast-enumeration drift-check mode. All in `followups.md`.

## Changed Reference Values
- **No reference-value changes** this batch (no live edits, no new IDs). The 16 authored `.pseudo` files are the only working-dir code-artefact changes; `state.md` + `followups.md` updated.
