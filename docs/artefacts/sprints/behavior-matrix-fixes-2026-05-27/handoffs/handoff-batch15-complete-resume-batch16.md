## Stopping Point
Batch 15 (BMX-R15-WF11 + BMX-R15-WF47) is complete and post-batch regression PASSED (78-edge dep map stable; WF-10→WF-11 `user.name` contract verified via `Build WF-10 Command Envelope`; corpus Postgres sanity clean; only T5 hit is WF-45, already FU-7-DEFERRED). Ready to commit Batch 15 (this handoff is included in that commit).

## Next Action
Re-invoke `build-sprint "behavior-matrix-fixes-2026-05-27"`; it resumes at Batch 16. **BMX-R16-PSEUDO (P2, Batch 16) — Documentation (pseudo only, NO live workflow changes).** Six `.pseudo` edits bringing design docs into convention + copy parity with live (operator directive: live↔pseudo match at go-live): **WF-00** (P1) rewrite `## Summary` Inputs bullet as enumerated typed block (messageId, phoneNumber, phoneNumberFormatted, messageType, messageContent, interactiveLabel, contactName, rawMessage, timestamp, metadata; required vs optional); **WF-01** (Section-1 #7) author structured `## Inputs` block replacing the prose "…etc."; **WF-10** (P3) renumber lettered Step 23a to a full linear step (or fold into Step 23); **WF-23** (P5) "Dr. Chinmay"→"Dr. Chinmay Mujumdar" + remove DRAFT caveat; **WF-41** (P4) remove dated `History:` bullet block; **WF-42** (P4) move DR-10 + SP-03 bullets under a `## Notes` header. One batch item, six files, no live race. Run `pseudo-md-drift-check` after; no `.md` regen needed (no live change). Then Batch 17 (BMX-P5-MATRIX, P0 exit gate) + Batch 18 (BMX-P8-DOCS/PLUGIN, P2 sprint close).

## Blockers
None blocking. Two BMX matrix items this batch (WF-11 change-3 LIST aod, WF-47 change-1 opt-out aod) were both **no-ops — already satisfied in live** (`alwaysOutputData` already present; added SP-02/earlier). Audit drift, caught by build-sprint Step-3 "verify plan target before mutating"; no action needed beyond the notes recorded in state.md. WF-45 T5 string-interp SQL (`Load User Record`, `Set status=payment_pending`) re-confirmed deferred to **FU-7-DEFERRED** (post-MVP). Live UNBLOCK end-to-end deferred to Batch-17 BMX-P5-MATRIX exit gate (users table empty, no test phone). No plugin improvement to flush (passthrough-caller envelope-trace + audit-drift-on-aod patterns already documented).

## Changed Reference Values
WF-11 versionId `b07c260f-6b3c-4030-8ac0-ef7a5a84f1a9`. Backup: `archive/backups/GoTYo0GS2y8qjjkw-2026-05-31-00-37.json`. WF-11 node count 19→18.
