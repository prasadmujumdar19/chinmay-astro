## Stopping Point
Mid **BMX-P5-MATRIX live smoke** (sprint exit gate). This session resolved a two-part design refinement that emerged from S8×F and validated it live; the S8 acceptance subset is now **5 of 7 PASS** (A, B, C from prior session; **E HELP, F reserved-kw** this session). **BUG-06b** (WF-43 `3va0M06kijgyLejf` — off-topic-but-legit messages in the `general_enquiry` pass-through were uncounted → response Gemini now returns JSON `{valid_user_message, response}`, `valid_user_message=false` → U2/WF-61 `off_topic` count + graceful redirect, parse fail-open) and **BUG-06c** (WF-25 `eTV1lUcYrXBg2q2T` — systemic: garbage/abuse branches dead-ended emitting 1 item so EVERY user-replying caller WF-30/31/43 double-messaged → added `Return Nothing` `return []` node fed by the 3 internally-handled terminals so callers `alwaysOutputData=false` skip; D4 active-garbage relay + abuse-no-relay preserved) are both **implemented, validated live, and (with this push) committed**. Test phone **61466927921** is `consultation_closed` (recovered from a threshold block that the BUG-06c repro triggered).

## Next Action
Re-baseline the exec cursor in the test dir (`docs/artefacts/tests/smoke-bmx-s8-optedout-reengage-2026-05-31/.cursors/exec-cursor`) to the live max, then run the **2 remaining S8 cells**: **S8×D REBOOK** (do LAST — lands in `payment_pending`) and **S8×I "Payment Completed" button** (tap an old interactive button). For each: operator sends STOP (→opted_out), then the cell input, then "check" → verify via Postgres MCP + n8n execs > cursor. Both branch BEFORE Gemini so expect clean single-message handling. Then: matrix HTML static-verdict update (`docs/artefacts/reviews/behavior-matrix-2026-05-27/index.html`), S8×G expectation rewrite (opted_out+media re-engages via WF-26, DR-4), and gate close (`rm _active`, lint state.md, registry WIP).

## Blockers
- **Plugin improvements to flush (2)** — apply via `flush-plugin-improvements` before next sprint: (1) `monitor-test-run` — always report the WF-XX + exec-id execution chain on tick verdicts (memory `feedback_report_workflow_chain` written); (2) `build-workflow` — "emit `return []` (0 items) to stop a caller from resuming after a sub-workflow call; a dead-end branch does NOT stop the caller" (new pattern, no prior precedent; BUG-06c). Both logged in `followups.md`.
- **`.md` AS-IS regen pending at sprint end** — WF-01/21/23/45/50/22 + WF-43/30/31 + now **WF-25**. One `generate-workflow-md` pass at gate close.
- **BUG-04 (eager pattern error markers on the Flow form fields)** — still present after the `(...)?` pattern wrap; operator deferred ("move on for now"). Non-blocking cosmetic.
- **WF-30/31 off-topic counting (consistency with D6)** — same uncounted pass-through-Gemini vector exists in the payment-stage handlers; deferred post-MVP (safety-net spec §12.7). Not a blocker.
- This session's full changeset is committed as the same push that carries this handoff — if you're reading this file on `main`, BUG-06b + BUG-06c + the flow JSON swap are pushed.

## Changed Reference Values
- **WF-43 (`3va0M06kijgyLejf`)** live changed: +5 nodes (Off-Topic? / Build U2 Off-Topic Payload / Call U2 (WF-61) / Blocked by U2? / Build Reply Payload), 22→27. `Gemini General Response` now `responseMimeType=application/json`. Backup `archive/backups/3va0M06kijgyLejf-2026-05-31-14-20.json`.
- **WF-25 (`eTV1lUcYrXBg2q2T`)** live changed: +1 node `Return Nothing` (`return []`), 20→21; 3 terminals (`Call WF-50 (Garbage Warning)`, `End — Garbage Blocked (Silent)`, `Call U2 (Abuse)`) rewired into it. Backup `archive/backups/eTV1lUcYrXBg2q2T-2026-05-31-15-24.json`.
- **Flow JSON:** `workflows/flows/collect-personal-details.json` **REMOVED**; `workflows/flows/collect-birth-details.json` **ADDED** (Meta template "Collect Birth Details", Flow ID `1137788551887662`, v7.2 — single `DETAILS_SCREEN`, EmbeddedLink + OptIn + email_address). Memory `project_whatsapp_flow_id` corrected to `1137788551887662` (the 2xx/14xx IDs are dead).
- **Test user 61466927921:** recovered to `consultation_closed` (blocked_* cleared); has a few post-recovery `silent_drop` rows (off_topic ×2, garbage ×1 — test noise, well under threshold 10).
