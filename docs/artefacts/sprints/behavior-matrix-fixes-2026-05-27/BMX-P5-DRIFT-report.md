# BMX-P5-DRIFT — Drift Audit + Missing-Axes Sweep Spec

**Sprint:** behavior-matrix-fixes-2026-05-27 · Batch 10 (Phase 5 · Verify), item BMX-P5-DRIFT
**First generated:** 2026-05-30 · **Restructured:** 2026-05-30T08:17:56Z
**Status of repo:** READ-ONLY audit. No live workflow, `.md`, or `.pseudo` was modified by this audit.

> **Document history (why this report was restructured).** The original Batch-10 audit was run by **11 Sonnet sub-agents** and proved unreliable: during Batch-11 prep the user caught it declaring **WF-30 "✅ correct"** when live plainly shows the same bug WF-30 was supposed to have. The full 31-workflow **drift + caller-contract** sweep was therefore **re-run with Opus** (Section 1 below — authoritative). The Sonnet run's narrative findings (former PARTS A–E) have been **removed from this report** to avoid confusion, because (a) several were wrong, and (b) the rest were never independently verified **and may be incomplete** (Sonnet may have missed instances entirely, the way it missed WF-34). The **Opus** raw output and structured findings are persisted alongside this report (`BMX-P5-DRIFT-opus-reaudit-raw-output.json`, `BMX-P5-DRIFT-opus-reaudit-findings.json`); the **original Sonnet report text** is recoverable from git history (commit `b7a11bf` and earlier) if ever needed.
>
> **This report now has exactly two parts:**
> - **Section 1 — Confirmed drift audit (Opus).** What we swept, the method, and the **confirmed** observations (with ground-truth live checks). This is trustworthy.
> - **Section 2 — Missing-axes sweep specification.** The algorithm + per-workflow checks for the **code-quality / tech-mechanism / data-contract / pseudo-convention** axes that the Opus drift sweep did **not** cover. **It contains NO observations** — those are to be produced **inline, workflow-by-workflow, by the plan-sprint / build-sprint remediation work** and appended to §2.6.

---

# SECTION 1 — CONFIRMED DRIFT AUDIT (Opus re-run, authoritative)

## 1.1 — Why this re-run happened
During Batch-11 prep, a live cross-check of WF-30's `Call WF-25` mapping contradicted the Sonnet audit's "WF-30 ✅ correct" verdict — WF-30 live passes key `messageText` (undefined on the envelope), identical to the WF-43/WF-31 bug the Sonnet run *did* flag. Root cause of the miss: a per-workflow reading error with no structured per-category coverage to catch it. Decision: re-run the full 31-WF drift sweep with Opus.

## 1.2 — Method
- **Fresh data, source-of-truth:** 31 live workflow JSON re-exported from live n8n → `generate-workflow-md.py` → 31 fresh AS-IS `.md` (scratch). 31 `.pseudo` pulled from **GitHub `main`** (confirmed **0 divergence** vs working-dir copies).
- **`.md` fidelity proven before auditing** (so no finding is an artifact of a lossy projection): across 431 nodes — **431/431 node names present**, **431/431 `parameters` blobs reproduced verbatim** (character-for-character), **all connection-edge counts match**, **0 disabled nodes**. The generator omits only `credentials` (not a drift category) and 5 node `notes` annotations (all 5 extracted and handed to the agents).
- **Fan-out:** dynamic Workflow, **11 Opus agents × ~3 workflows each**, applying the **D1–D9** drift taxonomy with two safeguards the Sonnet run lacked: (a) a **structured schema forcing a verdict on every category D1–D9 per workflow** (no silent skips); (b) an explicit **caller-contract cross-check** comparing every `executeWorkflow` call's emitted keys against the callee's pseudo Inputs contract.
- **Scope (important):** this sweep covered **pseudo-vs-md drift + caller-contract only.** It deliberately **excluded** the tech-mechanism / code-quality axis (`onError`, `alwaysOutputData`, re-SELECT, entry-guard completeness, string-SQL, retries) per the pseudo-vs-tech separation rule. That axis is specified in Section 2 and is **not yet audited**.
- **Cost:** 11 agents, ~812K subagent tokens, 202s.

## 1.3 — Status roll-up (31/31, drift + caller-contract axis only)
**22 CLEAN · 2 MINOR · 7 DRIFT.**

| Status | Workflows |
|--------|-----------|
| 🔴 DRIFT | WF-02, WF-22, WF-30, WF-31, WF-33, WF-34, WF-43 |
| ⚠️ MINOR | WF-01, WF-47 |
| ✅ CLEAN | WF-00, WF-10, WF-11, WF-20, WF-21, WF-23, WF-25, WF-26, WF-32, WF-40, WF-41, WF-42, WF-44, WF-45, WF-46, WF-50, WF-51, WF-52, WF-53, WF-60, WF-61, WF-62 |

> A "CLEAN" here means **only** no pseudo-vs-md drift and no caller-contract break. It says **nothing** about the Section-2 axes. Several of these CLEAN workflows may carry real code-quality issues that this sweep did not look for.

## 1.4 — Confirmed findings

### 🔴 LIVE BUGS (functional — fix live)

**1. WF-25 caller mis-key — THREE callers, THREE broken fields each.** *(ground-truth confirmed against live by the main thread)*
- **Callers:** WF-30 (`gGJBY5fJha0Let8I`, payment_pending), WF-31 (`HB8nXudAtk9iXz7C`, payment_submitted), WF-43 (`3va0M06kijgyLejf`, consultation_closed).
- **Callee:** WF-25 (`eTV1lUcYrXBg2q2T`) `Prepare Intent Request` destructures `input.messageContent` (no `messageText` fallback) and reads `userId`/`userStatus`.
- **Envelope truth (verified — WF-01 `Classify & Build Envelope`):** the WF-01 §2.1 canonical envelope (forwarded unchanged by WF-02) carries `phoneNumber`, `messageContent`, and `user.{id, name, status, slack_channel_id, …}`. It has **NO top-level `messageText`, `userId`, or `userStatus`.**
- **The break (identical in all three handlers):**
  - `messageText: ={{ $json.messageText }}` → undefined. **Fix:** drop key `messageText`, add `messageContent: ={{ $json.messageContent }}`.
  - `userId: ={{ $json.userId }}` → undefined. **Fix:** `userId: ={{ ($json.user || {}).id }}`.
  - `userStatus: ={{ $json.userStatus }}` → undefined. **Fix:** `userStatus: ={{ ($json.user || {}).status }}`.
  - (`phoneNumber`, `userName: ($json.user||{}).name`, `slackChannelId: ($json.user||{}).slack_channel_id` are already correct.)
- **Reference (correct) pattern = WF-40** (`du32QBZbSQOjfESe` `Call WF-25`): `user.id`, `messageContent`, `user.status` — the working template to match.
- **Effect:** every free-form user message on the payment_pending / payment_submitted / consultation_closed paths reaches WF-25 with an empty body + undefined user id/status → unreliable classification on all three live existing-user text paths.
- **Fix shape:** identical 3-field fix across 3 workflows = a **Batch Surgical** pass. Order still matters vs the WF-25 entry-guard (fix all three callers → then add WF-25 hard-fail guard, so the new guard never sees a bad caller). Each handler's own `.pseudo` (Step 2/3/8) already specifies the correct contract → live drifted from its own design; the `.pseudo` needs no contract change.

**2. WF-34 (`se82n3MUQ9xE5aEr`) — payment-rejection WhatsApp message broken (the Sonnet run missed this entirely).** *(ground-truth confirmed against live by the main thread)*
- `Prepare Rejection Message` returns `[{ json: { json: { phoneNumber, messageType, interactivePayload } } }]` — **double-nested** under an extra `json` key. `Call WF-50` uses `mappingMode:defineBelow` with empty `value:{}` (passthrough). So WF-50 receives `$json = { json: {…} }`, its guard reads `$json.phoneNumber` = **undefined** → the rejection message to the user does not send correctly.
- **Fix:** remove the extra wrapper — return `[{ json: { phoneNumber, messageType, interactivePayload } }]` (single-nested, matching every other WF-50 caller). *(Note: §2.5 T7 generalizes this check to ALL WF-50/WF-51 callers — there may be more.)*

**3. WF-22 (`dr8QM0m92Ml8MvIh`) — `email_address` always NULL.** *(Opus-flagged with verbatim evidence; not independently live-dumped by main thread — verify at fix time)*
- `Extract Form Data` returns `{phoneNumber, full_name, date_of_birth, time_of_birth, place_of_birth, consent, flow_token, raw_form_data}` — no `email_address`. `Create User Record` UPSERT binds `$json.email_address` → NULL for every submission. **Fix:** parse `email_address` from the Flow `response_json` in `Extract Form Data`.

### 🔴 DRIFT requiring a DECISION

**4. WF-33 (`NcHZedq9ycnAQ9SW`) — admin Slack activation message under-populated.** Live posts a one-line "Payment approved… consultation is now active." Pseudo Step 9 specifies a richer notice incl. user DOB/TOB/Place and an explicit `CLOSE CHAT CONSULT <phone>` operator reminder. DOB/TOB/Place are out-of-core (would need an envelope/SELECT change to render). **Decision:** restore the richer admin notice (and accept the SELECT cost), or accept the one-liner and update the pseudo?

### 🟡 PSEUDO-LAG (live is correct — fix the `.pseudo`, never live)

**5. WF-33 `status='verified'` vs pseudo `'approved'`.** *(ground-truth confirmed: live uses `'verified'` in 4 places; `pending_verification`→`verified` is the canonical payments lifecycle; no `'approved'` literal anywhere.)* Update WF-33.pseudo Step 3 to `'verified'`.
**6. WF-33 Inputs doc:** `Extract Command Data` reads `command`/`subCommand`; pseudo Inputs declares only `commandType`. Document the real WF-11 command payload contract in the pseudo.
**7. WF-01 (`hYGNM97sXvdo1WmI`) MINOR — D9/D8 Inputs shape:** pseudo Inputs is prose ending in "etc." with no required/optional/type enumeration; `Classify & Build Envelope` consumes `messageContentUpper`/`messageId`/`timestamp`/`metadata` not explicitly declared. Author a structured `## Inputs` block. (No runtime impact — all values arrive from WF-00.)

### ⚠️ COSMETIC / COPY
**8. WF-31** — under-review reassurance copy adds Markdown bold + blank lines not in pseudo Step 8 (cosmetic).
**9. WF-47 (MINOR)** — opt-out Slack notice adds a `(phone: <number>)` fragment not in the pseudo Step 4 locked copy.

## 1.5 — Ground-truth confirmations performed (live, by main thread — not agent-only)
| Claim | How verified | Verdict |
|-------|--------------|---------|
| WF-01 envelope has no top-level `messageText`/`userId`/`userStatus` | Dumped `Classify & Build Envelope` jsCode from live WF-01 | ✅ confirmed |
| WF-30/31/43 all read `$json.messageText`/`userId`/`userStatus` | Dumped all four callers' `Call WF-25` value maps side-by-side | ✅ confirmed (WF-40 = correct reference) |
| WF-30 has no upstream node aliasing `messageText` | Traced WF-30 connections + grepped for `messageText` emitters | ✅ confirmed |
| WF-34 double-nested WF-50 payload | Dumped `Prepare Rejection Message` return + `Call WF-50` mappingMode/value | ✅ confirmed |
| WF-33 `verified` is canonical (not `approved`) | Counted `status='…'` literals across all 31 live JSON | ✅ confirmed (`verified` ×4, no `approved`) |
| `.md` projection lossless for drift taxonomy | 431/431 node names + 431/431 param blobs verbatim + edges match + 0 disabled | ✅ confirmed |

## 1.6 — Disposition for plan-sprint (drift axis — trustworthy)
- **Live functional fixes (HIGH):** WF-25 caller mis-key Batch-Surgical across **WF-30 + WF-31 + WF-43** (3 fields each) → then WF-25 entry-guard; **WF-34** double-nest; **WF-22** email parse (verify at fix time).
- **Decision needed:** WF-33 admin-notice richness.
- **Pseudo-lag fixes (pseudo only):** WF-33 `verified` + command/subCommand Inputs; WF-01 Inputs shape.
- **Copy/cosmetic:** WF-31 copy, WF-47 copy.

---

# SECTION 2 — MISSING-AXES SWEEP — SPECIFICATION (no observations yet)

**This section is a spec, not a findings list.** The Opus drift sweep (Section 1) did not look at the code-quality / tech-mechanism / data-contract / pseudo-convention axes. The previous Sonnet attempt at these axes was unreliable and possibly incomplete, so its observations were removed. **The observations table (§2.6) is intentionally empty** — it is to be filled **inline, one workflow at a time, by the plan-sprint / build-sprint remediation work**, applying the algorithm and checks below and ground-truthing each HIGH against live before recording it.

## 2.1 — Why a separate sweep is required (and per-item verification is not enough)
- **False positives** (a listed finding isn't real / is obsolete) — caught by `build-workflow` Step 3 "verify plan target before mutating" at fix time.
- **False negatives** (real issues never listed — the WF-34 class) — **NOT** caught by per-item verification, because there is no item to verify. Only a fresh, systematic per-workflow sweep surfaces these. This is why §2 is a full sweep, not a punch-list.

## 2.2 — Inputs to the sweep (what to load per workflow)
1. AS-IS `.md` — the committed `docs/pseudocode/*.md` were **verified content-fresh vs live on 2026-05-30** (all 31 content-identical to a fresh `generate-workflow-md` run; they pass `assert-md-fresh.sh`). Use them directly — **no re-download or regeneration needed** unless a workflow is edited after this date.
2. The workflow's `.pseudo` (GitHub `main`).
3. `docs/reference/contract-reference.md` §A–§C (the canonical envelope + sub-workflow input contracts) — the authority for entry-guard completeness.
4. `docs/dependency-map.md` (to enumerate every caller/callee for the cross-cutting checks).

## 2.3 — Tech-mechanism / data-contract checks (per workflow)
| ID | Check | Flag when… | Reference |
|----|-------|------------|-----------|
| T1 | **Envelope-first / no re-SELECT** | a leaf workflow re-SELECTs core-envelope fields (`id`/`name`/`phone_number`/`status`) already forwarded by its caller, instead of reading the envelope; keep a SELECT only for genuinely out-of-core fields a guard needs | build-workflow Step 5a.5 |
| T2 | **`alwaysOutputData` hygiene** | a Postgres SELECT lookup/guard whose downstream IF checks empty/non-empty lacks `alwaysOutputData:true` (silent-halt-on-zero-rows) | build-workflow Step 5a.1 |
| T3 | **Silent-swallow of sub-workflow failure** | an `executeWorkflow`/HTTP call with `onError:continueRegularOutput` whose failure leaves the intended side-effect undelivered with no fallback/alert (e.g. returns success while the Slack/WA send silently failed) | build-workflow Step 5a.2 |
| T4 | **Entry-guard completeness** | a strict-envelope utility (WF-25/50/51/53/60/61/62) does not validate every contract-reference §A–§C REQUIRED field and hard-fail on violation (partial guards, `||`-fallbacks instead of throw) | contract-reference §A–§C |
| T5 | **Parameterized vs string-interpolated SQL** | a Postgres `query` interpolates `'{{ $json.x }}'` into the SQL string instead of using `$1` queryReplacement params (injection surface + inconsistency) | build-workflow Step 5b |
| T6 | **Set v3.4 `includeOtherFields`** | a derive-then-passthrough Set node leaves `includeOtherFields` default-false (drops upstream payload); or a contract-emit Set node sets it true (leaks fields) | build-workflow Step 5f.5 |
| T7 | **Payload nesting / double-nest** | a Code node feeding an `executeWorkflow` via empty-`defineBelow` passthrough returns a double-nested `{json:{json:{…}}}` (or otherwise non-flat) shape so the callee's guard reads `undefined` (the **WF-34** class — sweep ALL WF-50/WF-51/sub-workflow callers) | §1.4 finding 2 |
| T8 | **HTTP anti-patterns + sibling parity** | raw-string `jsonBody`; or a replicated HTTP node missing `retryOnFail`/`maxTries`/`onError`/`credentials` parity with its source pattern | build-workflow Step 5f.6 / technical-workflow-review |
| T9 | **queryReplacement comma-string** | a Postgres node passes queryReplacement as a single comma-joined string instead of an array | technical-workflow-review |
| T10 | **Unquoted camelCase SQL alias** | `SELECT col AS userId` (unquoted) silently lowercases → `$json.userId` undefined downstream | build-workflow Step 5b.4a |
| T11 | **Structural hygiene** | disabled nodes, orphaned nodes, typeVersion floor violations, position collisions, IF/executeWorkflow typeVersion mismatch | technical-workflow-review |

## 2.4 — Pseudo-convention checks (per `.pseudo`)
| ID | Check | Flag when… |
|----|-------|------------|
| P1 | **Structured `## Inputs` block** | Inputs are prose / vague ("…, etc.") rather than an enumerated required/optional + types + validity-rules block (discriminated-union variants enumerated) |
| P2 | **Explicit entry-guard line** | a strict-envelope utility's `.pseudo` declares no entry-guard |
| P3 | **Linear numbering / trigger-first** | embedded `(Branch A/B)` labels inside a linear sequence (WF-31), trigger-not-first numbering (WF-11), or lettered sub-steps used inconsistently |
| P4 | **Footer convention** | inconsistent `## Notes` vs bare `---` vs none; stray dated `History:` bullets |
| P5 | **Locked copy sync** | `.pseudo` carries draft "Dr. Chinmay" where live is locked to "Dr. Chinmay Mujumdar" (esp. WF-23) |

## 2.5 — Algorithm (how to run it inline, workflow-by-workflow)
1. Use the committed `docs/pseudocode/*.md` directly — verified content-fresh vs live on 2026-05-30 (§2.2). Only regenerate a workflow's `.md` if it was edited after that date.
2. For each workflow WF-XX (process one at a time; do **not** batch reasoning across workflows):
   a. Load §2.2 inputs.
   b. Run every applicable T1–T11 + P1–P5 check.
   c. For each violation, draft `{wf, check, severity, evidence (verbatim node/field), proposed-fix}`.
   d. **Ground-truth every HIGH against live** (dump the actual node from live JSON) before recording — same discipline that caught the WF-30/WF-34 issues. Mark each row `verified: live | evidence-only`.
   e. Append the rows to §2.6.
3. Cross-cutting passes (run once across the corpus, not per-WF): T7 across all WF-50/WF-51 callers; T1/T4 across all WF-25/WF-50/WF-51/WF-53/WF-60/WF-61/WF-62 callers via `dependency-map.md`.
4. **Reuse the methodology where it already mechanizes a check:** `n8n-whatsapp-methodology:technical-workflow-review` covers T2/T8/T9/T10/T11 mechanically — run it first and fold its tracker findings into §2.6 (marked as its source), so the inline reasoning only has to cover T1/T3/T4/T5/T6/T7 + P1–P5.
5. Do **not** record a fix as done until its `build-workflow` per-item verification passes.

## 2.6 — Observations (TO BE APPENDED by the inline sweep — currently empty)

> **Appended 2026-05-30T10:23:57Z by the missing-axes inline sweep** (Opus, 6 agents × ~5 workflows, 650K subagent tokens, 226s). Method per §2.5: each agent forced an explicit verdict on every T1–T11 / P1–P5 check per workflow (provable coverage, no silent skips); drift (D1–D9) and caller-contract were **excluded** (locked from the Section-1 Opus run); the §1.4 KNOWN findings were excluded from re-report. Ground-truth = the verbatim-live `.md` params (431/431 fresh 2026-05-30), so `verified: md-verbatim`.
>
> **37 net-new findings: 1 HIGH · 22 MED · 14 LOW**, across 19 workflows.
>
> **Two systemic themes dominate (~30 of 37):**
> - **T3 silent-swallow of send/alert failure** — WF-21, **WF-22 (HIGH)**, WF-23, WF-25, WF-45, WF-53, WF-61. A send/alert with `onError:continueRegularOutput` whose failure leaves the side-effect undelivered while the workflow reports success. Needs **one project-level error policy**, not 7 ad-hoc fixes.
> - **T2 silent-halt on zero DB rows** — WF-10, WF-11, WF-31, WF-33, WF-34, WF-45, WF-47, WF-60. A lookup/update matching zero rows emits nothing, so the intended graceful branch never runs. **WF-47 & WF-10 pseudo explicitly mandates the fix; live drifted off it.**
>
> Secondary: T5 string-interpolated SQL (WF-11, WF-31, WF-45) · T9 comma-string queryReplacement (WF-22, WF-32, WF-33) · T11 typeVersion lag (WF-11, WF-22, WF-30, WF-31, WF-43) · pseudo-convention P1/P3/P4/P5 (WF-00, WF-10, WF-11, WF-23, WF-25, WF-31, WF-41, WF-42).
>
> **Triage caveats flagged for the owner (not auto-accepted):** WF-45 T1 re-SELECT removal — design §3.4 deliberately did *not* list WF-45 for cleanup; WF-11 T5/T1 touch the `admin_actions` INSERT, and `admin_actions` is deprecated (TD-NEW-026) — confirm before carefully parameterizing a write slated for removal.

| WF | Check (T#/P#) | Severity | Evidence (verbatim) | Proposed fix | Verified |
|----|---------------|----------|---------------------|--------------|----------|
| WF-00 | P1 | LOW | **WF-00 Inputs line is prose, not an enumerated typed contract** — WF-00.pseudo line 5: '- Inputs: Inbound WhatsApp webhook POST (Meta payload) at path /whatsapp-astro-webhook' — single prose line; no enumerated required/optional fields, types, or validity rules for the parsed payload it forwards.| Rewrite the ## Summary Inputs bullet as an enumerated block listing the parsed-payload fields WF-00 emits downstream (messageId, phoneNumber, phoneNumberFormatted, messageType, messageContent, interactiveLabel, contactName, rawMessage, timestamp, metadata) with types and which are required vs optional. | md-verbatim |
| WF-10 | P3 | LOW | **Lone lettered sub-step (Step 23a) breaks linear pseudo numbering** — WF-10.pseudo line 131: 'Step 23a: Build WF-10 Relay Envelope (Code node, per design.md §2.2) ...' — a lettered sub-step inserted between Step 23 and Step 24 while every other step uses linear integer numbering.| Renumber Step 23a to a full linear step (and shift subsequent steps) per the linear-numbering convention, or fold it into Step 23. | md-verbatim |
| WF-10 | T2 | MED | **Orphan-channel detection can silently halt when no user matches the consult channel** — Node 'Load User Status': {"operation":"executeQuery","query":"SELECT id, status, name, phone_number, slack_channel_id, current_consultation_id FROM chinmay_astro.users WHERE slack_channel_id = $1 LIMIT 1","options":{"queryReplacement":"={{ [$json.channelId] }}"}} — no alwaysOutputData:true error-handling block, yet its downstream 'User Row Exists?' IF tests $('Load User Status').item.json.id for the orphan branch. (WF-10.pseudo Step 17 explicitly specifies 'Postgres, alwaysOutputData=true' — live is missing it.)| Set alwaysOutputData:true on the 'Load User Status' node so a zero-row result still emits one item; the 'User Row Exists?' IF then correctly routes to 'Build Orphan Channel Alert'. | md-verbatim |
| WF-11 | P3 | LOW | **Trigger step is numbered after the entry-guard step (not trigger-first)** — WF-11.pseudo line 24 'Step 1: Validate Inputs (Code node — entry guard ...)' precedes line 34 'Step 2: Start — triggered by WF-10 with the fully classified admin command payload.'| Reorder so the trigger is Step 1 (e.g., 'Step 1: Start — triggered by WF-10') and the Validate Inputs guard is Step 2, matching live wiring (When Executed by Another Workflow -> Validate Inputs) and the trigger-first convention. | md-verbatim |
| WF-11 | T1 | MED | **UNBLOCK path re-SELECTs user id/name already present in the command envelope** — Node 'Lookup Blocked User' SELECTs id, name, phone_number, status — all four are core-envelope fields (§2.2 user object {id, name, phone_number, status, slack_channel_id}); 'Unblock User' INSERT consumes $('Lookup Blocked User').item.json.id and 'Confirm User Unblocked' consumes $('Lookup Blocked User').item.json.name, both available as $json.user.id / $json.user.name.| Drop 'Lookup Blocked User' and read id/name from the envelope user object in 'Unblock User' and 'Confirm User Unblocked'. If a defensive status='blocked' re-check is wanted, it is unnecessary because WF-10 already validated UNBLOCK expects status='blocked'. | md-verbatim |
| WF-11 | T11 | LOW | **Postgres nodes mix typeVersion 2.5 and 2.6 within the same workflow** — Within WF-11: 'Get Active Users' and 'Get Stats' are postgres v2.6, while 'Lookup Blocked User' and 'Unblock User' are postgres v2.5.| Align the two v2.5 Postgres nodes to v2.6 (the floor present in this workflow) when next editing them; verify no parameter-shape regressions after the bump. | md-verbatim |
| WF-11 | T2 | MED | **LIST can produce no admin response when there are no pending/active users** — Node 'Get Active Users' (Postgres) has no alwaysOutputData:true; its only downstream is 'Format List', which contains an explicit empty-state branch ('if (users.length === 0) { ... "No pending payments or active consultations." }') that can only execute if the node emits an item on zero rows.| Set alwaysOutputData:true on 'Get Active Users' so the zero-row case still emits one (empty) item and 'Format List' runs its empty-state branch. | md-verbatim |
| WF-11 | T5 | MED | **UNBLOCK SQL string-interpolates the phone number and user id instead of using query parameters** — Node 'Lookup Blocked User': "=SELECT id, name, phone_number, status FROM chinmay_astro.users WHERE phone_number = '{{ $json.phoneNumber }}' AND status = 'blocked' LIMIT 1"; Node 'Unblock User': "=UPDATE chinmay_astro.users SET status = 'consultation_closed', updated_at = NOW() WHERE phone_number = '{{ $('When Executed by Another Workflow').item.json.phoneNumber }}'; INSERT INTO chinmay_astro.admin_actions (...) VALUES ('{{ $('Lookup Blocked User').item.json.id }}', ...)".| Convert both queries to parameterized form: use $1 placeholders with options.queryReplacement arrays (e.g. {{ [ $json.phoneNumber ] }}), matching the WF-01/WF-10 pattern. | md-verbatim |
| WF-21 | T3 | MED | **WhatsApp welcome/form send failure is silently swallowed with no alert (systemic across WF-21/23/25)** — Nodes 'Call WF-50 (Welcome)', 'Call WF-50 (Service)', 'Call WF-50 (Redirect)' each carry error handling {"onError":"continueRegularOutput"}; on failure flow continues to the U2 loop-guard with no fallback send or admin alert. Same pattern repeats in WF-23 (six 'Call WF-50 (...)' nodes) and WF-25 (Call WF-50 (Garbage Warning) / (Stop Clarifier)).| Either route the WF-50 call's error output to a 'Send Slack admin alert (WA send failed for <phone>)' branch, or keep continueRegularOutput but add an error-output branch that logs/escalates the dropped send. Decide once at the project level since the pattern is shared; if the swallow is intentional (so the U2 loop-guard always runs), document that decision and still add the admin alert on the error output. | md-verbatim |
| WF-22 | T11 | LOW | **Postgres nodes in WF-22 use mismatched typeVersions (v2.4 vs v2.6)** — Node 'Create User Record' is n8n-nodes-base.postgres v2.4; node 'Save Slack Channel ID' is n8n-nodes-base.postgres v2.6 in the same workflow.| Align both Postgres nodes to the highest version already present in the workflow (v2.6) after verifying parameter compatibility, per the typeVersion-floor convention. | md-verbatim |
| WF-22 | T3 | HIGH | **User-record creation failure is swallowed; workflow then builds a Slack channel and sends payment instructions for a non-existent user** — Node 'Create User Record' (postgres) has error handling {"onError":"continueRegularOutput"}; its output feeds 'Prepare WF-52 Payload' (reads $json.id / $json.name) with no IF checking that a row was actually returned. On INSERT failure those fields are undefined and the workflow continues to Ensure Slack Channel Exists (WF-52) and Prepare Payment Instructions.| Add an IF after 'Create User Record' that checks a row was returned (e.g. $json.id is present); on the empty/error branch, send a Slack admin alert ('Form submission for <phone> failed to persist — manual intervention needed') and stop, mirroring the existing WF-52-failure admin-alert branch. Alternatively remove onError:continueRegularOutput so the write failure halts loudly. | md-verbatim |
| WF-22 | T9 | MED | **Slack-channel-save query passes its parameters as one comma-joined string instead of an array** — Node 'Save Slack Channel ID' (postgres v2.6): options.queryReplacement = "={{ $('Ensure Slack Channel Exists (WF-52)').item.json.channelId }}, {{ $now }}, {{ $('Create User Record').item.json.id }}" — a comma-joined string, whereas sibling nodes (e.g. Create User Record) use array form ={{ [ ... ] }}.| Wrap the three expressions in an array: options.queryReplacement = ={{ [ $('Ensure Slack Channel Exists (WF-52)').item.json.channelId, $now, $('Create User Record').item.json.id ] }} so each maps to $1/$2/$3 as an actual list. | md-verbatim |
| WF-23 | P5 | LOW | **.pseudo carries draft consultant name 'Dr. Chinmay' where live copy is locked to 'Dr. Chinmay Mujumdar'** — WF-23.pseudo Steps 5/11/12 use 'Dr. Chinmay' (e.g. 'book your first session with Dr. Chinmay'); live .md nodes 'Build Clarifier Message (Alias)', 'Build Help Message', 'Build Redirect Message' use 'Dr. Chinmay Mujumdar'. The pseudo also self-flags 'Copy is DRAFT (BMX-06 §11.1) — verify verbatim at build.'| Update the WF-23.pseudo copy to the locked wording 'Dr. Chinmay Mujumdar' to match live, and remove the DRAFT caveat once reconciled. | md-verbatim |
| WF-23 | T3 | MED | **Pre-form clarifier/deflection/form-resend sends silently swallowed (same systemic WF-50 pattern as WF-21)** — Nodes 'Call WF-50 (Welcome)', 'Call WF-50 (Service)', 'Call WF-50 (Help)', 'Call WF-50 (Redirect)', 'Call WF-50 (Alias Clarifier)', 'Call WF-50 (Non-Text Deflection)' all carry {"onError":"continueRegularOutput"} with no error-output alert branch.| Same project-level decision as WF-21: add an admin-alert branch on the WF-50 call error output (or document the swallow as intentional and still alert on failure). | md-verbatim |
| WF-25 | P1 | LOW | **WF-25 Inputs block is a bare field list without types/required/validity, unlike sibling leaf docs** — WF-25.pseudo: 'Inputs: { phoneNumber, userId, messageContent, userStatus, userName, slackChannelId }.' — names only, no type/required/validity, vs the §-style Inputs tables in WF-22/WF-23.| Rewrite WF-25's Inputs block as a structured table (Field / Type / Required / Used by) enumerating each field's type and whether it is required, to match the contract-discipline convention and ground the forthcoming entry guard. | md-verbatim |
| WF-25 | T3 | MED | **Stop-clarifier and garbage-warning WhatsApp sends silently swallowed (same systemic WF-50 pattern)** — Nodes 'Call WF-50 (Garbage Warning)', 'Call WF-50 (Stop Clarifier)', 'Call U2 (Garbage)', 'Call U2 (Abuse)' all carry {"onError":"continueRegularOutput"}; 'Garbage Blocked?' then branches on $json.blocked which is undefined if Call U2 (Garbage) failed.| Project-level decision consistent with WF-21/23: alert admin on the WF-50/U2 call error output. At minimum, default $json.blocked to false explicitly and add an alert when the garbage-count call errors so the block safety net is not silently bypassed. | md-verbatim |
| WF-25 | T8 | MED | **Intent-classifier Gemini call lacks retry and timeout that its sibling Gemini nodes have** — Node 'Classify Intent' (httpRequest v4.2): error handling is {"onError":"continueErrorOutput"} only and options is {} (no timeout). Sibling Gemini node 'Generate Service Answer' in WF-21 and WF-23 has {"onError":"continueErrorOutput","retryOnFail":true,"maxTries":3} and "options":{"timeout":10000}.| Add retryOnFail:true, maxTries:3 and options.timeout:10000 to 'Classify Intent' to match the sibling Gemini call pattern, so transient failures retry and the request cannot hang. | md-verbatim |
| WF-30 | T11 | LOW | **Trigger node lags the version used by its sibling payment workflows** — Node 'When Executed by Another Workflow': type n8n-nodes-base.executeWorkflowTrigger (v1) with parameters {} — whereas WF-26/WF-32/WF-33 use (v1.1) with {"inputSource":"passthrough"}.| Bump the trigger to typeVersion 1.1 and set inputSource:'passthrough' to match the WF-26/32/33 sibling pattern (the live floor for this node type in the payment family). | md-verbatim |
| WF-31 | P3 | LOW | **Pseudo mixes embedded Branch A/B labels into a linear step sequence** — WF-31.pseudo lines: '(Branch A — user reply)' before Step 3 and '(Branch B — relay user message into the consult Slack channel)' before Step 9, embedded inside linear Step 1..11 numbering.| Keep the linear numbering and move the branch context into the Step text itself (e.g. 'Step 3 (reply branch): ...') or into the Summary, removing the embedded Branch A/B section headers per the linear-numbering convention. | md-verbatim |
| WF-31 | T11 | LOW | **Trigger node lags the version used by its sibling payment workflows** — Node 'When Executed by Another Workflow': type n8n-nodes-base.executeWorkflowTrigger (v1) with parameters {} — siblings WF-26/WF-32/WF-33 use (v1.1) with {"inputSource":"passthrough"}.| Bump trigger to typeVersion 1.1 with inputSource:'passthrough' to match the live floor in the payment family. | md-verbatim |
| WF-31 | T2 | MED | **Admin relay of a user's message silently stops when the user has no payment row** — Node 'Load Latest Payment': {"operation":"executeQuery","query":"=SELECT created_at FROM chinmay_astro.payments WHERE user_id = ... ORDER BY id DESC LIMIT 1","options":{}} — options has no alwaysOutputData:true. Downstream 'Prepare Admin Relay' reads $('Load Latest Payment').first()?.json and is designed to tolerate a missing row.| Add alwaysOutputData:true to the 'Load Latest Payment' node's options so the node emits an (empty) item on zero rows, allowing 'Prepare Admin Relay' to run and apply its elapsed='?' fallback. | md-verbatim |
| WF-31 | T5 | MED | **Payment lookup builds its SQL by pasting the user id straight into the query string** — Node 'Load Latest Payment': "query":"=SELECT created_at FROM chinmay_astro.payments WHERE user_id = {{ $('When Executed by Another Workflow').item.json.user.id }} ORDER BY id DESC LIMIT 1","options":{} — value interpolated into the string, no queryReplacement/$1.| Rewrite to parameterized form: query 'SELECT created_at FROM chinmay_astro.payments WHERE user_id = $1 ORDER BY id DESC LIMIT 1' with options.queryReplacement = '={{ [$("When Executed by Another Workflow").item.json.user.id] }}'. | md-verbatim |
| WF-32 | T9 | MED | **Payment-record insert passes its five values as one comma-joined string instead of a list** — Node 'Create Payment Record': "options":{"queryReplacement":"={{ $json.user.id }}, {{ 500 }}, {{ \"INR\" }}, {{ \"pending_verification\" }}, {{ \"gpay\" }}", ...} — one comma-joined string for five $1..$5 parameters.| Pass queryReplacement as an array expression: '={{ [$json.user.id, 500, "INR", "pending_verification", "gpay"] }}' so each element maps cleanly to $1..$5 regardless of content. | md-verbatim |
| WF-33 | T2 | MED | **A second APPROVE on an already-verified payment silently does nothing — no consultation, no notice** — Node 'Update Payment Status': "query":"UPDATE chinmay_astro.payments SET status='verified', verified_at=NOW(), verified_by=$1 WHERE user_id=$2 AND status='pending_verification' RETURNING id;","options":{"queryReplacement":"..."} — no alwaysOutputData:true; node feeds 'Update User Status' directly with no empty-row branch.| Add alwaysOutputData:true to 'Update Payment Status' and insert an IF after it that checks whether a payment id was returned; on the empty path, post a Slack notice to the admin channel ('No pending payment to approve for <phone>') instead of silently halting the activation chain. | md-verbatim |
| WF-33 | T9 | MED | **Three approval DB writes pass their parameters as comma-joined strings instead of lists** — Node 'Create Consultation Record' queryReplacement "={{ $(\"When Executed by Another Workflow\").item.json.user.id }}, {{ $(\"Update Payment Status\").item.json.id }}"; node 'Update Payment Status' queryReplacement "={{ $(\"Extract Command Data\").item.json.adminUserId }}, {{ $(\"When Executed by Another Workflow\").item.json.user.id }}"; node 'Update User Consultation Id' queryReplacement "={{ $('Create Consultation Record').item.json.id }}, {{ $('Create Consultation Record').item.json.user_id }}" — all comma-joined strings.| Convert each to an array expression, e.g. 'Create Consultation Record' → '={{ [$("When Executed by Another Workflow").item.json.user.id, $("Update Payment Status").item.json.id] }}', and likewise for the other two nodes, so each element maps to $1/$2 independent of content. | md-verbatim |
| WF-34 | T2 | MED | **Payment rejection silently does nothing if no pending payment row is found** — Node 'Load User by Phone' params: {"operation":"executeQuery","query":"SELECT p.id as payment_id ... WHERE u.phone_number = $1 AND p.status = 'pending_verification' ... LIMIT 1","options":{"queryReplacement":"={{ $json.phoneNumber }}"}} — no alwaysOutputData. Downstream 'Update Payment Record' depends on $json.payment_id; on zero rows the node emits nothing and the entire chain (Update Payment Record -> Reset User Status -> Prepare Rejection Message -> Call WF-50 -> Call WF-51) never executes.| Set alwaysOutputData:true on 'Load User by Phone' and add an explicit guard (IF payment_id present) that, on the empty branch, posts a Slack notice to the admin (e.g. 'No pending payment found to reject for <phone>') instead of silently halting. | md-verbatim |
| WF-41 | P4 | LOW | **Pseudo carries a stray dated History: bullet block** — WF-41.pseudo Notes block contains 'History:' followed by dated bullets '- A prior design carried a whatsapp_to_slack branch ... removed on 2026-05-20 (TD-003 F5)', '- On 2026-05-22 (SP-01) the redundant phone-extraction ...', '- On 2026-05-25 (data-contract Phase 1, sub-14) the Inputs block was updated ...'.| Remove the History: bullet block from the .pseudo; rely on Git history for the change log, matching the convention used in WF-34/WF-40/WF-43 pseudos. | md-verbatim |
| WF-42 | P4 | LOW | **Pseudo folds notes into Summary bullets instead of a dedicated ## Notes section** — WF-42.pseudo has no '## Notes' header; design rules appear as Summary bullets: '- **Design rule (DR-10):** Slack channel is NOT archived ...' and '- **Trust-mode input (SP-03):** Input is pre-validated by WF-10's centralized gate ...', then a bare '---' precedes the Algorithm.| Move the DR-10 and SP-03 bullets under a '## Notes' header to match the WF-34/WF-40/WF-43 footer convention. | md-verbatim |
| WF-43 | T11 | LOW | **Trigger node is on an older typeVersion than its siblings, with no explicit passthrough setting** — Node 'When Executed by Another Workflow' type: n8n-nodes-base.executeWorkflowTrigger (v1) params: {} — compared to sibling WF-34/40/41/42 'When Executed by Another Workflow' which are (v1.1) with {"inputSource":"passthrough"}.| Bump the trigger to executeWorkflowTrigger v1.1 and set params to {"inputSource":"passthrough"} to match the floor used by the other four workflows and make the passthrough behaviour explicit. | md-verbatim |
| WF-45 | T1 | LOW | **Rebook re-SELECTs core user fields its callers already forward** — Node 'Load User Record': SELECT id, name, phone_number, status FROM chinmay_astro.users WHERE phone_number = ... — all four are core-envelope fields; WF-45 Inputs block declares only phoneNumber + optional userId and does not consume the envelope user object.| If routing can rely on the envelope's user.status, read user.{id,name,status} from the trigger envelope and drop the SELECT. (Design §3.4 deliberately did not list WF-45 for cleanup, so confirm with the owner before removing — flagged for completeness.) | md-verbatim |
| WF-45 | T2 | MED | **Rebook lookup can silently halt when the user has no record** — Node 'Load User Record': {"operation":"executeQuery","query":"=SELECT id, name, phone_number, status FROM chinmay_astro.users WHERE phone_number = '{{ $json.phoneNumber }}' LIMIT 1","options":{}} — options has no alwaysOutputData; downstream 'Classify Rebook State' branches on rows[0].json.id presence (if (!row) routeClass='setup').| Set alwaysOutputData:true on 'Load User Record' so the zero-row case propagates an empty item to 'Classify Rebook State', preserving the defensive setup branch. | md-verbatim |
| WF-45 | T3 | MED | **WhatsApp send failures on rebook branches are swallowed** — Nodes 'Call WF-50 (Active)', 'Call WF-50 (Setup)', 'Call WF-50 (Under Review)' each carry error handling {"onError":"continueRegularOutput"}; node 'Send Payment Instructions' has no onError (default fail) — sibling inconsistency.| Either remove continueRegularOutput so a send failure surfaces, or add a fallback/admin-alert branch on the error output. Align all four WF-50 calls to the same error policy. | md-verbatim |
| WF-45 | T5 | MED | **Rebook SQL interpolates phone number into the query string** — Node 'Load User Record' query: "...WHERE phone_number = '{{ $json.phoneNumber }}' LIMIT 1"; node 'Set status=payment_pending' query: "...WHERE phone_number = '{{ $('When Executed by Another Workflow').item.json.phoneNumber }}'".| Convert both to parameterized queries: WHERE phone_number = $1 with options.queryReplacement set to [phoneNumber]. | md-verbatim |
| WF-47 | T2 | MED | **Opt-out missing alwaysOutputData its own design requires — pre-onboarding STOP halts silently** — Node 'Update User Status to opted_out': {"operation":"executeQuery","query":"UPDATE chinmay_astro.users SET status = 'opted_out', ... WHERE phone_number = $1 RETURNING slack_channel_id;","options":{"queryBatching":"independently","queryReplacement":...}} — options has no alwaysOutputData; pseudo Step 3 mandates 'Postgres node: alwaysOutputData=true (SP-02 May 2026)'.| Add alwaysOutputData:true to the 'Update User Status to opted_out' node options so the zero-row pre-onboarding STOP path propagates an empty item downstream and the opt-out confirmation still sends, matching the pseudo design. | md-verbatim |
| WF-53 | T3 | MED | **Admin escalation can vanish silently while the workflow claims it was sent** — Node 'Call WF-51 Admin Alert' (executeWorkflow) error handling: {"onError":"continueRegularOutput"}; followed by 'Halt on Gemini Failure' errorMessage: "Gemini classifier failed; admin alert (and user apology if user-facing) dispatched. Halting..."| Either drop onError:continueRegularOutput on 'Call WF-51 Admin Alert' so a failed escalation surfaces as a failed execution (admin alert is the core purpose — its failure should not be silently swallowed), or add a fallback that re-routes a failed admin alert to a guaranteed channel. At minimum, soften the halt message so it does not assert the alert was dispatched. | md-verbatim |
| WF-60 | T2 | MED | **Message logging silently dies for a not-yet-registered phone instead of returning the intended skip result** — Node 'Lookup User By Phone' (postgres) parameters contain only operation/query/options — no alwaysOutputData:true. Its single downstream is 'Merge Lookup Result' which assumes it always runs ('const lookup = $input.first()?.json;'). Connection: 'Lookup User By Phone' --[main#0]--> 'Merge Lookup Result'.| Set alwaysOutputData:true on the 'Lookup User By Phone' Postgres node so it emits an (empty) item on zero rows, allowing 'Merge Lookup Result' to run, resolve userId=null, and route through 'Has userId?' → 'Skip Log (no userId)' to return the intended {logged:false, reason:'pre_onboarding_user'}. | md-verbatim |
| WF-61 | T3 | MED | **Admin is never told a number was auto-blocked if the alert send fails** — Node 'Call WF-51 Block Alert' (executeWorkflow) error handling: {"onError":"continueRegularOutput"}; downstream 'Return Blocked' Set node unconditionally emits blocked=true regardless of whether the alert posted.| Remove onError:continueRegularOutput on 'Call WF-51 Block Alert' (the auto-block alert is the only audit signal for system-initiated blocks — its failure should surface as a failed execution), or add a fallback alert to a guaranteed channel so an auto-block is never silently invisible to the admin. | md-verbatim |

## 2.7 — Disposition (decided 2026-05-30, operator-confirmed)

Each finding (and the Section-1 findings) routed to one of: **fix this sprint** (behavior-matrix-fixes-2026-05-27 Batches 11–18), or a named **post-MVP backlog** item. Scope decisions D1–D5 + the copy/hygiene calls are recorded in the sprint state.md.

### Fix THIS sprint (Batches 11–18, grouped by workflow)
| Batch | Workflow | Items folded in |
|-------|----------|-----------------|
| 11 | WF-30 | mis-key 3-field fix (§1); trigger v1→v1.1 passthrough (T11) |
| 11 | WF-31 | mis-key (§1); payment-lookup SQL parameterize (T5); trigger bump (T11); under-review copy → keep live, sync pseudo (§1 #8); pseudo Branch-A/B → linear (P3) |
| 11 | WF-43 | mis-key (§1); trigger bump (T11) |
| 12 | WF-25 | entry-guard hard-fail (§1, root cause); classifier retry+timeout (T8, 3B); structured Inputs block (P1) — **hard dep on WF-30/31/43; guard enforces the exact contract those callers now emit** |
| 13 | WF-34 | un-double-nest the rejection WF-50 payload (§1 HIGH) |
| 13 | WF-33 | richer admin activation notice (§1 D2); 3 param-lists → array (T9); pseudo status='verified' + command/subCommand Inputs (§1) |
| 13 | WF-32 | payment-insert param-list → array (T9) |
| 14 | WF-22 | extract `email_address` (§1); Save-Slack-Channel param-list → array (T9); Postgres typeVersion v2.4→v2.6 (T11) |
| 15 | WF-11 | drop `admin_actions` INSERT + the now-pointless re-SELECT (TD-NEW-026 WF-11 step + T1); parameterize UNBLOCK UPDATE (T5); LIST empty-state alwaysOutputData (T2 — designed-empty, not missing-record); Postgres typeVersion align (T11); pseudo trigger-first (P3) |
| 15 | WF-47 | opt-out UPDATE alwaysOutputData (T2 — pre-onboarding STOP ack, pseudo-mandated); opt-out copy → keep live, sync pseudo (§1 #9) |
| 16 | WF-00 | enumerate Inputs block (P1) — pseudo only |
| 16 | WF-01 | structured Inputs block (§1 #7) — pseudo only |
| 16 | WF-10 | renumber Step 23a (P3) — pseudo only |
| 16 | WF-23 | 'Dr. Chinmay' → 'Dr. Chinmay Mujumdar' copy sync (P5) — pseudo only |
| 16 | WF-41 | remove stray History bullets (P4) — pseudo only |
| 16 | WF-42 | move design rules under ## Notes (P4) — pseudo only |

(Batch 17 = behavior-matrix re-verification exit gate; Batch 18 = CLAUDE.md/registry Flow-ID `1408…`→`2260…` correction + plugin-improvement flush. Neither is a §2.6 finding.)

### Deferred to post-MVP backlog
| Findings | Destination |
|----------|-------------|
| T3 silent-swallow of sends/alerts: WF-21, WF-23, WF-25 (sends + garbage-count safety-net), WF-45; admin-alert variants WF-53, WF-61; **WF-22 write-failure HIGH (§1.4 finding 1)** | **TD-NEW-035** (new — application-level error handling) |
| T2 zero-row (record-must-exist cases): WF-10 orphan-channel, WF-31 relay, WF-33 second-APPROVE, WF-34 no-pending-payment, WF-45 rebook, WF-60 pre-onboarding logging | **FU-7-DEFERRED** (DB-lookup hygiene audit) |
| WF-45 T1 re-SELECT (design §3.4 excluded) + WF-45 T5 SQL parameterize | **FU-7-DEFERRED** (WF-45 not otherwise touched this sprint) |
| WF-11 `admin_actions` table DROP + CLAUDE.md schema cleanup | **TD-NEW-026** (post-MVP DB session; the WF-11 INSERT removal itself lands this sprint, leaving zero live writers) |

> Note: TD-NEW-034 (the separate 23 `.pseudo`-only drift items from the 2026-05-24 eval) is unrelated to this sweep and stays as-is. All pseudo findings *from this sweep* are fixed this sprint (Batch 16 + ride-alongs) per the operator's "live↔pseudo match at go-live" directive.
