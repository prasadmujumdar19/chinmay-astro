## Stopping Point

Sprint `contract-first-sub-workflow-calls-design` parked at Phase C user-review gate. All 14 v2 recon JSONs (Sonnet, strict-schema) are written to `recon/` and the HTML comparison `phase-c-comparison.html` is built. The `_active` marker has been replaced by `_deferred`. Phase D pseudo writes have not started.

## Next Action

Open `docs/artefacts/sprints/contract-first-sub-workflow-calls-design/phase-c-comparison.html` in a browser, then give the verdict for each WF (approve all / approve with edits / defer specific WFs as needs-decision). Specific calls required:
- **WF-25** — `messageContent` (canonical, WF-25 reads this) vs `messageText` (passed by WF-30, WF-44, WF-23). Currently 3 callers silently send an empty Gemini prompt. Decide: (a) Sprint-1 doc canonical only + log Sprint 2+ caller renames, (b) defensive accept-both in WF-25, or (c) escalate as new TD bug separate from this sprint.
- **WF-60** — `Done` node hardcodes `logged: true` on all 3 terminal branches, masking the `logged: false` skip-path. Out of Sprint-1 scope (Decision #14: doc-only) — needs new TD entry for Sprint 2+.
- **WF-52, WF-50, WF-60** — confirm technical-duplicate canonical (camelCase) and log Sprint 2+ caller renames to `followups.md`.

Once verdicts are in, run `build-sprint @docs/artefacts/specs/contract-first-sub-workflow-calls-design.md` to resume Phase D pseudo writes for approved WFs.

## Blockers

- Phase A bundle data lives in `/tmp/claude-scratch/cfm-data/` — will be deleted at session cleanup. **If resumed in a new session, Phase D pseudo writes only need the recon JSONs at `recon/`, NOT the cfm-data bundles, so this is safe.** If Phase B v3 ever becomes necessary (unlikely), the bundles must be regenerated by re-running the gather script.
- **Plugin improvement (this session):** Subagent-driven task planning + execution methodology — being written as a new plugin skill `subagent-driven-task-execution` in the same session that produced this handoff. The new skill captures: foreground vs background dispatch primitive, permission-inheritance pitfall, strict-schema enforcement, model selection (Haiku for shape, Sonnet for schema discipline), and the parent-writes-from-subagent-result pattern. Will be committed to the plugin repo before session close.

## Changed Reference Values

- `.claude/settings.local.json` — added two `Write` permissions for `/tmp/claude-scratch/cfm-recon/**` and `/private/tmp/claude-scratch/cfm-recon/**` (subagent direct-write surface). These were needed for the v2 Sonnet dispatch and are session-scoped — safe to keep or remove later.
- Sprint state.md — `_active` marker removed, `_deferred` marker created; resume path documented in `resume_instructions` field.
