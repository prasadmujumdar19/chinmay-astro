# WF-45 Rebook Handler

- **ID:** `MUG7rPgSHc7UtAE9`
- **Active:** true
- **Nodes:** 4

## Nodes

### Load User Record
- type: `n8n-nodes-base.postgres` (v2.5)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"=SELECT id, name, phone_number FROM chinmay_astro.users WHERE phone_number = '{{ $json.phoneNumber }}' LIMIT 1"}
```

### Send Payment Instructions
- type: `n8n-nodes-base.executeWorkflow` (v1)
- parameters:

```json
{"workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"id"},"workflowInputs":{"mappingMode":"defineBelow","value":{"phoneNumber":"={{ $('When Executed by Another Workflow').item.json.phoneNumber }}","messageType":"interactive","interactivePayload":"={{ { type: 'button', body: { text: 'Welcome back, ' + ($('When Executed by Another Workflow').item.json.name || $('Load User Record').item.json.name) + '! 🙏\\n\\nYour birth details are already on file, so we can get started right away.\\n\\n*Payment Information:*\\nAmount: ₹500\\n\\nPlease send ₹500 via GPay / PhonePe / any UPI app to: +91-9653240263 (Chinmay Mujumdar). Once done, tap the button below:' }, action: { buttons: [ { type: 'reply', reply: { id: 'payment_completed', title: 'Payment Completed' } } ] } } }}"},"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":false},"options":{}}
```

### Set status=payment_pending
- type: `n8n-nodes-base.postgres` (v2.5)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"=UPDATE chinmay_astro.users SET status = 'payment_pending', stage = NULL, updated_at = NOW() WHERE phone_number = '{{ $('When Executed by Another Workflow').item.json.phoneNumber }}'"}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1)
- parameters:

```json
{}
```


## Connections

- `Load User Record` --[main#0]--> `Set status=payment_pending` (main, input #0)
- `Set status=payment_pending` --[main#0]--> `Send Payment Instructions` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Load User Record` (main, input #0)
