# WF-46 User Blocker

- **ID:** `UV62An60fzflU0uD`
- **Active:** true
- **Nodes:** 6

## Nodes

### Archive Slack Channel
- type: `n8n-nodes-base.slack` (v2.3)
- onError: `continueErrorOutput`
- credentials: slackApi=Slack - Chinmay Astro
- parameters:

```json
{"resource":"channel","operation":"archive","channelId":{"__rl":true,"value":"={{ $json.slack_channel_id }}","mode":"id"}}
```

### Get User Slack Channel
- type: `n8n-nodes-base.postgres` (v2.5)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"=SELECT slack_channel_id FROM chinmay_astro.users WHERE phone_number = '{{ $(\"When Executed by Another Workflow\").item.json.phoneNumber }}'","options":{}}
```

### Load User by Phone
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"SELECT * FROM chinmay_astro.users \nWHERE phone_number = $1\nLIMIT 1;","options":{"queryReplacement":"={{ $json.phoneNumber }}"}}
```

### Send a message
- type: `n8n-nodes-base.slack` (v2.3)
- credentials: slackApi=Slack - Chinmay Astro
- parameters:

```json
{"select":"channel","channelId":{"__rl":true,"value":"C0A5B0ZE81E","mode":"list","cachedResultName":"chinmay-admin-commands"},"text":"=Name: {{ $('Load User by Phone').item.json.name || 'Unknown' }} Phone: +{{ $('Load User by Phone').item.json.phone_number }} Reason: {{ $json.blockReason || 'Blocked by admin' }} Blocked by: {{ $json.adminUserId }}  This user will no longer be able to send messages.","otherOptions":{}}
```

### Update User to Blocked Status
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users\nSET status = 'blocked',\n    blocked_at = NOW(),\n    blocked_by = 'admin',\n    blocked_reason = 'Blocked by admin',\n    updated_at = NOW()\nWHERE id = $1\nRETURNING *","options":{"queryReplacement":"={{ $('Load User by Phone').item.json.id }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```


## Connections

- `Get User Slack Channel` --[main#0]--> `Archive Slack Channel` (main, input #0)
- `Load User by Phone` --[main#0]--> `Update User to Blocked Status` (main, input #0)
- `Send a message` --[main#0]--> `Get User Slack Channel` (main, input #0)
- `Update User to Blocked Status` --[main#0]--> `Send a message` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Load User by Phone` (main, input #0)
