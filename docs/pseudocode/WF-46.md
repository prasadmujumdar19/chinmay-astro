---
wf_id: WF-46
n8n_uuid: UV62An60fzflU0uD
active: true
node_count: 4
live_updated_at: 2026-05-26T02:47:10.168Z
generated_at: 2026-05-30T06:17:22Z
---

# WF-46 User Blocker

- **ID:** `UV62An60fzflU0uD`
- **Active:** true
- **Nodes:** 4

## Nodes

### Call WF-51 Notify Admin
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Prepare WF-51 Payload (Notify Admin Blocked)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('When Executed by Another Workflow').item.json.user;\nconst trigger = $('When Executed by Another Workflow').first().json;\nconst channelId = trigger.channelId || user.slack_channel_id;\nconst reason = (trigger.reason && trigger.reason.toString().trim()) ? trigger.reason : 'Not provided';\n\nreturn [{\n  json: {\n    channelId: channelId,\n    messageText: `\ud83d\udeab User blocked: ${user.name} (${user.phone_number}). Reason: ${reason}. Status set to 'blocked'.`\n  }\n}];"}
```

### Update User to Blocked Status
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users\nSET status = 'blocked',\n    blocked_at = NOW(),\n    blocked_by = 'admin',\n    blocked_reason = 'Blocked by admin',\n    updated_at = NOW()\nWHERE id = $1\nRETURNING *","options":{"queryReplacement":"={{ $(\"When Executed by Another Workflow\").item.json.user.id }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

## Connections

- `Prepare WF-51 Payload (Notify Admin Blocked)` --[main#0]--> `Call WF-51 Notify Admin` (main, input #0)
- `Update User to Blocked Status` --[main#0]--> `Prepare WF-51 Payload (Notify Admin Blocked)` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Update User to Blocked Status` (main, input #0)
