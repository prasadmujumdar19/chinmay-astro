# WF-42 Consultation Closer

- **ID:** `fx70vqyJtRdF2DgR`
- **Active:** true
- **Nodes:** 9

## Nodes

### Call WF-50 Send Feedback
- type: `n8n-nodes-base.executeWorkflow` (v1.3)
- parameters:

```json
{"workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9","cachedResultName":"WF-50 Send WhatsApp"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"options":{}}
```

### Close Consultation Record
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.consultations \nSET \n  status = 'closed',\n  ended_at = NOW(),\n  closed_by = 'admin'\nWHERE user_id = $1\n  AND status = 'active'\nRETURNING *;","options":{"queryReplacement":"={{ $('Load User by Phone').item.json.id }}"}}
```

### Load User by Phone
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"SELECT * FROM chinmay_astro.users \nWHERE phone_number = $1\nLIMIT 1;","options":{"queryReplacement":"={{ $json.phoneNumber }}"}}
```

### Notify Admin Wrong State
- type: `n8n-nodes-base.slack` (v2.2)
- credentials: slackOAuth2Api=Slack account
- parameters:

```json
{"authentication":"oAuth2","resource":"message","operation":"post","select":"channel","channelId":{"__rl":true,"value":"={{ $json.slack_channel_id }}","mode":"id"},"text":"={{ 'CLOSE failed — user ' + $json.wa_id + ' is not in consultation_active state (current: `' + $json.status + '`). No changes made.' }}","otherOptions":{}}
```

### Notify Admin in Slack
- type: `n8n-nodes-base.slack` (v2.3)
- credentials: slackApi=Slack - Chinmay Astro
- parameters:

```json
{"select":"channel","channelId":{"__rl":true,"value":"={{ $('When Executed by Another Workflow').item.json.channelName }}","mode":"name"},"text":"=✅ *Consultation Closed*\n\n*User:* {{ $('Load User by Phone').item.json.name }}\n*Phone:* {{ $('Load User by Phone').item.json.phone_number }}\n\nFeedback request sent to user.","otherOptions":{}}
```

### Prepare Feedback Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Load User by Phone').first().json;\n\nreturn [{\n  json: {\n    phoneNumber: user.phone_number,\n    messageType: 'interactive',\n    interactivePayload: {\n      type: 'button',\n      body: {\n        text: '✨ Your consultation with Chinmay is complete, ' + user.name + '! We hope it was insightful.\\n\\nWould you like to share your experience or book another session?'\n      },\n      action: {\n        buttons: [\n          { type: 'reply', reply: { id: 'btn_feedback', title: 'Leave Feedback' } },\n          { type: 'reply', reply: { id: 'btn_rebook', title: 'Book Again' } }\n        ]\n      }\n    }\n  }\n}];"}
```

### Update User Status
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users \nSET \n  status = 'consultation_closed',\n  current_consultation_id = NULL,\n  total_consultations = total_consultations + 1,\n  updated_at = NOW()\nWHERE id = $1\nRETURNING *;","options":{"queryReplacement":"={{ $('Load User by Phone').item.json.id }}"}}
```

### User in Correct State?
- type: `n8n-nodes-base.if` (v2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":true,"leftValue":"","typeValidation":"strict"},"conditions":[{"id":"close-guard-1","leftValue":"={{ $json.status }}","rightValue":"consultation_active","operator":{"type":"string","operation":"equals"}}],"combinator":"and"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```


## Connections

- `Call WF-50 Send Feedback` --[main#0]--> `Notify Admin in Slack` (main, input #0)
- `Close Consultation Record` --[main#0]--> `Update User Status` (main, input #0)
- `Load User by Phone` --[main#0]--> `User in Correct State?` (main, input #0)
- `Prepare Feedback Message` --[main#0]--> `Call WF-50 Send Feedback` (main, input #0)
- `Update User Status` --[main#0]--> `Prepare Feedback Message` (main, input #0)
- `User in Correct State?` --[main#0]--> `Close Consultation Record` (main, input #0)
- `User in Correct State?` --[main#1]--> `Notify Admin Wrong State` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Load User by Phone` (main, input #0)
