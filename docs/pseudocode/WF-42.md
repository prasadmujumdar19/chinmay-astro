---
wf_id: WF-42
n8n_uuid: fx70vqyJtRdF2DgR
active: true
node_count: 7
live_updated_at: 2026-05-26T04:41:50.117Z
generated_at: 2026-05-26T04:46:11Z
---

# WF-42 Consultation Closer

- **ID:** `fx70vqyJtRdF2DgR`
- **Active:** true
- **Nodes:** 7

## Nodes

### Call WF-50 Send Feedback
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Close Consultation Record
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.consultations \nSET \n  status = 'closed',\n  ended_at = NOW(),\n  closed_by = 'admin'\nWHERE user_id = $1\n  AND status = 'active'\nRETURNING *;","options":{"queryReplacement":"={{ $(\"When Executed by Another Workflow\").item.json.user.id }}"}}
```

### Notify Admin in Slack
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Prepare Feedback Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('When Executed by Another Workflow').item.json.user;\n\nreturn [{\n  json: {\n    phoneNumber: user.phone_number,\n    messageType: 'interactive',\n    interactivePayload: {\n      type: 'button',\n      body: {\n        text: '\u2728 Your consultation with Chinmay is complete, ' + user.name + '! We hope it was insightful.\\n\\nWould you like to share your experience or book another session?\\n\\nIn the meantime, you can ask any general questions here, or email chinmay_astro@gmail.com if you need anything we can\\'t help with right now.'\n      },\n      action: {\n        buttons: [\n          { type: 'reply', reply: { id: 'btn_feedback', title: 'Leave Feedback' } },\n          { type: 'reply', reply: { id: 'btn_rebook', title: 'Book Again' } },\n          { type: 'reply', reply: { id: 'btn_done', title: 'Done, thanks' } }\n        ]\n      }\n    }\n  }\n}];"}
```

### Prepare WF-51 Payload (Notify Admin Closed)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('When Executed by Another Workflow').item.json.user;\n\nreturn [{\n  json: {\n    channelId: user.slack_channel_id,\n    messageText: `\u2705 Consultation closed for ${user.name} (${user.phone_number}). Feedback prompt sent via WhatsApp; channel kept open for future rebook.`\n  }\n}];"}
```

### Update User Status
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users \nSET \n  status = 'consultation_closed',\n  current_consultation_id = NULL,\n  total_consultations = total_consultations + 1,\n  updated_at = NOW()\nWHERE id = $1\nRETURNING *;","options":{"queryReplacement":"={{ $(\"When Executed by Another Workflow\").item.json.user.id }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

## Connections

- `Call WF-50 Send Feedback` --[main#0]--> `Prepare WF-51 Payload (Notify Admin Closed)` (main, input #0)
- `Close Consultation Record` --[main#0]--> `Update User Status` (main, input #0)
- `Prepare Feedback Message` --[main#0]--> `Call WF-50 Send Feedback` (main, input #0)
- `Prepare WF-51 Payload (Notify Admin Closed)` --[main#0]--> `Notify Admin in Slack` (main, input #0)
- `Update User Status` --[main#0]--> `Prepare Feedback Message` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Close Consultation Record` (main, input #0)
