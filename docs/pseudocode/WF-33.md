---
wf_id: WF-33
n8n_uuid: NcHZedq9ycnAQ9SW
active: true
node_count: 11
live_updated_at: 2026-05-23T07:13:30.222Z
generated_at: 2026-05-24T16:53:39Z
---

# WF-33 Payment Approval Processor

- **ID:** `NcHZedq9ycnAQ9SW`
- **Active:** true
- **Nodes:** 11

## Nodes

### Call WF-50 Notify User
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-51 Notify Admin in Channel
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Create Consultation Record
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"INSERT INTO chinmay_astro.consultations (\n  user_id,\n  payment_id,\n  status,\n  started_at\n)\nVALUES ($1, $2, 'active', NOW())\nRETURNING *;","options":{"queryReplacement":"={{ $('Load User by Phone').item.json.id }}, {{ $('Update Payment Status').item.json.id }}"}}
```

### Extract Command Data
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"// Input from WF-11 (Command Parser) will contain:\n// - command: \"APPROVE\"\n// - subCommand: \"PAYMENT\"\n// - phoneNumber: \"61466927921\"\n// - adminUserId: \"U0A4A6X857D\"\n\nconst input = $input.first().json;\n\nreturn [{\n  json: {\n    phoneNumber: input.phoneNumber,\n    adminUserId: input.channelId,\n    command: input.command,\n    subCommand: input.subCommand\n  }\n}];"}
```

### Load User by Phone
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"SELECT * FROM chinmay_astro.users \nWHERE phone_number = $1\nLIMIT 1;","options":{"queryReplacement":"={{ $json.phoneNumber }}"}}
```

### Prepare User Activation Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Load User by Phone').first().json;\n\nreturn [{\n  json: {\n    phoneNumber: user.phone_number,\n    messageType: \"template\",\n    templateName: \"consultation_activated\",\n    templateParams: [user.name]\n  }\n}];"}
```

### Prepare WF-51 Payload (Notify Admin)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Load User by Phone').first().json;\n\nreturn [{\n  json: {\n    channelId: user.slack_channel_id,\n    messageText: `\u2705 Payment approved for ${user.name} (${user.phone_number}). User notified via WhatsApp; consultation is now active.`\n  }\n}];"}
```

### Update Payment Status
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.payments \nSET \n  status = 'verified',\n  verified_at = NOW(),\n  verified_by = $1\nWHERE user_id = $2 \n  AND status = 'pending_verification'\nRETURNING id;","options":{"queryReplacement":"={{ $('Extract Command Data').item.json.adminUserId }}, {{ $json.id }}"}}
```

### Update User Consultation Id
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users \nSET \n  current_consultation_id = $1,\n  updated_at = NOW()\nWHERE id = $2\nRETURNING *;","options":{"queryReplacement":"={{ $('Create Consultation Record').item.json.id }}, {{ $('Create Consultation Record').item.json.user_id }}"}}
```

### Update User Status
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users \nSET \n  status = 'consultation_active',\n  updated_at = NOW()\nWHERE id = $1\nRETURNING *;","options":{"queryReplacement":"={{ $('Load User by Phone').item.json.id }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

## Connections

- `Call WF-50 Notify User` --[main#0]--> `Prepare WF-51 Payload (Notify Admin)` (main, input #0)
- `Create Consultation Record` --[main#0]--> `Update User Consultation Id` (main, input #0)
- `Extract Command Data` --[main#0]--> `Load User by Phone` (main, input #0)
- `Load User by Phone` --[main#0]--> `Update Payment Status` (main, input #0)
- `Prepare User Activation Message` --[main#0]--> `Call WF-50 Notify User` (main, input #0)
- `Prepare WF-51 Payload (Notify Admin)` --[main#0]--> `Call WF-51 Notify Admin in Channel` (main, input #0)
- `Update Payment Status` --[main#0]--> `Update User Status` (main, input #0)
- `Update User Consultation Id` --[main#0]--> `Prepare User Activation Message` (main, input #0)
- `Update User Status` --[main#0]--> `Create Consultation Record` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Extract Command Data` (main, input #0)
