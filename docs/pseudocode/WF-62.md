---
wf_id: WF-62
n8n_uuid: tJknCwk2PzLpEwTX
active: true
node_count: 7
live_updated_at: 2026-05-29T12:58:19.312Z
generated_at: 2026-05-30T06:17:22Z
---

# WF-62 U3 New-Contact Intent Classifier

- **ID:** `tJknCwk2PzLpEwTX`
- **Active:** true
- **Nodes:** 7

## Nodes

### Build Gemini Request
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const input = $input.first().json;\nconst stage = input.stage;\nconst text = input.text;\nconst prompt = `You are an intent classifier for Chinmay Astro, a Vedic astrology consultation service on WhatsApp.\nClassify the user's message into EXACTLY ONE bucket. Output ONLY strict JSON:\n{\"bucket\": \"<bucket>\", \"confidence\": <number 0.0-1.0>}.\n\nStage = ${stage}:\n- \"new\": never contacted us before; no record.\n- \"pre_form\": we already sent this person the onboarding form; they have not completed it yet.\n\nBuckets:\n- greeting: hello/greeting or polite small talk (\"hi\", \"namaste\", \"how are you\").\n- wants_consultation: wants a reading/consultation or interest in booking (\"can you help me\", \"I want my kundli read\", \"I heard about your service from a friend\").\n- service_related_question: a genuine question ABOUT the service, astrology, fees, process, or Dr. Chinmay (\"what is Vedic astrology?\", \"how much does it cost?\", \"how does this work?\").\n- HELP: asking what to do / how to proceed (\"help\", \"what do I do\", \"how do I start\").\n- unrelated: a coherent human message NOT about our service and NOT abusive (\"are you hiring?\", \"wrong number\", an unrelated topic).\n- garbage: gibberish, random characters, tests, meaningless content (\"asdfgh\", \"1234\", \".\").\n- stop_intent: wants to stop/opt out/unsubscribe in natural language (\"leave me alone\", \"don't message me\", \"remove me\").\n- malicious: threats, scams, manipulation/jailbreak attempts, or harmful intent.\n- abusive: insults, harassment, hostile or aggressive language.\n- inappropriate: sexual, obscene, or otherwise inappropriate content.\n\nRules:\n- Pick the SINGLE best bucket. If a message both greets and asks (\"hi, what is astrology?\"), prefer the higher-value intent (service_related_question or wants_consultation over greeting).\n- The literal words \"STOP\" and \"REBOOK\" are handled upstream and will not reach you - do not special-case.\n- If genuinely unsure or ambiguous, set LOW confidence (< 0.5); the caller treats low confidence as the fail-open (unrelated) path.\n- Messages may be English, Hindi, Marathi, or Hinglish - classify by meaning regardless of language.\n- Output ONLY the JSON. No prose, no markdown.\n\nUser message:\n\"\"\"${text}\"\"\"`;\nconst geminiBody = JSON.stringify({\n  contents: [{ parts: [{ text: prompt }] }],\n  generationConfig: { temperature: 0, maxOutputTokens: 60, responseMimeType: \"application/json\" }\n});\nreturn [{ json: { ...input, geminiPrompt: prompt, geminiBody } }];"}
```

### Build U1 Payload
- type: `n8n-nodes-base.set` (v3.4)
- parameters:

```json
{"assignments":{"assignments":[{"id":"3c841f94-39ac-469d-b857-5ca5a668d4aa","name":"phoneNumber","value":"={{ $('Entry Guard').first().json.phoneNumber }}","type":"string"},{"id":"6ecca928-4c8c-4a0e-8348-5a2538afbd67","name":"userFacing","value":"={{ true }}","type":"boolean"},{"id":"6c462327-f09b-4ea0-ab6a-87b21ed9be40","name":"consultChannelId","value":"={{ $('Entry Guard').first().json.consultChannelId }}","type":"string"},{"id":"b3c57b60-ce2a-4e03-acad-818bd3a0655e","name":"context","value":"={{ { source: 'U3', userMessage: $('Entry Guard').first().json.text, errorDetail: 'The automated message classifier could not process this message.' } }}","type":"object"}]},"includeOtherFields":false,"options":{}}
```

### Call U1 (Gemini Error)
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"ONzUJ1Lj9hIbUYT0","mode":"list","cachedResultUrl":"/workflow/ONzUJ1Lj9hIbUYT0","cachedResultName":"WF-53 U1 Gemini Error Handler"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Classify Intent
- type: `n8n-nodes-base.httpRequest` (v4.2)
- parameters:

```json
{"method":"POST","url":"https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-lite:generateContent","sendHeaders":true,"headerParameters":{"parameters":[{"name":"Content-Type","value":"application/json"}]},"sendBody":true,"specifyBody":"json","jsonBody":"={{ $json.geminiBody }}","options":{"timeout":10000},"authentication":"predefinedCredentialType","nodeCredentialType":"googlePalmApi"}
```

- error handling:

```json
{"onError":"continueErrorOutput","retryOnFail":true,"maxTries":3,"waitBetweenTries":1000}
```

### Entry Guard
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"// U3 New-Contact Intent Classifier - strict envelope entry guard. Hard-fail on contract violation.\nconst input = $input.first().json;\nconst e164 = /^\\+?[1-9]\\d{6,14}$/;\nif (typeof input.phoneNumber !== 'string' || !e164.test(input.phoneNumber)) {\n  throw new Error('U3 entry-guard: phoneNumber missing or not E.164: ' + JSON.stringify(input.phoneNumber));\n}\nif (typeof input.text !== 'string' || input.text.trim() === '') {\n  throw new Error('U3 entry-guard: text missing or empty');\n}\nif (input.stage !== 'new' && input.stage !== 'pre_form') {\n  throw new Error('U3 entry-guard: stage must be \"new\" or \"pre_form\": ' + JSON.stringify(input.stage));\n}\nconst cid = input.consultChannelId;\nif (cid !== undefined && cid !== null && cid !== '' && !/^[CDG][A-Z0-9]+$/.test(cid)) {\n  throw new Error('U3 entry-guard: consultChannelId not a valid Slack channel id: ' + JSON.stringify(cid));\n}\nreturn [{ json: {\n  phoneNumber: input.phoneNumber,\n  text: input.text,\n  stage: input.stage,\n  consultChannelId: (cid === undefined || cid === '' ? null : cid)\n} }];"}
```

### Execute Workflow Trigger
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

### Parse Classification
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const geminiResp = $input.first().json;\nconst VALID = ['greeting','wants_consultation','service_related_question','HELP','unrelated','garbage','stop_intent','malicious','abusive','inappropriate'];\nlet bucket = 'unrelated';\nlet confidence = 0;\ntry {\n  let raw = (geminiResp.candidates?.[0]?.content?.parts?.[0]?.text || '').trim();\n  raw = raw.replace(/^```(?:json)?/i, '').replace(/```$/, '').trim();\n  const parsed = JSON.parse(raw);\n  if (parsed && VALID.includes(parsed.bucket) && typeof parsed.confidence === 'number') {\n    bucket = parsed.bucket;\n    confidence = parsed.confidence;\n  }\n} catch (e) { /* malformed -> safe fail-open default unrelated/0 (caller treats <0.5 as unrelated) */ }\nreturn [{ json: { bucket, confidence } }];"}
```

## Connections

- `Build Gemini Request` --[main#0]--> `Classify Intent` (main, input #0)
- `Build U1 Payload` --[main#0]--> `Call U1 (Gemini Error)` (main, input #0)
- `Classify Intent` --[main#0]--> `Parse Classification` (main, input #0)
- `Classify Intent` --[main#1]--> `Build U1 Payload` (main, input #0)
- `Entry Guard` --[main#0]--> `Build Gemini Request` (main, input #0)
- `Execute Workflow Trigger` --[main#0]--> `Entry Guard` (main, input #0)
