# WF-34 Payment Rejection Processor

- **ID:** `se82n3MUQ9xE5aEr`
- **Active:** true
- **Nodes:** 14

## Nodes

### Call WF-50 WhatsApp Sender
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-51 Notify Admin Rejected
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-51 Notify Admin User Not Found
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-51 Notify Admin Wrong State
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Load User by Phone
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"SELECT u.*, p.id as payment_id\nFROM chinmay_astro.users u\nLEFT JOIN chinmay_astro.payments p ON p.user_id = u.id \n  AND p.status = 'pending_verification'\nWHERE u.phone_number = $1\nORDER BY p.created_at DESC\nLIMIT 1","options":{"queryReplacement":"={{ $json.phoneNumber }}"}}
```

### Prepare Rejection Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const userData = $('Load User by Phone').item.json;\n\nreturn {\n  json: {\n    phoneNumber: userData.phone_number,\n    messageType: \"interactive\",\n    interactivePayload: {\n      type: \"button\",\n      body: {\n        text: \"We couldn't verify your payment. Please check the details and try again.\\n\\nPayment Instructions:\\nAmount: \u20b9500\\nPlease send \u20b9500 via GPay / PhonePe / any UPI app to +91-9653240263 (Chinmay Mujumdar)\\n\\nAfter payment, tap the button below.\"\n      },\n      action: {\n        buttons: [\n          {\n            type: \"reply\",\n            reply: {\n              id: \"payment_completed\",\n              title: \"Payment Completed \u2713\"\n            }\n          }\n        ]\n      }\n    }\n  }\n};"}
```

### Prepare WF-51 Payload (Notify Admin Rejected)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Load User by Phone').first().json;\n\nreturn [{\n  json: {\n    channelId: user.slack_channel_id,\n    messageText: `\ud83d\udd34 Payment rejection processed for ${user.name} (${user.phone_number}). User has been asked to retry with correct payment details.`\n  }\n}];"}
```

### Prepare WF-51 Payload (User Not Found)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const trigger = $('When Executed by Another Workflow').first().json;\n\nreturn [{\n  json: {\n    channelId: trigger.channelId,\n    messageText: `\u26a0\ufe0f REJECT ignored: no user record found for phone ${trigger.phoneNumber}. Verify the phone number and retry.`\n  }\n}];"}
```

### Prepare WF-51 Payload (Wrong State)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Load User by Phone').first().json;\n\nreturn [{\n  json: {\n    channelId: user.slack_channel_id,\n    messageText: `\u26a0\ufe0f REJECT ignored for ${user.name} (${user.phone_number}): user is in state '${user.status}', expected 'payment_submitted'. No action taken.`\n  }\n}];"}
```

### Reset User Status to payment_pending
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users\nSET status = 'payment_pending', updated_at = NOW()\nWHERE phone_number = $1","options":{"queryReplacement":"={{ $('When Executed by Another Workflow').first().json.phoneNumber }}"}}
```

### Update Payment Record
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.payments\nSET status = 'rejected',\n    rejected_at = NOW(),\n    rejection_reason = COALESCE($2, 'Payment not verified')\nWHERE id = $1\nRETURNING *","options":{"queryReplacement":"={{ $json.payment_id }}, {{ $json.rejectionReason || 'Payment not verified' }}"}}
```

### User Found?
- type: `n8n-nodes-base.if` (v2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":true,"leftValue":"","typeValidation":"strict"},"conditions":[{"id":"user-found-1","leftValue":"={{ $json.id }}","rightValue":"","operator":{"type":"string","operation":"notEmpty"}}],"combinator":"and"},"options":{}}
```

### User in Correct State?
- type: `n8n-nodes-base.if` (v2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":true,"leftValue":"","typeValidation":"strict"},"conditions":[{"id":"state-guard-wf34","leftValue":"={{ $json.status }}","rightValue":"payment_submitted","operator":{"type":"string","operation":"equals"}}],"combinator":"and"},"options":{}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

## Connections

- `Call WF-50 WhatsApp Sender` --[main#0]--> `Prepare WF-51 Payload (Notify Admin Rejected)` (main, input #0)
- `Load User by Phone` --[main#0]--> `User Found?` (main, input #0)
- `Prepare Rejection Message` --[main#0]--> `Call WF-50 WhatsApp Sender` (main, input #0)
- `Prepare WF-51 Payload (Notify Admin Rejected)` --[main#0]--> `Call WF-51 Notify Admin Rejected` (main, input #0)
- `Prepare WF-51 Payload (User Not Found)` --[main#0]--> `Call WF-51 Notify Admin User Not Found` (main, input #0)
- `Prepare WF-51 Payload (Wrong State)` --[main#0]--> `Call WF-51 Notify Admin Wrong State` (main, input #0)
- `Reset User Status to payment_pending` --[main#0]--> `Prepare Rejection Message` (main, input #0)
- `Update Payment Record` --[main#0]--> `Reset User Status to payment_pending` (main, input #0)
- `User Found?` --[main#0]--> `User in Correct State?` (main, input #0)
- `User Found?` --[main#1]--> `Prepare WF-51 Payload (User Not Found)` (main, input #0)
- `User in Correct State?` --[main#0]--> `Update Payment Record` (main, input #0)
- `User in Correct State?` --[main#1]--> `Prepare WF-51 Payload (Wrong State)` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Load User by Phone` (main, input #0)
