# WF-34 Payment Rejection Processor

- **ID:** `se82n3MUQ9xE5aEr`
- **Active:** true
- **Nodes:** 7

## Nodes

### Call WF-50 WhatsApp Sender
- type: `n8n-nodes-base.executeWorkflow` (v1.3)
- parameters:

```json
{"workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9","cachedResultName":"WF-50 Send WhatsApp"},"workflowInputs":{"mappingMode":"defineBelow","value":{}},"options":{}}
```

### Load User by Phone
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"SELECT u.*, p.id as payment_id\nFROM chinmay_astro.users u\nLEFT JOIN chinmay_astro.payments p ON p.user_id = u.id \n  AND p.status = 'pending_verification'\nWHERE u.phone_number = $1\nORDER BY p.created_at DESC\nLIMIT 1","options":{"queryReplacement":"={{ $json.phoneNumber }}"}}
```

### Prepare Rejection Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const userData = $('Load User by Phone').item.json;\n\nreturn {\n  json: {\n    phoneNumber: userData.phone_number,\n    messageType: \"interactive\",\n    interactivePayload: {\n      type: \"button\",\n      body: {\n        text: \"We couldn't verify your payment. Please check the details and try again.\\n\\nPayment Instructions:\\nAmount: ₹500\\nUPI ID: payments@chinmay\\nReference: Your name\\n\\nAfter payment, click the button below.\"\n      },\n      action: {\n        buttons: [\n          {\n            type: \"reply\",\n            reply: {\n              id: \"payment_completed\",\n              title: \"Payment Completed\"\n            }\n          }\n        ]\n      }\n    }\n  }\n};"}
```

### Reset User Status to payment_pending
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users\nSET status = 'payment_pending', updated_at = NOW()\nWHERE phone_number = $1","options":{"queryReplacement":"={{ $('When Executed by Another Workflow').first().json.phoneNumber }}"}}
```

### Send a message
- type: `n8n-nodes-base.slack` (v2.3)
- credentials: slackApi=Slack - Chinmay Astro
- parameters:

```json
{"select":"channel","channelId":{"__rl":true,"value":"C0A5B0ZE81E","mode":"list","cachedResultName":"chinmay-admin-commands"},"text":"=❌ Payment rejected for {{ $('Load User by Phone').item.json.name || 'User' }} (+{{ $('Load User by Phone').item.json.phone_number }})  User notified to retry payment.","otherOptions":{}}
```

### Update Payment Record
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.payments\nSET status = 'rejected',\n    rejected_at = NOW(),\n    rejection_reason = COALESCE($2, 'Payment not verified')\nWHERE id = $1\nRETURNING *","options":{"queryReplacement":"={{ $json.payment_id }}, {{ $json.rejectionReason || 'Payment not verified' }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```


## Connections

- `Call WF-50 WhatsApp Sender` --[main#0]--> `Send a message` (main, input #0)
- `Load User by Phone` --[main#0]--> `Update Payment Record` (main, input #0)
- `Prepare Rejection Message` --[main#0]--> `Call WF-50 WhatsApp Sender` (main, input #0)
- `Reset User Status to payment_pending` --[main#0]--> `Prepare Rejection Message` (main, input #0)
- `Update Payment Record` --[main#0]--> `Reset User Status to payment_pending` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Load User by Phone` (main, input #0)
