---
wf_id: WF-34
n8n_uuid: se82n3MUQ9xE5aEr
active: true
node_count: 8
live_updated_at: 2026-05-26T02:47:08.073Z
generated_at: 2026-05-26T05:37:28Z
---

# WF-34 Payment Rejection Processor

- **ID:** `se82n3MUQ9xE5aEr`
- **Active:** true
- **Nodes:** 8

## Nodes

### Call WF-50 WhatsApp Sender
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-51 Notify Admin Rejected
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Load User by Phone
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"SELECT p.id as payment_id\nFROM chinmay_astro.payments p\nJOIN chinmay_astro.users u ON p.user_id = u.id\nWHERE u.phone_number = $1\n  AND p.status = 'pending_verification'\nORDER BY p.created_at DESC\nLIMIT 1","options":{"queryReplacement":"={{ $json.phoneNumber }}"}}
```

### Prepare Rejection Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const trigger = $('When Executed by Another Workflow').first().json;\n\nreturn [{ json: {\n  json: {\n    phoneNumber: trigger.user.phone_number,\n    messageType: \"interactive\",\n    interactivePayload: {\n      type: \"button\",\n      body: {\n        text: \"We couldn't verify your payment. Please check the details and try again.\\n\\nPayment Instructions:\\nAmount: \u20b9500\\nPlease send \u20b9500 via GPay / PhonePe / any UPI app to +91-9653240263 (Chinmay Mujumdar)\\n\\nAfter payment, tap the button below.\"\n      },\n      action: {\n        buttons: [\n          {\n            type: \"reply\",\n            reply: {\n              id: \"payment_completed\",\n              title: \"Payment Completed \u2713\"\n            }\n          }\n        ]\n      }\n    }\n  }\n} }];"}
```

### Prepare WF-51 Payload (Notify Admin Rejected)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const trigger = $('When Executed by Another Workflow').first().json;\nconst user = trigger.user;\nconst name = user.name || 'User';\nconst reason = (trigger.reason && trigger.reason.toString().trim()) ? trigger.reason : 'Payment not verified';\n\nreturn [{\n  json: {\n    channelId: user.slack_channel_id,\n    messageText: `\u274c Payment rejected for ${name} (+${user.phone_number}). Reason: ${reason}. User notified to retry payment.`\n  }\n}];"}
```

### Reset User Status to payment_pending
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users\nSET status = 'payment_pending', updated_at = NOW()\nWHERE phone_number = $1","options":{"queryReplacement":"={{ $('When Executed by Another Workflow').first().json.phoneNumber }}"}}
```

### Update Payment Record
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.payments\nSET status = 'rejected',\n    rejected_at = NOW(),\n    rejection_reason = COALESCE($2, 'Payment not verified')\nWHERE id = $1\nRETURNING *","options":{"queryReplacement":"={{ [$json.payment_id, $('When Executed by Another Workflow').item.json.reason || 'Payment not verified'] }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

## Connections

- `Call WF-50 WhatsApp Sender` --[main#0]--> `Prepare WF-51 Payload (Notify Admin Rejected)` (main, input #0)
- `Load User by Phone` --[main#0]--> `Update Payment Record` (main, input #0)
- `Prepare Rejection Message` --[main#0]--> `Call WF-50 WhatsApp Sender` (main, input #0)
- `Prepare WF-51 Payload (Notify Admin Rejected)` --[main#0]--> `Call WF-51 Notify Admin Rejected` (main, input #0)
- `Reset User Status to payment_pending` --[main#0]--> `Prepare Rejection Message` (main, input #0)
- `Update Payment Record` --[main#0]--> `Reset User Status to payment_pending` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Load User by Phone` (main, input #0)
