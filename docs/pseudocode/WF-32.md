---
wf_id: WF-32
n8n_uuid: emUOLWVZiNVxcOe3
active: true
node_count: 11
live_updated_at: 2026-05-22T21:50:46.672Z
generated_at: 2026-05-24T18:23:59Z
---

# WF-32 Payment Confirmation Receiver

- **ID:** `emUOLWVZiNVxcOe3`
- **Active:** true
- **Nodes:** 11

## Nodes

### Already Payment Submitted?
- type: `n8n-nodes-base.if` (v2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":true,"leftValue":"","typeValidation":"strict"},"conditions":[{"id":"status-check","leftValue":"={{ $json.user.status }}","rightValue":"payment_submitted","operator":{"type":"string","operation":"equals"}}],"combinator":"and"},"options":{}}
```

### Call WF-50 (Already Submitted)
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-50 (Send Payment Confirmation Received Message)
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Call WF-51 (Notify Admin)
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Create Payment Record
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"INSERT INTO chinmay_astro.payments (\n  user_id,\n  amount,\n  currency,\n  status,\n  payment_method\n)\nVALUES ($1, $2, $3, $4, $5)\nRETURNING *;","options":{"queryReplacement":"={{ $json.user.id }}, {{ 500 }}, {{ \"INR\" }}, {{ \"pending_verification\" }}, {{ \"gpay\" }}","largeNumbersOutput":"text"}}
```

### Load User Channel from DB
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"SELECT id, name, phone_number, slack_channel_id FROM chinmay_astro.users WHERE id = $1 LIMIT 1;","options":{"queryReplacement":"={{ $('When Executed by Another Workflow').item.json.user.id }}"}}
```

### Prepare Admin Notification
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Load User Channel from DB').first().json;\nconst channelId = user.slack_channel_id;\nconst paymentId = $('Create Payment Record').first().json.id;\n\nreturn [{\n  json: {\n    channelId: channelId,\n    messageText: '\ud83d\udd14 *New Payment Submission*\\n\\n*User:* ' + user.name + '\\n*Phone:* +' + user.phone_number + '\\n*Amount:* \u20b9500\\n*Payment ID:* ' + paymentId + '\\n\\nPlease verify payment and run:\\n`APPROVE PAYMENT ' + user.phone_number + '`'\n  }\n}];"}
```

### Prepare Reassurance Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('When Executed by Another Workflow').item.json.user;\nreturn [{\n  json: {\n    phoneNumber: user.phone_number,\n    messageType: 'text',\n    messageContent: 'Your payment has already been received and is under review \u2705 Chinmay will approve your consultation shortly \u2014 please wait a moment! \ud83d\ude4f'\n  }\n}];"}
```

### Prepare User Confirmation
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const user = $('Update User Status').first().json;\n\nreturn [{\n  json: {\n    phoneNumber: user.phone_number,\n    messageType: \"text\",\n    messageContent: `Thank you ${user.name}! \\n\\nYour payment confirmation has been received. \\n\\nWe'll verify your payment and activate your consultation within 24 hours.\\n\\nYou'll receive a confirmation message once approved.`\n  }\n}];"}
```

### Update User Status
- type: `n8n-nodes-base.postgres` (v2.6)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users \nSET \n  status = 'payment_submitted',\n  updated_at = NOW(),\n  last_message_at = NOW()\nWHERE id = $1\nRETURNING *;","options":{"queryReplacement":"={{ $('When Executed by Another Workflow').item.json.user.id }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```

## Connections

- `Already Payment Submitted?` --[main#0]--> `Prepare Reassurance Message` (main, input #0)
- `Already Payment Submitted?` --[main#1]--> `Create Payment Record` (main, input #0)
- `Call WF-50 (Send Payment Confirmation Received Message)` --[main#0]--> `Load User Channel from DB` (main, input #0)
- `Create Payment Record` --[main#0]--> `Update User Status` (main, input #0)
- `Load User Channel from DB` --[main#0]--> `Prepare Admin Notification` (main, input #0)
- `Prepare Admin Notification` --[main#0]--> `Call WF-51 (Notify Admin)` (main, input #0)
- `Prepare Reassurance Message` --[main#0]--> `Call WF-50 (Already Submitted)` (main, input #0)
- `Prepare User Confirmation` --[main#0]--> `Call WF-50 (Send Payment Confirmation Received Message)` (main, input #0)
- `Update User Status` --[main#0]--> `Prepare User Confirmation` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Already Payment Submitted?` (main, input #0)
