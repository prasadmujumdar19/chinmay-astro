# WF-21 New User Welcome + Form

- **ID:** `zM8WbxSdt9nXRoLZ`
- **Active:** true
- **Nodes:** 4

## Nodes

### Build Welcome Message
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const input = $('When Executed by Another Workflow').first().json;\nconst phoneNumber = input.phoneNumber;\nconst userId = input.id || null;\nconst messageId = input.messageId || null;\nconst userMessage = input.messageContent || null;\n\nconst policyUrl = 'https://chinmaymujumdar.com/privacy-policy';\n\nconst messageText = [\n  'Welcome! 🙏',\n  '',\n  \"I'm Chinmay Mujumdar, a Vedic astrology consultant.\",\n  '',\n  '📋 Privacy Policy: ' + policyUrl,\n  '',\n  '✨ How it works:',\n  '• Share your birth details using the form below',\n  '• Pay ₹500 consultation fee via GPay',\n  '• Receive your personalised Vedic astrology consultation on WhatsApp',\n  '',\n  'To get started, please fill in the form with your birth details.'\n].join('\\n');\n\nconst interactivePayload = {\n  type: 'flow',\n  header: { type: 'text', text: 'Birth Details Form' },\n  body: { text: messageText },\n  flowId: '1408011897720771',\n  flowCta: 'Fill Details'\n};\n\nreturn [{\n  json: {\n    phoneNumber,\n    phoneNumberFormatted: input.phoneNumberFormatted || phoneNumber,\n    messageType: 'interactive',\n    messageContent: messageText,\n    interactivePayload,\n    userId,\n    inboundMessageId: messageId,\n    userMessage\n  }\n}];"}
```

### Call WF-50 Send WhatsApp
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9","cachedResultName":"WF-50 Send WhatsApp"},"workflowInputs":{"mappingMode":"defineBelow","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"options":{}}
```

### Insert Pending User
- type: `n8n-nodes-base.postgres` (v2.6)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"INSERT INTO chinmay_astro.pending_users (phone_number, contact_name, created_at)\nVALUES ($1, $2, NOW())\nON CONFLICT (phone_number) DO UPDATE \n  SET contact_name = EXCLUDED.contact_name,\n      created_at = NOW()\nRETURNING phone_number","options":{"queryReplacement":"={{ $json.phoneNumber }}, {{ $json.contactName }}"}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```


## Connections

- `Build Welcome Message` --[main#0]--> `Call WF-50 Send WhatsApp` (main, input #0)
- `Insert Pending User` --[main#0]--> `Build Welcome Message` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Insert Pending User` (main, input #0)
