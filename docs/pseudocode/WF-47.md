---
wf_id: WF-47
n8n_uuid: 2U7mxHMyqA41ROKX
active: true
node_count: 9
live_updated_at: 2026-05-21T10:42:56.674Z
generated_at: 2026-05-22T11:49:58Z
---

# WF-47 Unsubscribe Handler

- **ID:** `2U7mxHMyqA41ROKX`
- **Active:** true
- **Nodes:** 9

## Nodes

### Close Open Consultation
- type: `n8n-nodes-base.postgres` (v2.5)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.consultations\nSET status = 'closed',\n    ended_at = NOW(),\n    closed_by = 'user_opted_out'\nWHERE user_id = $1\n  AND status = 'active';","options":{"queryReplacement":"={{ [$('When Executed by Another Workflow').first().json.userId] }}"}}
```

### Has Slack Channel?
- type: `n8n-nodes-base.if` (v2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":true,"leftValue":"","typeValidation":"loose"},"conditions":[{"id":"9b7fa7cb-8d60-488c-82fd-9e0113313b75","leftValue":"={{ $('Update User Status to opted_out').first().json.slack_channel_id }}","rightValue":"","operator":{"type":"string","operation":"notEmpty","singleValue":true}}],"combinator":"and"},"options":{}}
```

### Notify Admin Opt-out via WF-51
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"list","cachedResultUrl":"/workflow/wlZRK0YxnhP0b2RL"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{}}
```

### Prepare WF-50 Payload (Opt-out Confirmation)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const t = $('When Executed by Another Workflow').first().json;\n\nreturn [{\n  json: {\n    phoneNumber: t.phoneNumber,\n    messageType: 'text',\n    messageContent: 'You have been unsubscribed from Chinmay Astro. No further messages will be sent unless you reach out to us again.'\n  }\n}];"}
```

### Prepare WF-51 Payload (Opt-out Notice)
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const trig = $('When Executed by Another Workflow').first().json;\nconst upd  = $('Update User Status to opted_out').first().json;\n\nreturn [{\n  json: {\n    channelId: upd.slack_channel_id,\n    messageText: '\u26a0\ufe0f User has opted out via STOP (phone: ' + trig.phoneNumber + '). This consultation will not progress further. (Channel preserved per DR-10 in case of REBOOK.)'\n  }\n}];"}
```

### Send Opt-out Confirmation via WF-50
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"operation":"call_workflow","source":"database","workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"list","cachedResultUrl":"/workflow/BUVun38WEKb12zg9"},"workflowInputs":{"mappingMode":"passthrough","value":{},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"once","options":{"waitForSubWorkflow":true}}
```

### Update User Status to opted_out
- type: `n8n-nodes-base.postgres` (v2.5)
- parameters:

```json
{"operation":"executeQuery","query":"UPDATE chinmay_astro.users\nSET status = 'opted_out',\n    current_consultation_id = NULL,\n    updated_at = NOW()\nWHERE phone_number = $1\nRETURNING slack_channel_id;","options":{"queryBatching":"independently","queryReplacement":"={{ [$('When Executed by Another Workflow').first().json.phoneNumber] }}"}}
```

### Was Consultation Active?
- type: `n8n-nodes-base.if` (v2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":true,"leftValue":"","typeValidation":"strict"},"conditions":[{"id":"63fa4496-701c-486e-ba7e-b7b2fb96d6ef","leftValue":"={{ $('When Executed by Another Workflow').first().json.userStatus }}","rightValue":"consultation_active","operator":{"type":"string","operation":"equals"}}],"combinator":"and"},"options":{}}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1)
- parameters:

```json
{}
```

## Connections

- `Close Open Consultation` --[main#0]--> `Has Slack Channel?` (main, input #0)
- `Has Slack Channel?` --[main#0]--> `Prepare WF-51 Payload (Opt-out Notice)` (main, input #0)
- `Has Slack Channel?` --[main#1]--> `Prepare WF-50 Payload (Opt-out Confirmation)` (main, input #0)
- `Notify Admin Opt-out via WF-51` --[main#0]--> `Prepare WF-50 Payload (Opt-out Confirmation)` (main, input #0)
- `Prepare WF-50 Payload (Opt-out Confirmation)` --[main#0]--> `Send Opt-out Confirmation via WF-50` (main, input #0)
- `Prepare WF-51 Payload (Opt-out Notice)` --[main#0]--> `Notify Admin Opt-out via WF-51` (main, input #0)
- `Update User Status to opted_out` --[main#0]--> `Was Consultation Active?` (main, input #0)
- `Was Consultation Active?` --[main#0]--> `Close Open Consultation` (main, input #0)
- `Was Consultation Active?` --[main#1]--> `Has Slack Channel?` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Update User Status to opted_out` (main, input #0)
