# WF-40 User -> Admin Relay

- **ID:** `du32QBZbSQOjfESe`
- **Active:** true
- **Nodes:** 7

## Nodes

### Call WF-50 (J-19 STOP Response)
- type: `n8n-nodes-base.executeWorkflow` (v1.2)
- parameters:

```json
{"workflowId":{"__rl":true,"value":"BUVun38WEKb12zg9","mode":"id"},"workflowInputs":{"mappingMode":"defineBelow","value":{"phoneNumber":"={{ $json.phoneNumber }}","messageType":"={{ $json.messageType }}","messageBody":"={{ $json.messageBody }}"},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true},"mode":"each","options":{}}
```

### Call WF-51 (Post to Slack)
- type: `n8n-nodes-base.executeWorkflow` (v1.1)
- parameters:

```json
{"workflowId":{"__rl":true,"value":"wlZRK0YxnhP0b2RL","mode":"id"},"workflowInputs":{"mappingMode":"defineBelow","value":{"channelId":"={{ $json.channelId }}","messageText":"={{ $json.messageText }}"},"matchingColumns":[],"schema":[],"attemptToConvertTypes":false,"convertFieldsToString":true}}
```

### Format Slack Message
- type: `n8n-nodes-base.set` (v3.4)
- parameters:

```json
{"assignments":{"assignments":[{"id":"f1","name":"channelId","value":"={{ $('Load User Record').item.json.slack_channel_id }}","type":"string"},{"id":"f2","name":"messageText","value":"=📲 *{{ $('Load User Record').item.json.name }}:* {{ $('When Executed by Another Workflow').item.json.messageContent }}","type":"string"}]},"options":{}}
```

### Is STOP Intercept
- type: `n8n-nodes-base.if` (v2.2)
- parameters:

```json
{"conditions":{"options":{"caseSensitive":false,"typeValidation":"loose","version":2,"leftValue":""},"conditions":[{"id":"stop-check-001","leftValue":"={{ ($json.messageContent || '').trim().toUpperCase() }}","rightValue":"STOP","operator":{"type":"string","operation":"equals"}}],"combinator":"and"},"options":{}}
```

### Load User Record
- type: `n8n-nodes-base.postgres` (v2.5)
- credentials: postgres=PostgreSQL - Chinmay Astro Production
- parameters:

```json
{"operation":"executeQuery","query":"=SELECT id, name, phone_number, slack_channel_id FROM chinmay_astro.users WHERE phone_number = '{{ $json.phoneNumber }}' LIMIT 1"}
```

### Prepare J-19 Response
- type: `n8n-nodes-base.code` (v2)
- parameters:

```json
{"jsCode":"const input = $('When Executed by Another Workflow').first().json;\nreturn [{\n  json: {\n    phoneNumber: input.phoneNumber,\n    messageType: 'text',\n    messageBody: 'Your consultation with Chinmay is currently active. STOP cannot be used during a live consultation — please continue your conversation here. If you would like to end the consultation, please ask Chinmay to close it.'\n  }\n}];"}
```

### When Executed by Another Workflow
- type: `n8n-nodes-base.executeWorkflowTrigger` (v1.1)
- parameters:

```json
{"inputSource":"passthrough"}
```


## Connections

- `Format Slack Message` --[main#0]--> `Call WF-51 (Post to Slack)` (main, input #0)
- `Is STOP Intercept` --[main#0]--> `Prepare J-19 Response` (main, input #0)
- `Is STOP Intercept` --[main#1]--> `Load User Record` (main, input #0)
- `Load User Record` --[main#0]--> `Format Slack Message` (main, input #0)
- `Prepare J-19 Response` --[main#0]--> `Call WF-50 (J-19 STOP Response)` (main, input #0)
- `When Executed by Another Workflow` --[main#0]--> `Is STOP Intercept` (main, input #0)
